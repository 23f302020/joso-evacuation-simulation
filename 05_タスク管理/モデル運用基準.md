# モデル運用基準

> 作成日：2026/07/09  
> 目的：本研究で使用するAIモデル・エージェントの役割分担を固定し、判断の再現性と記録の一貫性を担保する。

本研究では作業の性質ごとにモデルを使い分ける。特に**研究方針の判断**は、論点整理・判断・記録を別のモデルに分離することで、判断者が自らの整理を追認する構図を避ける。

---

## 1. モデル一覧と担当

| モデル | モデルID | 担当する作業 |
|--------|---------|-------------|
| **Opus 4.8** | `claude-opus-4-8` | 論点整理、事実検証、実装、オーケストレーション、記録 |
| **Fable 5** | `claude-fable-5` | 研究方針の**判断**（方法論・仮定・会計方式の決定） |
| **Sonnet 5** | `claude-sonnet-5` | 判断記録の文書化、既存書式への整形 |
| **Haiku 4.5** | `claude-haiku-4-5-20251001` | 定型処理（テキスト整形・ログ分類・データ抽出） |
| **Codex** | — | 既存コードの修正・デバッグ、テスト実行、動作確認（`/codex:rescue`） |

サブエージェント呼び出し時の `model` 引数は `opus` / `fable` / `sonnet` / `haiku` を指定する。

---

## 2. 作業種別ごとの担当

| 作業 | 担当 | 備考 |
|------|------|------|
| Python スクリプト新規実装 | Opus（Claude Code） | |
| コード修正・テスト実行・動作確認 | Codex（`/codex:rescue`） | |
| 研究方針の判断 | **Fable 5** | 下記§3のプロセスに従う |
| 判断記録の文書化 | Sonnet 5 | 書式は既存メモに揃える |
| 文書作成・調査・タスク管理 | Opus（Claude Code） | |
| テキスト整形・ログ分類 | Haiku 4.5 | 繰り返しが多い場合のみ |

---

## 3. 研究方針の判断プロセス（3段階分離）

方針判断は必ず次の順で行い、各段階を別モデルが担当する。

```
論点整理（Opus）→ 判断（Fable 5）→ 記録（Sonnet 5 または Opus）
```

| 段階 | 担当 | やること | やらないこと |
|------|------|---------|-------------|
| **論点整理** | Opus | 関連文書・実装・実測値を読み、事実と論点を抽出して判断者に渡す | 結論を出さない |
| **判断** | Fable 5 | 方法論上の決定を下す。提案の追認ではなく批判的検証を行う | ファイルの作成・編集をしない |
| **記録** | Sonnet 5 / Opus | 判断内容を既存書式のメモに書き起こす | 数値を再導出しない・判断を変えない |

### 段階分離の理由

- 論点を整理したモデルがそのまま判断すると、自らの整理の枠内でしか結論が出ない。判断者を分けることで前提そのものを疑える。
- 記録者は判断結果を写すだけに徹する。数値の再導出を許すと、記録段階で判断と食い違う値が混入する。
- **判断の入力となる数値は、記録前に必ず実ファイルで検証する**（判断が誤った数値の上に立っていないことを担保するため）。

### 判断メモの保存先

`開発メモ/方針判断_fable5/{段階フォルダ}/{論点名}_方針判断_fable5.md`

書式は `車両会計_方針判断_fable5.md` に揃える（メタ情報テーブル → 判断 → 根拠 → 確定パラメータ表 → 留意点・未解決 → 波及タスク → 確信度 → 参照）。メタ情報テーブルには必ず「プロセス」欄を設け、担当モデルを明記する。

> **注意：** `開発メモ/` は `.gitignore` 対象のため、判断メモはリモートに残らない（ローカル運用）。

---

## 4. 判断メモに必須の記載事項

| 項目 | 内容 |
|------|------|
| 判断日 | 絶対日付で記す |
| プロセス | 論点整理・判断・記録の担当モデル |
| 前提正本 | 判断が依拠する確定済み文書のパス |
| 結論 | 1〜2文で判断の核心 |
| 確信度 | 項目別に 高／中／低 を付す（総合だけでは不可） |
| 状態 | 方針確定か、実装着手済みか |

確信度を項目別に付すのは、後続タスクが「どの前提から検証すべきか」を判断できるようにするため。

---

## 参照

- `CLAUDE.md`（エージェント分担の要約）
- `開発メモ/方針判断_fable5/01_基礎方針/車両会計_方針判断_fable5.md`（書式の基準）
- `開発メモ/方針判断_fable5/02_実行系是正/バス輸送実測_方針判断_fable5.md`（3段階分離の適用例）
