# 決定128実装手順（オプションC）

## メタ
- 作成：2026-07-13／記録：Sonnet 5
- 目的：決定128（実データ一本化＋訂正カスケード・run無し）を段階的に実施し、4値ゲート合格を確認してから文書差し戻しへ進む
- 正本判断：`開発メモ/方針判断_fable5/07_段4_結論・感度/段4_計画判断_方針判断_fable5.md`（決定126〜130）
- 前提：主結論ヌルは不変（正13/負2も非一貫）。本手順は監査値の是正のみ。新規SUMO run不要

## 前提確認（着手前）
1. `git status`・`git log --oneline -1` で HEAD が **`e2d6c55`（現HEAD）** であることを確認（旧手順の`419ceaf`は誤り＝会話開始時のHEAD・E1/S系/V系の3コミット前。巻き戻し禁止＝決定131）。e2d6c55は 21c5eb3 E1＋e2004d7ハードコード＋e2d6c55 S系/V系 を含み、ハードコードを前方で除去する
2. 確定route `scenario_b.rou.xml` SHA `3d6977…8297`・CRLF が無傷か確認（**決定86：`git restore`厳禁**。band script修正はrouteに触れないが念のため）
3. band scriptを現状のまま1回実行し `build_metrics` が A={1341,1066,1349}・B={1273,1304,1298,1330,1317} を出すことを再確認（再生成入力の基準固定）

## 段C-1：コード修正（run無し）
- **`scripts/p3_phase3r_e1_bands.py`**：`VERIFIED_COMPLETION_RATES` 辞書を削除／符号計算（121-126行）の `a_rates.get("raw", a["raw_completion_rate"])` を `a["raw_completion_rate"]` へ変更しハードコード参照を除去／**自己整合assert新設（決定129）**＝band_summaryに書く `a_completion_rate_values` 等がper-run行（replicate_metrics）と一致することをassert、乖離で例外halt
- **`scripts/p3_s10_sensitivity_metrics.py`**：同型ハードコードA集合（{75.02,96.58}）があれば実データ算出A（{75.87,96.01}）へ差し替え／**保守列新設（決定123）**＝各S10 runのbusを `min(n',124.2)` にcapした `conservative_completion_rate`・保守15組符号・`conservative_sign_counts` を出力
- **テスト**：`tests/unit/test_phase3r_e1_bands.py`・`test_phase3_e2_comparison.py` は誤値（正10/負5・+0.23）をassertしている可能性が高く、修正後は正値で失敗する（＝正しい失敗）→期待値を正値（正13/負2・+1.23）へ更新
- **テスト脱循環化（最重要・決定132）**：`test_verified_completion_rates_produce_decision_110_sign_counts` 等ハードコード値をassertするテストを削除または「アーカイブ実算出＋独立地上真実（tripinfo/vehicle_log到着数 A#1=1341/A#2=1066/A#3=1349等）へのassert」へ改稿。band_summaryをband_summaryで確かめる形を禁止
- **旧164行復活禁止（決定132）**：削除された従指標表を旧コードで無検証復活させない（撤回値0.271255再混入リスク・復活は正しいbus_transport=125で実データ確認後のみ）
- **従指標の所在確認（決定132・別タスク・E3着手前）**：需要充足率50.6%/3.8%・乗車中孤立16・削減54・not_arrived・terminated便の単一正本ソースを文書化
- 検証C-1：`py_compile` 通過／`VERIFIED_COMPLETION_RATES` 全参照が `grep` で0件

## 段C-2：成果物の再生成（run無し）
1. band script → `phase3r_e1_replicate_metrics.csv`・`phase3r_e1_band_summary.json`・`phase3r_e1_15_combination_signs.csv`
2. S系script → `phase3_s10_replicate_metrics.csv`（保守列追加）・`phase3_s10_band_summary.json`（保守符号追加）・`phase3_s10_15_combination_signs.csv`（保守列追加）
3. E2 → `phase3_ab_comparison.csv`
4. Excel → `p3_build_phase3_excel.mjs`（S系保守含む・102%はraw保持＋保守併記）
5. V2/V3 HTML → `phase3_viz.html`・`phase3.html`（数値ラベル+1.23・S系保守追加）
- 検証C-2：各ファイルmtime更新／band_summaryの `a_completion_rate_values` が {95.44,75.87,96.01} になっていること（ハードコード除去の実証）

## 段C-3：4値ゲート＋自己整合ゲート（停止・報告ポイント）
※ゲートは「テスト通過」で代替せず、ディスク上の生成物ファイルをReadして実測再検証する（決定135・正本は再生成まで旧値のまま・OneDrive同期消失の前科）。
再生成後に機械チェックし、1つでも外れたら即halt→Fable（決定128・130）。全合格でユーザーへ報告し段C-4承認を得る：

| 検査 | 期待値 |
|---|---|
| 主系列 raw符号 | 正13/負2 |
| 主系列 保守符号 | 正13/負2 |
| S系 raw符号 | 正10/負5 |
| S系 保守符号 | 正8/負7 |
| raw点推定／帯 | +1.2348%pt／[−3.09,+22.97] |
| 保守点推定／帯 | +1.2099644%pt／[−3.088349,+22.633452]（max combo=B#4対A#2・旧+21.71は決定139で撤回） |
| 自己整合 | band_summary値＝replicate_metrics値（乖離0） |

## 段C-4：文書の系統的差し戻し（ゲート合格後のみ）

**決定141（二層方針）**：文書差し戻しは以下の二層で扱う。
- **現状記述文書**（Phase3_研究結果・CLAUDE.md・評価フレーム・RQ・用語定義・モデル仮定・規模整合・論文構成）＝旧値を全て正本値へ**全差し戻し**。
- **判断記録**（決定105〜140の経緯・段4メモ本体）＝当時の値は監査記録として**保持**し、前方訂正ポインタを付す（改竄禁止・研究誠実性の資産）。
- **例外＝段4メモの⚠️注記のみ訂正必須**：これは過去判断の記録ではなく「+0.23/正10負5が是正値」という正誤逆の能動的主張であり、汚染された断定として訂正する。区別＝(a)過去判断の値の記録→保持＋注記／(b)現在の正本を誤って主張→訂正。

`+0.23→+1.234721`／`正10負5→正13負2`／`保守+0.13→+1.209964`／`raw帯→[−3.088349,+22.970757]`／`保守帯[−4.23,+21.32]および[−3.09,+21.71]→[−3.088349,+22.633452]`（決定139・旧+21.71は撤回） を置換：
- `06_研究結果/phase3/Phase3_研究結果.md`
- 先行更新した研究文書群（評価フレーム・RQ・用語定義・モデル仮定・規模整合・論文構成 等のうち該当値を持つもの）
- CLAUDE.md（注記済み誤値行を正値へ本置換）・進捗ボード
- 撤回値ブロック注意：line 24等の「引用禁止・監査用」表は歴史記録として保持（V-3でPASS済）。対象は"是正済み正値"として提示していた+0.23系のみ
- 検証C-4：現状記述文書横断 `grep` で `+0.23%pt`・`正10/負5`・`[−4.2,+22.7]` が"正値"として残っていないこと（監査用撤回表・判断記録本体は除外）／pytest緑

## 段C-4'：従指標nativeソース文書化（決定143・C-4と並行可）

各従指標のnativeソースを明示し、**補正値を実測確認**してから文書化する（E3着手前に完了必須）：
- 需要充足率 50.6% ＝ `scenario_b_bus_summary.json` の `bus_arrived_passengers(125)/initial_bus_candidate_total(247)`。3.8% ＝ 125/3255（Type3/4総数）。
- 乗車中孤立16 ＝ `scenario_b_bus_summary.json`（terminated便車内・not_arrived内訳）。
- 削減54 ＝ `scenario_b_rescue_reduction.csv`（`rescue_removed_count`総和・外生固定）。
- not_arrived（従指標ヌル 正2/負13・決定101）＝ replicate not_arrived系列。
- terminated便数 ＝ `scenario_b_bus_summary.json` の `termination_by_reason`。
- **【必須】各nativeソースが補正値（bus_arrived=125・削減54）を示すことを実測確認**してから文書化（旧撤回値141/67/0.271の再混入防止・決定132項2）。**旧164行equityコードは復活させない。**

## 段C-5：単一コミット＋最終確認
**`git add .`／`git add -u` 全面禁止**。明示allowlist（4 scripts/tests＋再生成正本〔E1/S10/E2/Excel/V2 phase3_viz.html/V3 phase3.html〕＋未追跡 p3_build_phase3_excel.mjs＋段C-4文書）をパス個別add。

**決定142（allowlist拡張・最重要）**：
- allowlistに**gen_index.py・p3_build_visualization.py**を追加（C-2の正当な生成器変更）。
- **【最重要】gen_index.py を再実行しない**：index.html・phase1.html・phase2.html・phase1 assetsを上書きする多ページ生成器。phase3.htmlは既にディスク上で正しいので再生成不要。再実行すれば未変更の他ページを巻き込みallowlistが破れる。
- **output系はパス明示で個別add・churn分類必須**：実内容差分（`--ignore-cr-at-eol`で残る）＝**phase3.html・phase3_viz.html のみ**＋E1/S/E2データCSV/JSON＋Excel(.xlsx)。CRLF churnのみ（phase1-pages.js・phase1-components.js）と未変更（index/phase1/phase2）は除外し、**一括addを禁止**。

**除外＝`scenario_b.rou.xml`（scenarios/・results/両方・決定86・CRLF churn・stage/restore禁止）＋無関係EOL churn ~250。**

コミット前ゲート＝`git diff --cached --name-only` で(i)route不在(ii)churn非混入(iii)**index.html/phase1.html/phase2.html/phase1-*.jsがstaged外**を目視。コミット後＝route SHA `3d6977…8297` 不変を実測。

- 未追跡 `p3_build_phase3_excel.mjs` を単一コミットに含める（決定133）。順序＝band系再生成→.mjs再実行→正しいExcelとともにコミット（混在生成物はコミットしない）
- 上記allowlistで `git add <path>` を個別実行 → 1コミット（メッセージに「e2004d7回帰の是正・正13/負2復元・ハードコード除去」を明記）
- `git status` で allowlist外に想定外変更がないこと・確定route SHA無傷を最終確認

## 分担
- 段C-1（コード修正＋テスト更新）：`/codex:rescue` 委任 or Claude Code直接（着手時に決定）
- 段C-2〜C-3（再生成＋ゲート）：Claude Codeが実行・4値検証・報告
- 段C-4（文書差し戻し）：Claude Code直接
- 段C-4'（従指標文書化・決定137）＝C-4と並行・E3前ゲート必須・旧equityコード復活に依存しない

## halt条件
- 段C-3の4値/自己整合ゲートで1つでも不一致 → 文書を一切触らずFable差し戻し
- 再生成入力（A/B per-run値）が前提確認3の基準と異なる → halt

## E3前 最終go/no-goゲート（決定138・決定144で更新）

順序：C-2 再生成（決定135・run無し）→ C-3 ディスク4値ゲート実測再検証（テスト非依存・ファイルReadで確認）→〔C-4 文書差し戻し ∥ C-4' 従指標文書化（決定137・143）〕→ 決定136・142 allowlistコミット（コミット前後ゲート：route SHA・staged名簿・index/phase1/phase2誤stage検査）→ 【E3前 最終go/no-goゲート】。

**E3着手の最終ゲート（決定144・全PASSでのみgo）**：
1. ディスク実測で band/S10/replicate/E2/Excel/V2/V3 が raw正13負2・保守正13負2・S raw正10負5・S保守正8負7・**raw帯[−3.088349,+22.970757]・保守帯[−3.088349,+22.633452]**・per-run=tripinfo(1341/1066/1349) で整合（raw/保守両帯照合。保守点+1.2099644%pt・保守max combo=B#4対A#2・決定139・旧+21.71は撤回）
2. **現状記述文書**（CLAUDE.md・研究結果・下流研究文書）で +0.23→+1.23／正10負5→正13負2 差し戻し済・旧値の残存ゼロをgrep実測（**判断記録〔`開発メモ/方針判断_fable5/`配下〕は明示的除外リストとしてgrep-zero対象外・段4メモ⚠️注記の正誤逆のみ訂正済であることを確認**・決定141・144）
3. `scenario_b.rou.xml`がコミットに不在・route SHA `3d6977…8297`不変・**index.html/phase1.html/phase2.html/phase1-*.jsが誤stageされていない**（決定142）
4. 従指標の単一正本ソース文書化済＋補正値実測確認（決定137・143）
5. 循環テスト残存ゼロ（地上真実assertのみ・決定132）

※帯のmin/maxは15組全数のmax/minから取る（raw argmax runを保守へ引き継がない・決定140・cap124.2がargmaxを組替える）。

このゲートはFable差し戻し対象（C-3のディスク実測が4値を再現しない、または現状記述文書に旧値残存が見つかればE3着手前にhalt）。

**恒久規範（決定129＋134）**：git SHA・完了率・符号カウント等の陳腐化しうる値を手順書/判断にハードコードせず「現HEAD/現アーカイブを実測」と書く。真実源はアーカイブ算出とgit実測のみ・ミラーのassert禁止

### 詳細実行手順

#### 全体の流れ
```
Opus(判断者でなく実測)が5条件を read-only 実測（保存値を信じずディスク/gitを全数照合＝決定129/135）
  → 全PASS → Fableが最終裁可（決定138：本ゲートはFable差し戻し対象）→ E3本文着手
  → 1つでもFAIL → halt→Fable（E3着手禁止）
記録はSonnet（判定結果を写す）
```

#### 条件1：ディスク4値＋両帯＋per-run地上真実の整合
- **何を**：再生成済み正本ファイルが正値を保持（テスト緑でなくファイル実物を読む・決定135）。
- **方法**：`phase3r_e1_band_summary.json` をRead→`raw_sign_counts={正13,負2}`・`conservative_sign_counts={正13,負2}`・`raw_delta_[min,max]_percentage_points=[−3.088349,+22.970757]`・`conservative_delta_[min,max]=[−3.088349,+22.633452]`／`phase3_s10_band_summary.json`→`sign_counts={正10,負5}`・`conservative_sign_counts={正8,負7}`／`phase3r_e1_replicate_metrics.csv` のper-run rescue到着=A#1=1341/A#2=1066/A#3=1349 を各run archiveの `scenario_a_tripinfo.xml` 独立パース値と照合／E2 `phase3_ab_comparison.csv`・Excel・phase3.html・phase3_viz.html が同値表示。
- **合格基準**：すべて一致。**現状**：f404837コミット前に実測確認済み（保守max=B#4対A#2も確認）。ゲートでは再読み込みで再確認。

#### 条件2：現状文書の旧値残存ゼロ（監査記録は除外）
- **何を**：現状記述文書に `+0.23`／主系列の`正10負5`／旧帯 が**現在値として**残っていないか。
- **方法**：`grep -rnE "\+0\.23|正10/負5|正8/負7|22\.6651|21\.32|21\.71|4\.2266"` を現状文書（`06_研究結果`・`03_研究設計文書`・両CLAUDE.md）に実行。**除外リスト（決定144）**：`開発メモ/方針判断_fable5/`・`01_経過報告`・CLAUDE.mdの日付付き決定バブル・`Phase3_研究結果.md`の訂正バナー＋符号表CSV行（旧値を誤りと明示する文脈）。ヒットを1件ずつ文脈判定（現在値の主張＝FAIL／旧値を誤りと説明・監査記録＝PASS）。**主系列 vs S系の判別**：`正10/負5`はS系では正しいのでS系文脈ならPASS。
- **合格基準**：現在値主張としての旧値ゼロ。**注意**：最も文脈判断を要する（機械grepだけで白黒つかない）。

#### 条件3：route不変・禁止ファイル非混入
- **何を**：確定route無傷・コミットの健全性。
- **方法**：`sha256sum scenario_b.rou.xml`=`3d697750…8297`（決定86）／route bare LF=0（CRLF維持）／`git show --name-only HEAD`(f404837)に `scenario_b.rou.xml`・`index.html`・`phase1.html`・`phase2.html`・`phase1-*.js` が不在。
- **合格基準**：SHA一致・LF0・禁止ファイル非混入。**現状**：f404837直後に確認済み（route不変・非混入）。

#### 条件4：従指標nativeソース文書化＋補正値
- **何を**：E3が引く従指標がnativeソースから正しく文書化され撤回値でないか。
- **方法**：`Phase3_研究結果.md` §5「従指標の単一正本ソース」節の存在確認／各nativeソース再Read（需要充足率=`bus_summary` candidate247/arrived125・削減54=`rescue_reduction.csv` `rescue_removed_count`総和54.0・孤立16=`bus_corrected_accounting.json`）／撤回値（67/141/29/0.271）非混入。
- **合格基準**：§5存在・全ソースが補正値・撤回値ゼロ。**現状**：C-4'で追加・実測済み。

#### 条件5：循環テスト残存ゼロ
- **何を**：テストが地上真実assertのみで自己照合の循環がないか。
- **方法**：`grep -rn "VERIFIED_COMPLETION_RATES|decision_110_sign_counts" scripts/ tests/`=0件／`test_phase3r_e1_bands.py` がtripinfo独立パース（`ET.iterparse`）で到着数assert／関連2テストpass。
- **合格基準**：ハードコード0・循環テスト0・地上真実assert存在・テストpass。**現状**：段C-1で達成・f404837でコミット済み。

#### 判定の性質
- 条件1・3・4・5は機械的（実測で白黒）。ほぼf404837で達成済みゆえゲートは主に再確認。
- 条件2が唯一の判断含み（旧値ヒットが監査記録か現在値主張かの文脈判定）＝Opus実測→Fableが除外リストの妥当性と各ヒット分類を裁可。
- 全PASSでも決定138により最終goはFableが出す（自己判定で済ませない）。
