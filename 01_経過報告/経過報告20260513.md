# 経過報告（2026/05/13時点）

> 前回報告：2026/04/22（経過報告20260422.md）
> 報告期間：2026/04/22 〜 2026/05/13

---

## 今回の活動概要

前回報告（2026/04/22）に対する先生のコメントを受けてスコープを見直し（Part 1〜3）、Phase 1実装に必要な調査データの取得・確認を完了（Part 4〜5）、続いて実装仕様書・手順書の整備とPython環境構築・STEP 0（config.py）の実装を完了した（Part 6〜7）。その後、Phase 1の全スクリプト実装・動作確認、浸水ナビデータを用いた閉鎖時系列の再構築、HTML成果物確認ページの作成まで進めた（Part 8〜11）。

| # | 活動 | 概要 |
|---|------|------|
| 1 | **先生コメントの整理** | RQ文の承認・実装量過多の指摘・最低限の研究範囲の確認 |
| 2 | **スコープの再定義（3フェーズ化）** | 卒論として達成すべき最低条件（Phase 1）を明確化 |
| 3 | **タスク管理の更新** | 実装タスクI-1〜I-9を新規追加・優先順位を再整理 |
| 4 | **実装前調査タスク完了（2026/04/27）** | J-1/J-2/J-3/J-4/J-6完了、A31aデータ取得・構造確認、統合戦略決定 |
| 5 | **残存データの取得・確認完了（2026/04/28）** | J-7〜J-9完了、避難施設19件・車両台数25,698台・65歳以上人口16,860人確認 |
| 6 | **実装仕様書作成・Python環境構築完了（2026/04/28）** | 実装仕様書・requirements.txt・環境ツール記録を作成、Env-1〜3完了（pyogrio採用） |
| 7 | **実装手順書作成・STEP 0 完了・文書整合性修正（2026/04/28）** | 詳細手順書作成、config.py 実装完了、全文書の fiona→pyogrio 統一修正 |
| 8 | **Phase 1 STEP 1a〜3 実装・デバッグ・完了（2026/04/29〜30）** | c3/e1/i1/i2 を実装・PR マージ・空間照合バグ修正（案2+3複合）・道路封鎖リスト生成まで完了 |
| 9 | **PR #4 競合解消・動作確認（2026/04/30）** | Codex 新PR の競合を master 修正済み版で解消・全4スクリプトの連続実行動作確認完了 |
| 10 | **全スクリプト実用修正・i3完成（2026/04/30）** | Windows/OneDriveファイルロック対策・KMLバッファCRS修正・i3列名/座標式/最近傍ノード修正 |
| 11 | **浸水ナビデータ反映・Phase 1成果物整理（2026/05/01）** | BP030ハイドログラフを用いて閉鎖タイムラインを再構築、ルートHTMLに浸水範囲を重ね、確認用トップページを作成 |

---

## Part 1：先生コメントの内容と対応方針

### 1-1. 先生コメント（2026/04/22報告後）

> 「現在のRQ文で問題ない。しかし、1年間の研究として、実装が多い。そのため優先順位をつけての実装を行う。配車し、渋滞緩和シミュレーションをし、避難可能人数・避難時間のデータの取得まではいかなくとも、鬼怒川周辺の地図から、時間ごとに道路を封鎖し、封鎖を予測し、迂回ルートを検索するだけでも十分な研究内容となる。」

### 1-2. コメントの解釈

| コメントの要点 | 対応方針 |
|--------------|---------|
| RQ文は問題ない | G-5（RQ確認）を✅完了に更新。RQ文は現状維持 |
| 実装量が1年の研究として多い | 全実装を「必達」「余裕があれば」「さらに余裕があれば」の3段階に分離 |
| 最低限の研究内容の提示 | 地図取得・道路封鎖・迂回ルート検索をPhase 1（必達）と定義 |

---

## Part 2：スコープ再定義（3フェーズ化）

### 2-1. フェーズ定義

| フェーズ | 内容 | 位置づけ |
|---------|------|---------|
| **Phase 1（必達）** | 地図取得 → 浸水時系列データ取得 → 浸水ポリゴン×道路リンクの空間照合 → 時刻別道路封鎖リスト生成 → 迂回ルート検索 | 卒論提出の最低条件 |
| **Phase 2（余裕があれば）** | SUMOへの変換・車両エージェント投入・TraCIによる動的道路閉鎖（シナリオA渋滞再現） | Phase 1完了後に着手 |
| **Phase 3（さらに余裕があれば）** | シナリオB（バス追加）・A/B比較・避難完了人数・避難時間の集計 | 時間的余裕次第 |

### 2-2. 変更前後の比較

| 項目 | 変更前（2026/04/22時点） | 変更後（現在） |
|------|------------------------|--------------|
| 最終目標 | シナリオA/B比較・避難人数・避難時間の取得 | Phase 1〜3の段階的達成。Phase 1が最低条件 |
| 最優先実装 | SUMO+TraCIによる渋滞シミュレーション全体 | 道路封鎖リスト生成・迂回ルート検索（Pythonのみで完結可） |
| バス・渋滞比較 | 必達スコープ内 | Phase 3（さらに余裕があれば）に移動 |

---

## Part 3：タスク管理の更新

### 3-1. 新規追加した実装タスク（I-1〜I-9）

#### Phase 1（必達）

| タスクID | 内容 | 状態 |
|---------|------|------|
| I-1 | 浸水ポリゴン × 道路リンクの空間照合を実装 | ❌ 未着手 |
| I-2 | 時刻別の道路封鎖リストを生成するスクリプトを作成 | ❌ 未着手 |
| I-3 | 封鎖を考慮した迂回ルート検索を実装 | ❌ 未着手 |

#### Phase 2（余裕があれば）

| タスクID | 内容 | 状態 |
|---------|------|------|
| I-4 | SUMOへの道路NW変換（netconvert） | ❌ 未着手 |
| I-5 | 車両エージェント投入・基本走行確認（シナリオA） | ❌ 未着手 |
| I-6 | TraCIで浸水時系列に応じた動的道路閉鎖を実装 | ❌ 未着手 |

#### Phase 3（さらに余裕があれば）

| タスクID | 内容 | 状態 |
|---------|------|------|
| I-7 | バス（固定ルート・シナリオB）をSUMOに追加 | ❌ 未着手 |
| I-8 | 台数感度分析（3・5・10台） | ❌ 未着手 |
| I-9 | 出力データ集計・A/B比較・避難完了人数の取得 | ❌ 未着手 |

### 3-2. ステータス変更したタスク

| タスクID | 内容 | 変更前 | 変更後 | 理由 |
|---------|------|--------|--------|------|
| G-5 | RQ文を指導教員に提示・確認 | 🔴 未実施 | ✅ 完了 | 先生から「問題ない」と確認 |

---

## 現在の課題と今後の方針

### 解決済みの課題

| 課題 | 状況 |
|------|------|
| RQ文の指導教員確認 | ✅ 「現在のRQ文で問題ない」と確認済み |
| 実装スコープの過多 | ✅ 3フェーズに分離・Phase 1を最低条件として明確化 |

### 残っている課題

| 課題 | タスクID | フェーズ |
|------|---------|---------|
| osmnxで常総市の道路ネットワーク取得 | C-3 | Phase 1 |
| 国土地理院 浸水時系列KMLのダウンロード | E-1 | Phase 1 |
| 浸水ポリゴン × 道路リンクの空間照合実装 | I-1 | Phase 1 |
| 時刻別 道路封鎖リスト生成スクリプト | I-2 | Phase 1 |
| 迂回ルート検索の実装 | I-3 | Phase 1 |
| 浸水ナビAPIの試験呼び出し | E-6 | Phase 2 |

### 次回報告までの予定

1. osmnxで常総市の道路ネットワークを実際に取得する（C-3）
2. 国土地理院サイトから浸水時系列KML（D1-No.917）をダウンロードする（E-1）
3. Pythonで浸水ポリゴンと道路リンクの空間照合スクリプトを作成する（I-1）
4. 時刻別の道路封鎖リストを出力するスクリプトを完成させる（I-2）
5. 封鎖リストを使った迂回ルート検索を実装する（I-3）

---

## Part 4：実装前調査の完了（2026/04/27）

### 4-1. 今回の活動概要

前回報告後のタスクを受けて、Phase 1実装に必要な調査タスク（J系）を集中的に実施した。

| # | 活動 | 結果 |
|---|------|------|
| 1 | **GSI時系列KMLのデータ取得・構造確認（J-1）** | ✅ 完了 |
| 2 | **A31a洪水浸水想定区域GMLのダウンロード・構造確認（J-2）** | ✅ 完了 |
| 3 | **浸水深コードリスト確認・道路閉鎖しきい値決定（J-3）** | ✅ 完了 |
| 4 | **KML × A31a統合戦略の決定（J-4）** | ✅ Option B推奨 |
| 5 | **破堤時刻の確認（J-6）** | ✅ 2015/09/10 12:50 |
| 6 | **調査結果のドキュメント化** | ✅ J_実装前調査結果.md作成 |
| 7 | **CLAUDE.md作成（進捗のセッション間保存）** | ✅ 完了 |

---

### 4-2. GSI時系列KML（J-1）

**ステータス：✅ 確認済み**

D1-No.917（鬼怒川・常総市）の8時点KMLファイルを確認した。

| 項目 | 確認結果 |
|------|---------|
| ジオメトリ種別 | **LineStringのみ**（Polygonなし） |
| 深度属性 | **なし** |
| 時点数 | 8時点（2015/09/10 18:00 〜 2015/09/16 10:20） |
| 属性種別 | 浸水範囲・越水箇所・浸水範囲（変化分）（不明瞭） |

**⚠ 結論：KMLは浸水輪郭線のみ。深度情報はA31aから補完する必要がある。**

---

### 4-3. A31a洪水浸水想定区域GMLのダウンロード・構造確認（J-2）

**ステータス：✅ 完了**

国土数値情報のNLFTPポータルはJavaScriptレンダリングのため、Playwrightブラウザ自動操作でダウンロードURLを特定した。

| 項目 | 内容 |
|------|------|
| ダウンロード元 | `https://nlftp.mlit.go.jp/ksj/gml/data/A31a/A31a-24/A31a-24_08_10_GML.zip` |
| ファイルサイズ | 約12.8MB |
| 展開後ファイル数 | 84 XMLファイル（5サブディレクトリ） |
| CRS | JGD2011（EPSG:6668） |
| フィーチャ型 | `ksj:PlanScale` |
| ジオメトリ | `gml:Surface`（Polygon）|
| 深度属性 | `ksj:waterDepth`（整数コード1〜6） |

**GMLフィーチャ構造（抜粋）：**

```xml
<ksj:PlanScale gml:id="id0">
  <ksj:bounds xlink:href="#sf0"/>
  <ksj:riverNumber>8303030018</ksj:riverNumber>
  <ksj:riverName>鬼怒川</ksj:riverName>
  <ksj:waterDepth>2</ksj:waterDepth>
</ksj:PlanScale>
```

**鬼怒川（常総市）の河川番号：**

| riverNumber | 内容 |
|-------------|------|
| 8303030018 | 鬼怒川（利根川水系）本川 |
| 8303030046 | 同水系 支川 |
| 8303030064 | 同水系 支川 |

---

### 4-4. 浸水深コードリスト確認・道路閉鎖しきい値決定（J-3）

**ステータス：✅ 完了**

`water_depth_code.xlsx` をPythonで解析し、コード値の意味を確認した。

| コード | 浸水深範囲 | 道路閉鎖 |
|--------|-----------|---------|
| 1 | 0〜0.5m未満 | 対象外 |
| **2** | **0.5〜3.0m未満** | **✅ 閉鎖対象** |
| 3 | 3.0〜5.0m未満 | ✅ 閉鎖対象 |
| 4 | 5.0〜10.0m未満 | ✅ 閉鎖対象 |
| 5 | 10.0〜20.0m未満 | ✅ 閉鎖対象 |
| 6 | 20.0m以上 | ✅ 閉鎖対象 |

**✅ 道路閉鎖条件：`ksj:waterDepth >= 2`（浸水深0.5m以上）**

---

### 4-5. KML × A31a統合戦略（J-4）

**ステータス：✅ Option B推奨**

| 戦略 | 内容 | 評価 |
|------|------|------|
| Option A | KMLのLineStringを閉じてPolygon化し時系列で封鎖 | 深度しきい値を反映できない |
| **Option B（推奨）** | **A31a（深度ポリゴン）× KML（時系列）の組み合わせ** | 深度条件を正確に反映できる |

- A31a `waterDepth >= 2` のポリゴンで**封鎖対象道路を確定**
- KML時系列（8時点）で**封鎖タイミングを決定**

---

### 4-6. 破堤時刻確認（J-6）

**ステータス：✅ 確認済み**

| 項目 | 内容 |
|------|------|
| 破堤日時 | **2015年9月10日 12時50分** |
| 破堤箇所 | 茨城県常総市三坂町 |
| 出典 | Wikipedia「平成27年9月関東・東北豪雨」 |

KML最初の時点（t1 = 18:00）は破堤から+5時間10分後に相当する。

---

### 4-7. ドキュメント整備

以下のファイルを新規作成・更新した。

| ファイル | 内容 | 操作 |
|---------|------|------|
| `02_情報調査/J_実装前調査結果.md` | J-1〜J-9全調査結果のまとめ | 新規作成 |
| `05_タスク管理/調査タスク一覧.md` | J-1/J-2/J-3/J-6を✅完了に更新 | 更新 |
| `4年次本研究/CLAUDE.md` | セッション間の進捗保存用ドキュメント | 新規作成 |

---

### 4-8. 残存課題（手動作業が必要）

以下のデータはJavaScriptポータルのため自動ダウンロード不可。次のセッションまでに手動でダウンロードが必要。

| データ | 取得先 | 保存先 |
|--------|--------|--------|
| 避難所CSV（J-7） | `https://hinanmap.gsi.go.jp/` → 茨城県 → 常総市 → CSV出力 | `data/shelters/joso_shelters.csv` |
| 250mメッシュ人口CSV（J-8） | e-Stat 統計ID: T001109 → 2次メッシュ 543975 | `data/population_mesh/mesh_population_2015.csv` |
| 車両保有台数PDF（J-9） | 関東運輸局「茨城県 市区町村別 自動車保有車両数」H27 | `data/vehicle_stats/` |

---

### 4-9. 次回以降の実装方針

調査タスクの基盤が揃ったため、次の順序で実装に着手する。

1. **Env-1〜3**：Python仮想環境構築（osmnx, geopandas, pyogrio, networkx, folium, pyproj, shapely, pandas, matplotlib）
2. **C-3**：osmnxで常総市道路ネットワーク取得 → `/codex:rescue` に委任
3. **I-1**：A31a GML + KML → 浸水ポリゴン × 道路リンク 空間照合 → `/codex:rescue` に委任
4. **I-2**：時刻別道路封鎖リスト（JSON/CSV）生成
5. **I-3**：封鎖考慮済み迂回ルート検索（networkx Dijkstra）

---

## Part 5：残存データの手動取得・確認完了（2026/04/28）

### 5-1. 活動概要

| 作業 | 内容 | 結果 |
|------|------|------|
| ファイル整理 | 研究室フォルダ内のPDF/ZIPを `04_プログラム/data/` 配下に移動 | ✅ 完了 |
| ZIPファイル展開 | 各ZIPを適切なサブフォルダに展開し中身を確認 | ✅ 完了 |
| 小地域人口データ確認 | e-Stat CSVの構造・常総市該当値を確認 | ✅ 完了 |
| 避難施設データ確認 | 国土数値情報P20 ShapefileのDBF解析 | ✅ 完了 |
| 250mメッシュデータ確認 | T001178/T001208の列構成・常総市メッシュを確認 | ✅ 完了 |
| 車両保有台数確認 | PDFをMD変換済み、常総市の値を読み取り | ✅ 完了 |
| データカタログ作成 | `02_情報調査/データカタログ.md` に全データを集約 | ✅ 完了 |

---

### 5-2. ファイル整理（移動・展開）

研究室フォルダ内に保存されていたファイルを `04_プログラム/data/` 配下へ移動・展開した。

| 元ファイル | 移動先 | 備考 |
|-----------|--------|------|
| 小地域ZIP×4 | `data/population_mesh/` | 年齢別人口・世帯・就業・250mメッシュ |
| 避難施設ZIP | `data/shelters/` | 国土数値情報P20 茨城県 |
| 250mメッシュZIP×2 | `data/population_mesh/` | T001178（5歳階級別）・T001208（常住地等） |
| 車両保有PDF（MD変換済） | `data/vehicle_stats/` | 市区町村別自動車保有台数_茨城.md |

---

### 5-3. 小地域人口データ確認（e-Stat 国勢調査2015）

| 統計ID | ファイル名 | エンコード | 常総市KEY_CODE | 確認した値 |
|--------|-----------|-----------|--------------|-----------|
| T000849 | tblT000849C08.txt | Shift-JIS | 08211 | 65歳以上人口 = **16,860人** |
| T000850 | tblT000850C08.txt | Shift-JIS | 08211 | 65歳以上世帯員あり = **10,692世帯**（T000851より） |
| T000851 | tblT000851C08.txt | Shift-JIS | 08211 | 一般世帯総数 = 約20,504世帯 |
| T000852 | tblT000852C08.txt | Shift-JIS | 08211 | 住宅・世帯補足情報 |

**注意事項：**
- KEY_CODEは `08211`（常総市）。`08224` は守谷市のため混同注意。
- 列インデックスはゼロ始まり。T000849の65歳以上は col[25]。

---

### 5-4. 避難施設データ確認（国土数値情報 P20）

| 項目 | 内容 |
|------|------|
| ファイル | `data/shelters/避難施設データ_茨城/P20-12_08.dbf` |
| 座標系 | JGD2000（EPSG:4612） |
| 茨城県全体レコード数 | 2,326件 |
| 常総市（P20_001=08211）件数 | **64件** |
| うち洪水対応（P20_007=1）件数 | **19件** |

**主要フィールド：**

| フィールド | 内容 |
|-----------|------|
| P20_001 | 市区町村コード |
| P20_002 | 施設名 |
| P20_003 | 住所 |
| P20_005 | 収容人数 |
| P20_007 | 洪水フラグ（1=洪水対応） |
| 緯度・経度 | 座標（JGD2000） |

**⚠ 旧データ注記：** `hinanmap.gsi.go.jp` はログイン必須のため取得不可。国土数値情報P20を代替として採用（座標・収容人数・災害種別フラグ付きで高品質）。

---

### 5-5. 250mメッシュ人口データ確認

| 統計ID | ファイル | 列数 | 常総市メッシュ2次コード |
|--------|---------|------|----------------------|
| T001178 | tblT001178Q08.txt | 63列 | 543907 / 543917 |
| T001208 | tblT001208Q08.txt | 53列 | 543907 / 543917 |

- KEY_CODEは10桁（2次メッシュ6桁 + 250mメッシュ4桁）
- 常総市域（緯度36.01〜36.09、経度139.96〜140.01）は 2次メッシュ `543907`・`543917` に該当
- 当初の `543975` は誤り。実ファイルのKEYを確認して修正済み。

---

### 5-6. 車両保有台数確認

| 項目 | 値 |
|------|-----|
| データ年度 | H29（2017年）※H27データ未入手のため代替 |
| 対象行政区 | 常総市（旧水海道市・石下町合併後） |
| 乗用車（自家用） | **25,698台** |
| 世帯あたり台数 | **≈ 1.25台/世帯**（÷ 20,504世帯） |

**⚠ 注意：** 「水海道市」名義のエントリ（33,236台）は旧コード名残であり別集計。常総市として使用する値は `25,698台`。

---

### 5-7. データカタログ作成

全データを一元参照できるマークダウンカタログを新規作成した。

| 項目 | 内容 |
|------|------|
| ファイルパス | `02_情報調査/データカタログ.md` |
| 記載データ数 | 10データソース |
| 記載内容 | ファイルパス・エンコーディング・主要カラム・常総市抽出条件・確認済み値 |

---

### 5-8. 調査タスク状態更新

| タスクID | 内容 | 状態 |
|---------|------|------|
| J-7 | 避難所データ取得 | ✅ 完了（P20 Shapefile採用） |
| J-8 | 車両保有台数取得 | ✅ 完了（H29 MD変換済） |
| J-9 | 65歳以上人口取得 | ✅ 完了（T000849より16,860人） |

J-4（250mメッシュCSV）、J-5（避難所CSV旧形式）はP20・T001178/T001208で代替完了。

---

### 5-9. 次回以降の実装方針（更新）

全データが揃ったため、実装フェーズへ移行する。

1. **Env-1〜3**：Python仮想環境構築（osmnx, geopandas, pyogrio, networkx, folium, pyproj, shapely, pandas, matplotlib）
2. **C-3**：osmnxで常総市道路ネットワーク取得 → `/codex:rescue` に委任
3. **I-1**：A31a GML + KML → 浸水ポリゴン × 道路リンク 空間照合 → `/codex:rescue` に委任
4. **I-2**：時刻別道路封鎖リスト（JSON/CSV）生成
5. **I-3**：封鎖考慮済み迂回ルート検索（networkx Dijkstra）

**シミュレーション入力データ（確定値）：**

| 入力 | 値 | ソース |
|------|-----|--------|
| 避難施設数（洪水対応） | 19件 | P20 DBF |
| 65歳以上人口 | 16,860人 | T000849 |
| 乗用車（自家用）保有台数 | 25,698台 | 車両保有MD |
| 世帯あたり車両台数 | 1.25台/世帯 | 上記より算出 |
| 浸水ポリゴン（閾値） | waterDepth≥2 | A31a GML |
| 道路閉鎖条件 | 浸水深0.5m以上 | 研究設計確定値 |

---

## Part 6：実装仕様書作成・Python環境構築完了（2026/04/28）

### 6-1. 活動概要

| 作業 | 内容 | 結果 |
|------|------|------|
| 実装仕様書作成 | コーディング前の設計仕様を文書化 | ✅ 完了 |
| Python環境構築（Env-1〜3） | venv作成・ライブラリインストール・import確認 | ✅ 完了 |
| 環境・ツール記録作成 | 使用環境・ライブラリ・ツールを記録ファイルに保存 | ✅ 完了 |

---

### 6-2. 実装仕様書（`03_研究設計文書/実装仕様書.md`）

コーディング開始前に実装レベルの仕様書を新規作成した。

| 章 | 内容 |
|----|------|
| システム概要 | 比較シナリオ・評価指標・シミュレーション時間設定 |
| ディレクトリ・ファイル構成 | `data/`・`output/`・`scripts/` の全ファイル一覧 |
| 定数・設定値（config.py） | 全スクリプト共通パラメータ（CRS・破堤時刻・バス設定等） |
| データ入力仕様 | KML・A31a GML・T001178・P20 DBF・OSMの各仕様 |
| モジュール仕様（Phase 1） | c3/e1/i1/i2/i3 の処理フロー・入出力・関数シグネチャ |
| Phase 2〜3 概要 | SUMO・TraCI・感度分析の概略 |
| データフロー | モジュール間の依存関係図 |
| CRS管理方針 | 全レイヤーを EPSG:6668 に統一する原則 |
| エラー処理・テスト方針 | 各スクリプトの動作確認基準 |
| 実装順序と依存関係 | c3とe1を並列実行可とする順序図 |

---

### 6-3. Python環境構築（Env-1〜3）

| ステップ | 内容 | 結果 |
|---------|------|------|
| Env-1 | `04_プログラム/venv/` に Python 3.13.12 の仮想環境を作成 | ✅ |
| Env-2 | 必要ライブラリ10件をインストール | ✅ |
| Env-3 | 全ライブラリの `import` 確認 | ✅ 全 OK |

**インストール済みライブラリ（主要）：**

| ライブラリ | バージョン | 用途 |
|-----------|-----------|------|
| osmnx | 2.1.0 | 道路ネットワーク取得 |
| geopandas | 1.1.3 | 空間データ処理 |
| shapely | 2.1.2 | ジオメトリ演算 |
| networkx | 3.6.1 | グラフ・ルート検索 |
| folium | 0.20.0 | 地図可視化（HTML） |
| pyogrio | 0.12.1 | KML/GML/Shapefile I/O |
| pyproj | 3.7.2 | CRS変換 |
| matplotlib | 3.10.9 | グラフ描画 |
| pandas | 3.0.2 | データ集計 |
| numpy | 2.4.4 | 数値配列演算 |

**技術的決定事項：**
当初仕様書では `fiona` を指定していたが、Python 3.13 + Windows 環境では GDAL バイナリの互換性問題が発生するため `pyogrio` に変更した。pyogrio は KML・GML・Shapefile・GeoPackage すべてに対応しており、機能的に fiona を完全に代替できることを確認済み。仕様書の記述も併せて更新した。

---

### 6-4. 作成・更新ファイル一覧

| ファイル | 種別 | 内容 |
|---------|------|------|
| `03_研究設計文書/実装仕様書.md` | 新規作成 | Phase 1詳細・Phase 2〜3概要の実装仕様 |
| `04_プログラム/venv/` | 新規作成 | Python 3.13.12 仮想環境 |
| `04_プログラム/requirements.txt` | 新規作成 | インストール済みライブラリ一覧（バージョン固定） |
| `04_プログラム/環境・ツール記録.md` | 新規作成 | OS・ライブラリ・ツール・データソースの記録 |
| `04_プログラム/scripts/` | 新規作成 | Pythonスクリプト格納ディレクトリ |
| `04_プログラム/output/` | 新規作成 | 生成ファイル出力ディレクトリ（6サブディレクトリ） |

---

### 6-5. 次回以降の実装方針（更新）

環境構築・仕様確定が完了したため、次は Phase 1 のスクリプト実装に着手する。

| 優先順位 | タスク | 担当 | 備考 |
|---------|--------|------|------|
| 1（並列） | C-3：`c3_get_road_network.py` | /codex:rescue | osmnxで常総市道路NW取得 |
| 1（並列） | E-1：`e1_load_flood_data.py` | /codex:rescue | A31a GML + KML 時系列読み込み |
| 2 | I-1：`i1_spatial_join.py` | /codex:rescue | 浸水ポリゴン × 道路リンク 空間照合 |
| 3 | I-2：`i2_generate_closure.py` | /codex:rescue | 時刻別道路封鎖リスト生成 |
| 4 | I-3：`i3_route_search.py` | /codex:rescue | 迂回ルート検索・逃げ遅れカウント |

---

## Part 7：実装手順書作成・STEP 0 完了・文書整合性修正（2026/04/28）

### 7-1. 活動概要

| 作業 | 内容 | 結果 |
|------|------|------|
| 実装手順書作成 | ミスを防ぐためPhase 1の詳細手順・役割分担・Codexプロンプトを文書化 | ✅ 完了 |
| STEP 0 実施 | `scripts/config.py` を Claude Code が直接作成・動作確認 | ✅ 完了 |
| 文書整合性修正 | 全マークダウンの `fiona`→`pyogrio` 統一、仕様書に出力パス定数を追記 | ✅ 完了 |
| CLAUDE.md 更新 | 進捗・タスク状態・次アクション・重要ファイルパスを最新化 | ✅ 完了 |

---

### 7-2. 実装手順書（`05_タスク管理/実装手順書_Phase1.md`）

Phase 1 の各 STEP を「何を」「誰が」「どのように」行うかを詳細化した手順書を新規作成した。

| STEP | 対象スクリプト | 担当 | 内容 |
|------|-------------|------|------|
| 0 | `config.py` | Claude Code | 定数ファイル作成（動作確認コマンド付き） |
| 1a | `c3_get_road_network.py` | Codex | 道路NW取得（Codex向けプロンプト全文記載） |
| 1b | `e1_load_flood_data.py` | Codex | 浸水データ読み込み（1a と並列実施可） |
| 2 | `i1_spatial_join.py` | Codex | 浸水ポリゴン×道路リンク空間照合 |
| 3 | `i2_generate_closure.py` | Codex | 時刻別封鎖リスト生成 |
| 4 | `i3_route_search.py` | Codex | 迂回ルート検索・逃げ遅れカウント |

各 STEP に「Codex への指示文（全文）」「ユーザー実行コマンド」「Claude Code による出力確認手順」「エラー対処表」を記載した。

---

### 7-3. STEP 0 完了（`scripts/config.py`）

`config.py` を Claude Code が直接作成し、動作確認を実施した。

**確認済み定数（主要）：**

| 定数 | 値 | 確認結果 |
|------|-----|---------|
| `JOSO_CODE` | `"08211"` | ✅ |
| `CRS_JGD2011` | `"EPSG:6668"` | ✅ |
| `FLOOD_DEPTH_THRESHOLD` | `2` | ✅ |
| `KML_TIMESTAMPS` | 8件 | ✅ |
| 出力パス定数（`OUT_*_DIR` 等） | 17件 | ✅ |

**仕様書との差異（改善点）：** 当初の仕様書セクション3では `DATA_DIR`・`OUTPUT_DIR` 等6定数のみ記載していたが、スクリプト間の参照を単純化するため出力サブディレクトリ定数（`OUT_NETWORK_DIR` 等6件）と個別ファイルパス定数（`GRAPHML_PATH` 等11件）を追加した。仕様書も同内容に更新済み。

---

### 7-4. 文書整合性修正

環境構築で `fiona` から `pyogrio` へ変更が生じた（セクション6-3参照）ため、全マークダウンファイルを対象に整合性を確認・修正した。

| 修正ファイル | 修正内容 |
|---------|---------|
| `05_タスク管理/実装タスク一覧.md` | Env-2 ライブラリ一覧・E-1-2 の `fiona` → `pyogrio` |
| `01_経過報告/経過報告20260513.md`（本ファイル） | Part 4-9・Part 5-9 の構築ライブラリ一覧の `fiona` → `pyogrio` |
| `03_研究設計文書/実装仕様書.md` | Section 3 に出力パス定数17件を追記 |
| `4年次本研究/CLAUDE.md` | データ取得状況・タスク状態・次アクション・重要ファイルパスを全更新 |

**変更しなかった箇所：**
- `環境・ツール記録.md`・`経過報告20260513.md` Part 6 の「`fiona` は互換性問題により不採用」という記述は経緯の説明として正しいため変更不要。
- `実装手順書_Phase1.md` の「`pyogrio` を使う（`fiona` は使わない）」は正しい注意書きのため変更不要。

---

### 7-5. 作成・更新ファイル一覧

| ファイル | 種別 | 内容 |
|---------|------|------|
| `05_タスク管理/実装手順書_Phase1.md` | 新規作成 | Phase 1 詳細手順・Codex プロンプト全文・エラー対処表 |
| `04_プログラム/scripts/config.py` | 新規作成 | 全スクリプト共通定数ファイル（STEP 0 完了） |
| `03_研究設計文書/実装仕様書.md` | 更新 | Section 3 に出力パス定数17件を追記 |
| `05_タスク管理/実装タスク一覧.md` | 更新 | `fiona` → `pyogrio`・E-1-2 ドライバ記述修正 |
| `4年次本研究/CLAUDE.md` | 更新 | 進捗・タスク状態・ファイルパス最新化 |

---

### 7-6. 次回以降の実装方針（更新）

文書整合性の修正および STEP 0 が完了したため、次は Phase 1 スクリプトの実装に着手する。

| 優先順位 | タスク | 担当 | 参照手順 |
|---------|--------|------|---------|
| 1（並列） | STEP 1a：`c3_get_road_network.py` | /codex:rescue | 実装手順書 STEP 1a |
| 1（並列） | STEP 1b：`e1_load_flood_data.py` | /codex:rescue | 実装手順書 STEP 1b |
| 2 | STEP 2：`i1_spatial_join.py` | /codex:rescue | STEP 1a・1b 完了後 |
| 3 | STEP 3：`i2_generate_closure.py` | /codex:rescue | STEP 2 完了後 |
| 4 | STEP 4：`i3_route_search.py` | /codex:rescue | STEP 3 完了後 |

---

## Part 8：Phase 1 STEP 1a〜3 実装・デバッグ・完了（2026/04/29〜30）

### 8-1. 実装したスクリプト概要

| スクリプト | STEP | 役割 | 状態 |
|-----------|------|------|------|
| `c3_get_road_network.py` | 1a | 常総市道路ネットワーク取得（OSM/osmnx）→ GraphML + GPKG 出力 | ✅ 完了 |
| `e1_load_flood_data.py` | 1b | A31a GML 読み込み・KML タイムライン読み込み・時刻別浸水ポリゴン構築 | ✅ 完了 |
| `i1_spatial_join.py` | 2 | 浸水ポリゴン×道路エッジ空間照合 → closure_dict.pkl 出力 | ✅ 完了 |
| `i2_generate_closure.py` | 3 | closure_dict を JSON / CSV に展開 | ✅ 完了 |

---

### 8-2. PR マージと競合解消

Phase 1 スクリプトは Codex ブランチ（`codex/implement-coding-based-on-documentation-ch8tdp`）で実装され、PR #2・#3 としてプッシュされた。

| PR | 対象ファイル | 競合状況 | 対応 |
|----|------------|---------|------|
| PR #2 | c3・e1 | 競合なし（auto merge 可能） | `gh pr merge 2 --merge` |
| PR #3 | i1・i2 | add/add 競合（5箇所） | 競合マーカー除去・master 版を手動作成 → マージ |

---

### 8-3. テスト結果と判明した問題

構文チェック（`py_compile`）は全4ファイル成功。実行テストでは環境依存ライブラリ未導入のため停止（コード自体の問題ではない）。結果を `04_プログラム/テスト結果_phase1_01.md` に記録した。

**発見した空間照合バグ（0 edges closed 問題）：**

実際に c3・e1 の出力を用いて i1 を実行したところ、全タイムスタンプで「0 edges closed」と出力された。原因調査の結果、2段階の問題が判明した。

| 問題 | 原因 | 影響 |
|------|------|------|
| 問題1 | KML LineString に 10m バッファを付けても A31a ポリゴンと交差しない | `build_flood_polygons` が空の GeoDataFrame を返す |
| 問題2 | A31a グリッドセルが 5〜20m 四方の極小ポリゴン。OSM 道路中心線がセル間の隙間を通過 | sjoin で道路エッジが全て非交差と判定される |

---

### 8-4. 採用した修正アプローチ（案2+3複合）

**e1_load_flood_data.py の修正（問題1への対策）：**

KML の LineString バッファ（10m）による polygon 化を廃止し、各時点の全 LineString の **`union_all().convex_hull`** を浸水エンベロープとして採用（案2）。凸包内に A31a が 0 件の場合は A31a 全体をフォールバック採用（案3）。

```python
# KML 全ジオメトリの凸包を浸水エンベロープとして使用
kml_hull = kml_gdf.geometry.union_all().convex_hull
kml_envelope = gpd.GeoDataFrame(geometry=[kml_hull], crs=a31a.crs)
joined = gpd.sjoin(a31a, kml_envelope, how="inner", predicate="intersects")
result = a31a.loc[joined.index.unique()].copy()
if len(result) == 0:
    result = a31a.copy()  # フォールバック（案3）
```

**i1_spatial_join.py の修正（問題2への対策）：**

投影 CRS（EPSG:6690 / JGD2011 UTM54N）で浸水ポリゴン全体を **30m バッファ**してから sjoin を実施。30m = グリッド離散化誤差（最大 10m）＋車道幅余裕。

```python
_CRS_METRIC = "EPSG:6690"
_FLOOD_BUFFER_M = 30
flood_buffered = (
    gpd.GeoDataFrame(
        geometry=[flood.to_crs(_CRS_METRIC).geometry.union_all().buffer(_FLOOD_BUFFER_M)],
        crs=_CRS_METRIC,
    ).to_crs(flood.crs)
)
hit = gpd.sjoin(base[...], flood_buffered[...], predicate="intersects", how="inner")
```

---

### 8-5. 修正後の動作確認結果

| タイムスタンプ | A31a ポリゴン数 | 閉鎖エッジ数 |
|--------------|--------------|------------|
| 2015-09-10T18:00 | 72 | 15 |
| 2015-09-11T09:00 | 8,792 | 33 |
| 2015-09-12T09:00 | 8,792 | 33 |
| 2015-09-13T09:00 | 8,792 | 33 |
| 2015-09-14T09:00 | 8,792 | 33 |
| 2015-09-15T09:00 | 8,792 | 33 |
| 2015-09-15T18:00 | 8,792 | 33 |
| 2015-09-16T10:20 | 8,792 | 33 |

各タイムスタンプで 15〜33 edges closed を確認。「全 0 edges」から脱却し、時刻別の道路封鎖リストが正常に生成されることを確認した。

---

### 8-6. 残っている課題と次のステップ

| 項目 | 状態 | 次アクション |
|------|------|------------|
| STEP 4：`i3_route_search.py` | ❌ 未着手 | 迂回ルート検索スクリプトの実装 |
| Phase 2（SUMO/TraCI） | ❌ 未着手 | Phase 1 完了後に着手 |
| Phase 3（バス・比較） | ❌ 未着手 | Phase 2 完了後に着手 |

**次回報告までの予定：**
1. STEP 4（`i3_route_search.py`）実装：閉鎖リストを参照しながら時刻別最短経路を検索
2. Phase 1 全体動作確認（c3 → e1 → i1 → i2 → i3 の連続実行）
3. Phase 1 完了報告の準備

---

## Part 9：PR #4 競合解消・プロキシ要否確認・動作確認（2026/04/30）

### 9-1. 活動概要

| # | 活動 | 結果 |
|---|------|------|
| 1 | Codex が新規作成した PR #4 の競合解消 | ✅ master 修正済み版で統一・マージ完了 |
| 2 | プログラムのプロキシ設定要否の確認 | ✅ 条件付き不要（c3 初回のみ・環境依存） |
| 3 | 全4スクリプトの連続実行動作確認 | ✅ 全スクリプト正常動作・出力ファイル生成確認 |

---

### 9-2. PR #4 競合解消

Codex が新たに `codex/implement-coding-based-on-documentation-9tamui` ブランチで PR #4 を作成したが、master にすでに修正済みの版（案2+3複合・30m バッファ）が存在するため add/add 競合が4ファイル全てで発生した。

| ファイル | PR #4 の問題点 | 採用した版（master） |
|---------|--------------|-------------------|
| `e1_load_flood_data.py` | pyogrio 直接読み込み（A31a GML 非対応）＋10m バッファ（旧方式） | XML ET パーサー＋凸包エンベロープ（案2+3複合） |
| `i1_spatial_join.py` | 30m バッファなし → 0 edges closed になる | EPSG:6690 で 30m バッファ → 15〜33 edges closed |
| `c3_get_road_network.py` | センター座標ハードコード（`[36.05, 140.0]`） | データから動的計算 |
| `i2_generate_closure.py` | ほぼ同一 | master 版（詳細 print あり） |

解消手順：ローカルに PR ブランチをチェックアウト → `git merge master` → `git checkout master -- <files>` で master 版に統一 → コミット・プッシュ → `gh pr merge 4 --merge` でマージ完了。

---

### 9-3. プロキシ設定の要否確認

スクリプト別のネットワーク通信依存を調査した結果、以下の通り判明した。

| スクリプト | ネットワーク通信 | プロキシ必要性 |
|-----------|---------------|-------------|
| `c3_get_road_network.py` | Overpass API（OSM）への HTTP リクエスト（osmnx 内部・初回のみ） | **条件付き必要**（キャッシュ生成後は不要） |
| `e1_load_flood_data.py` | なし（ローカルの GML・KML を読む） | 不要 |
| `i1_spatial_join.py` | なし（ローカルの pkl・gpkg を読む） | 不要 |
| `i2_generate_closure.py` | なし（pickle・JSON/CSV の読み書きのみ） | 不要 |

**結論：プロキシ設定は必須ではない。** 必要になるのは「c3 の初回実行時 × プロキシ経由でなければ外部に出られないネットワーク環境」の場合のみ。osmnx は `requests` ライブラリを使い、OS の環境変数 `HTTP_PROXY` / `HTTPS_PROXY` を自動参照するためコード変更は不要。

---

### 9-4. 全スクリプト動作確認結果

仮想環境（`04_プログラム/venv/`）で全4スクリプトを順番に実行し、正常動作を確認した。

**環境：**
- Python 仮想環境：`04_プログラム/venv/`
- 全依存ライブラリ：requirements.txt 準拠で導入済み

**実行結果：**

| スクリプト | 実行結果 | 主要出力 |
|-----------|---------|---------|
| `c3_get_road_network.py` | ✅ 正常 | GraphML キャッシュ使用・12,860 edges → GPKG 出力 |
| `e1_load_flood_data.py` | ✅ 正常（警告あり） | A31a 8,792 ポリゴン・8時点浸水辞書生成 |
| `i1_spatial_join.py` | ✅ 正常 | 全8タイムスタンプで 15〜33 edges closed |
| `i2_generate_closure.py` | ✅ 正常 | JSON・CSV 出力完了 |

**タイムスタンプ別封鎖エッジ数：**

| タイムスタンプ | 封鎖エッジ数 |
|--------------|------------|
| 2015-09-10T18:00:00 | 33 |
| 2015-09-11T06:00:00 | 31 |
| 2015-09-11T18:00:00 | 31 |
| 2015-09-12T06:00:00 | 33 |
| 2015-09-12T18:00:00 | 20 |
| 2015-09-13T06:00:00 | 15 |
| 2015-09-13T18:00:00 | 33 |
| 2015-09-16T10:20:00 | 33 |

**e1 の警告について：** KML LineString を凸包入力用に微小バッファ（0.0001 度）処理する際、地理座標系（EPSG:6668）のまま `buffer()` を呼んでいるため geopandas が警告を出す。このバッファは convex hull の入力として使うだけで最終的な浸水ポリゴンには影響せず、i1 の封鎖エッジ数も前回結果と一致しているため、研究結果への影響はない。

---

### 9-5. 生成された出力ファイル一覧

| ファイル | サイズ | 更新日時 |
|---------|--------|---------|
| `output/network/joso_road_network.graphml` | 5.8 MB | 2026-04-30 |
| `output/network/joso_edges.gpkg` | 3.3 MB | 2026-04-30 |
| `output/network/joso_network_map.html` | 6.7 MB | 2026-04-30 |
| `output/flood/flood_polygons.pkl` | 7.2 MB | 2026-04-30 |
| `output/flood/flood_timeline_map.html` | 16.4 MB | 2026-04-30 |
| `output/closure/closure_dict.pkl` | 0.8 KB | 2026-04-30 |
| `output/closure/road_closure_timeline.json` | 3.2 KB | 2026-04-30 |
| `output/closure/road_closure_timeline.csv` | 5.8 KB | 2026-04-30 |

---

### 9-6. 現在の残課題と次のステップ

| 項目 | 状態 | 次アクション |
|------|------|------------|
| STEP 4：`i3_route_search.py` | ❌ 未着手 | 時刻別封鎖リストを参照した迂回ルート検索の実装 |
| Phase 2（SUMO/TraCI） | ❌ 未着手 | Phase 1 完了後に着手 |
| Phase 3（バス・比較） | ❌ 未着手 | Phase 2 完了後に着手 |

**次回報告までの予定：**
1. STEP 4（`i3_route_search.py`）実装：`closure_dict.pkl` を読み込み、時刻別に封鎖エッジを除外した networkx グラフで最短経路を検索
2. Phase 1 全体通し確認（c3 → e1 → i1 → i2 → i3 の連続実行）
3. Phase 1 完了報告の準備

---

## Part 10：全スクリプト実用修正・i3 完成（2026/04/30）

### 10-1. 活動概要

コミット `f52fced` にて全5スクリプト（c3/e1/i1/i2/i3）を実用環境向けに修正した。

| # | 修正内容 | 対象スクリプト |
|---|---------|-------------|
| 1 | Windows/OneDrive でのファイルロック対策（PermissionError ハンドリング） | c3・e1・i1・i2・i3 |
| 2 | KML バッファを地理座標系から投影 CRS（EPSG:6690）で実施するよう修正 | e1 |
| 3 | T001178 CSV の列名解釈を修正（総数列・65歳以上列を明示指定） | i3 |
| 4 | 250m メッシュ座標式をセル中心点に修正 | i3 |
| 5 | 避難所フラグ（P20_007）の型差異対策（`.astype(str)` 追加） | i3 |
| 6 | scikit-learn 不要の最近傍ノード探索に差し替え | i3 |

---

### 10-2. 修正詳細

#### Windows/OneDrive ファイルロック対策（c3・e1・i1・i2・i3）

OneDrive 同期中は既存ファイルへの書き込みが `PermissionError` で失敗する。
各スクリプトのファイル書き出し関数（`save_edges`・`save_flood_polygons`・`save_closure_dict` 等）に以下のパターンを追加した。

```python
try:
    # 通常書き込み
except PermissionError:
    if Path(path).exists():
        print(f"[WARN] 既存ファイルを上書きできないため保持: {path}")
        return False   # 既存成果物を使って後続処理を続行
    raise
```

戻り値を `bool` に変更し、`main()` 側で保持か新規保存かをログ出力する。

---

#### KML バッファを投影 CRS で実施（e1）

変更前は地理座標系（EPSG:6668）のまま `buffer(0.0001)` (度) していたため geopandas の警告が出ていた。
変更後は投影 CRS（EPSG:6690 / JGD2011 UTM54N）に変換してから `buffer(10)` (m) を適用し、地理座標系に戻す。

```python
# 変更前
gdf["geometry"] = gdf.geometry.buffer(_KML_BUFFER_DEG)   # 度単位 → 警告

# 変更後
_KML_BUFFER_M = 10
_CRS_METRIC = "EPSG:6690"
gdf = gdf.to_crs(_CRS_METRIC).copy()
gdf["geometry"] = gdf.geometry.buffer(_KML_BUFFER_M)     # メートル単位 → 警告なし
gdf = gdf.to_crs(config.CRS_JGD2011)
```

---

#### T001178 CSV 列名の明示指定（i3）

T001178（国勢調査2015年 250m メッシュ）の CSV ファイルでは、総人口が `T001178001` 列、65 歳以上は年齢・性別ごとの `T001178043`〜`T001178055` 列に格納されている。

変更前は汎用的な列探索ロジック（`_find_col`）に頼っていたため誤列を参照するリスクがあった。変更後はファイル固有の列名を直接指定し、合算処理も明示した。

```python
total_col = "T001178001"  # 総人口
elderly_cols = ["T001178043", "T001178046", "T001178049", "T001178052", "T001178055"]
# 65歳以上 5歳刻み（男女計）を合算
work["elderly_pop"] = work[elderly_cols].apply(pd.to_numeric, ...).fillna(0).sum(axis=1)
```

---

#### 250m メッシュ座標式をセル中心点に修正（i3）

変更前の座標式はセル南西隅を返していた。空間結合の精度向上のためセル中心点（`+ 3.75秒` / `+ 5.625秒`）に修正した。

また、空間結合をポイントではなくセルのバウンディングボックス（`box()`）で行うよう変更し、浸水ポリゴンと部分重複するメッシュも捕捉できるようにした。

---

#### 避難所フラグ型差異の対策（i3）

`P20_007`（洪水対応フラグ）を Shapefile から読み込むと整数型になる場合がある。文字列 `"1"` との比較が失敗しないよう `.astype(str)` を追加した。

```python
gdf = gdf[
    (gdf["P20_001"].astype(str) == config.JOSO_CODE)
    & (gdf["P20_007"].astype(str) == "1")
].copy()
```

---

#### scikit-learn 不要の最近傍ノード探索（i3）

`ox.distance.nearest_nodes()` は内部で scikit-learn の KDTree を使用するため、scikit-learn 未導入環境ではエラーになる。requirements.txt に scikit-learn を追加せず、純 Python の `min()` で代替した。

```python
def find_nearest_node(G, lon, lat):
    return min(
        G.nodes,
        key=lambda n: (G.nodes[n]["x"] - lon)**2 + (G.nodes[n]["y"] - lat)**2,
    )
```

---

### 10-3. 残っている課題

コミットコメントに記載の通り、現状では全タイムスタンプで到達不可メッシュ数が一定となっている。封鎖なし（`closure=[]`）では到達不可 0 になるため封鎖の影響自体は出ているが、時系列変化が見られない。

原因と対応方針の選択肢：

| 選択肢 | 内容 | 影響 |
|-------|------|------|
| A（累積封鎖） | 浸水ポリゴンを時点ごとに累積扱いにする | 時間が進むほど封鎖が増え到達不可が増加する |
| B（浸水範囲拡張） | 出発地の対象メッシュを浸水エリア外まで広げる | より多くの出発地を扱い変化が出やすくなる |

どちらを採用するかは研究のモデル設定（実際の洪水進行をどう扱うか）に関わるため、**指導教員への確認が必要**。

**→ 採用方針：選択肢A（累積封鎖）を採用。**
実際の鬼怒川氾濫（2015/09/10〜09/16）は約1週間継続しており、一度浸水した道路は期間中ほぼ回復しなかった。この実態に合致するため累積封鎖を採用する。`i3_route_search.py` の修正後、指導教員に事後報告する予定。

---

#### 【補足】なぜ到達不可数が変化しないのか — 具体的な仕組み

**本来期待される動作：**

洪水は時間とともに広がるため、時刻が進むほど封鎖される道路が増え、避難できなくなるメッシュも増えていくはずである。

```
t=0（破堤直後）: 封鎖15本 → 到達不可 X 件
t=1            : 封鎖31本 → 到達不可 X+α 件（増加するはず）
...
t=7（最終）    : 封鎖33本 → 到達不可 X+β 件（さらに増加するはず）
```

**現在の動作（問題）：**

全8タイムスタンプで到達不可件数が同じ値のまま変化しない。

**原因 — 出発地（origin）が固定されている：**

現在のコードでは、出発地メッシュを **t=0 の浸水ポリゴン** と重なるセルのみを使って一度だけ決定している。

```python
# i3_route_search.py の main() より
first_flood = flood_dict[config.KML_TIMESTAMPS[0]]   # t=0 の浸水ポリゴンのみ使用
origins = load_mesh_origins(config.MESH_FILE, first_flood)  # ← 以後 変わらない
```

この「出発地の固定」と「封鎖エッジ数が少ない」という2つの要因が重なることで、以下の状況が生じる。

| 要因 | 詳細 |
|------|------|
| 出発地が t=0 基準 | t=0 の浸水範囲は小さい（封鎖15本相当）ため、選ばれるメッシュ数が少ない |
| 封鎖エッジ数の変化幅が小さい | t=0〜t=7 でも封鎖エッジは15〜33本（道路網全体の12,860本に対し0.3%未満） |
| 迂回路が常に存在する | 封鎖エッジ数が少ないため、t=0 時点の出発地メッシュからは全タイムスタンプで避難所への迂回ルートが見つかる |

その結果、封鎖数が変化しても到達不可件数は変わらないという状態になっている。

**各選択肢が何を変えるか：**

**選択肢A（累積封鎖）** — 「洪水は引かない」という現実に合わせるアプローチ

```
現在: t=3 では「t=3 の浸水範囲だけ」を封鎖対象とする
案A:  t=3 では「t=0 + t=1 + t=2 + t=3 の全浸水範囲」を累積して封鎖する
```

過去に浸水した道路を累積的に封鎖し続けることで、時刻が進むほど通行不能区間が増え、到達不可件数の増加が現れる。ただし「洪水は一度浸水したら絶対に引かない」という前提になるため、その妥当性を研究内で説明する必要がある。

**選択肢B（浸水範囲拡張）** — 出発地の定義を広げるアプローチ

```
現在: 出発地 = t=0 の浸水エリア内のメッシュのみ（浸水被害を受けた住民）
案B:  出発地 = 常総市全域（または浸水リスクエリア全体）のメッシュ
```

浸水エリア外にいる住民も避難者として含めることで、時刻ごとに異なる封鎖パターンの影響が出やすくなる。ただし「浸水していない住民も自発的に避難する」という研究上の前提が追加されるため、その根拠を説明する必要がある。

---

### 10-4. 現在の実装状態まとめ

| スクリプト | 状態 | 主な変更 |
|-----------|------|---------|
| `c3_get_road_network.py` | ✅ 完了 | PermissionError ハンドリング追加 |
| `e1_load_flood_data.py` | ✅ 完了 | KML バッファを投影 CRS で実施・PermissionError 対策 |
| `i1_spatial_join.py` | ✅ 完了 | edge_id を `u_v_key` 形式に・PermissionError 対策 |
| `i2_generate_closure.py` | ✅ 完了 | PermissionError 対策 |
| `i3_route_search.py` | ✅ 完了（要モデル判断） | 列名・座標式・型差異・最近傍ノード修正 |

**Phase 1（必達スコープ）の全スクリプト実装が完了した。** 時系列変化の表現方法（累積封鎖 vs 浸水範囲拡張）について指導教員に確認し、モデルを確定させることが次の課題。

---

## Part 11：浸水ナビデータ反映・Phase 1成果物整理（2026/05/01時点）

### 11-1. 活動概要

Phase 1 の閉鎖時系列構造問題に対し、追加取得した浸水ナビ API ハイドログラフ（BP030）をプログラムへ反映した。
また、Google Chrome で確認しやすいように、Phase 1 の HTML 成果物を1つのトップページから遷移できる構成に整理した。

| # | 活動 | 結果 |
|---|------|------|
| 1 | **浸水ナビデータの追加取得・確認** | ✅ BP030・11メッシュ×8時点のハイドログラフ JSON を取得済み |
| 2 | **閉鎖タイムライン生成ロジックの修正** | ✅ `i1_spatial_join.py` で浸水ナビ由来の時刻別メッシュ深度を利用可能にした |
| 3 | **閉鎖診断CSVの追加** | ✅ `instant_edges` / `new_edges` / `lost_edges` / `cumulative_edges` を出力 |
| 4 | **i2・i3の再実行** | ✅ 新しい閉鎖タイムラインで JSON/CSV・到達不可CSV・ルートHTMLを再生成 |
| 5 | **HTML確認トップページ作成** | ✅ `output/index.html` から地図・ルートHTMLへ遷移可能にした |
| 6 | **ルートHTMLへの浸水範囲重ね合わせ** | ✅ t0〜t4 のルート地図で浸水範囲を青色半透明で確認可能にした |

---

### 11-2. 追加データと採用方針

| データ | パス | 役割 |
|--------|------|------|
| 浸水ナビハイドログラフ | `04_プログラム/data/suiboumap/hydrograph_origins_BP030.json` | 各タイムスタンプの250mメッシュ別浸水深を取得 |
| 浸水ナビ調査結果 | `02_情報調査/浸水ナビAPI_ハイドログラフ取得結果.md` | データ取得結果・Phase 1への活用方針を記録 |

`config.py` では閉鎖生成元を以下のように設定した。

```python
CLOSURE_SOURCE = "suiboumap_hydrograph"
FLOOD_DEPTH_THRESHOLD_M = 0.5
USE_CUMULATIVE_CLOSURE = True
```

これにより、従来の KML+A31a 凸包ベースの閉鎖生成ではなく、浸水ナビの時刻別メッシュ深度（0.5m以上）を用いて道路閉鎖候補を生成する。
ただし、研究上は2015年鬼怒川氾濫の長期浸水実態に合わせ、ルート検索時には累積封鎖（過去に閉鎖された道路は通行不可のまま）を維持している。

---

### 11-3. 閉鎖タイムライン再構築結果

浸水ナビデータを用いて `i1_spatial_join.py` を再実行した結果、時刻別の即時閉鎖エッジ数は以下の通りになった。

| タイムスタンプ | 即時閉鎖エッジ数 | 新規閉鎖 | 前時点から消失 | 累積閉鎖エッジ数 |
|--------------|----------------|---------|---------------|----------------|
| 2015-09-10T18:00:00 | 20 | 20 | 0 | 20 |
| 2015-09-11T06:00:00 | 20 | 0 | 0 | 20 |
| 2015-09-11T18:00:00 | 14 | 0 | 6 | 20 |
| 2015-09-12T06:00:00 | 12 | 0 | 2 | 20 |
| 2015-09-12T18:00:00 | 10 | 0 | 2 | 20 |
| 2015-09-13T06:00:00 | 0 | 0 | 10 | 20 |
| 2015-09-13T18:00:00 | 0 | 0 | 0 | 20 |
| 2015-09-16T10:20:00 | 0 | 0 | 0 | 20 |

**出力ファイル：**

| ファイル | 内容 |
|---------|------|
| `output/closure/closure_dict.pkl` | 浸水ナビ由来の閉鎖辞書 |
| `output/closure/road_closure_timeline.json` | 時刻別閉鎖エッジリスト |
| `output/closure/road_closure_timeline.csv` | CSV形式の閉鎖エッジ一覧 |
| `output/closure/closure_diagnostics.csv` | 即時・新規・消失・累積閉鎖数の診断結果 |

---

### 11-4. ルート検索結果

新しい閉鎖タイムラインを用いて `i3_route_search.py` を再実行した。

| 項目 | 結果 |
|------|------|
| 出発地メッシュ数 | 11 |
| 出発地総人口 | 723人 |
| 避難所数 | 19施設 |
| ルートHTML | t0〜t7 の8ファイルを再生成 |
| 到達不可 | 全時点 6メッシュ・人口429・高齢者68 |

`USE_CUMULATIVE_CLOSURE = True` のため、t0時点の20本の閉鎖が以後も累積的に維持される。
浸水ナビデータ自体は t0〜t1 がピークで、その後は退水方向に変化するため、累積封鎖を維持すると到達不可数は時系列で増加せず、全時点で一定となる。

---

### 11-5. HTML成果物の整理

Google Chrome で Phase 1 の成果物を確認しやすくするため、`output/index.html` を新規作成し、各 HTML ファイルへ遷移できるようにした。

| ファイル | 内容 |
|---------|------|
| `04_プログラム/output/index.html` | Phase 1 HTML成果物のトップページ |
| `04_プログラム/output/assets/phase1-pages.js` | リンク先データ |
| `04_プログラム/output/assets/phase1-components.js` | リンクカード生成処理 |
| `04_プログラム/output/assets/phase1.css` | 白基調の最小デザイン |

トップページから以下の成果物を開ける。

| 区分 | ファイル | 内容 |
|------|----------|------|
| 道路ネットワーク | `output/network/joso_network_map.html` | 常総市道路NW |
| 浸水時系列 | `output/flood/flood_timeline_map.html` | 浸水範囲の時系列表示 |
| 避難ルート | `output/routes/evacuation_routes_t0.html`〜`t7.html` | 時刻別避難ルート |

また、避難ルート HTML にはその時点の浸水範囲レイヤーを追加した。
t0〜t4 では浸水範囲を青色半透明ポリゴンとして表示し、ルートと浸水範囲の位置関係を同一地図上で確認できる。
t5〜t7 は浸水ナビデータ上で0.5m以上の浸水メッシュが0件のため、浸水範囲レイヤーは表示されない。

---

### 11-6. 現時点で判明している課題

| 課題 | 内容 | 対応方針 |
|------|------|----------|
| 出発地メッシュが少ない | 現在のルート検索は浸水範囲全体ではなく、浸水エリアと交差した人口メッシュ11地点のみを対象としている | Phase 1の簡略モデルとして説明しつつ、必要に応じて対象メッシュ拡張を検討 |
| 浸水時系列マップとルート地点数の見た目の差 | 浸水マップは多数の浸水ポリゴンを表示する一方、ルートは人口メッシュ代表点のみを表示するため少なく見える | ルート検索対象の定義を「人口メッシュ重心」と明記する |
| 到達不可数が全時点で一定 | 浸水ナビデータはt0〜t1がピークで以後退水するため、累積封鎖では閉鎖数がt0以降増えない | 累積封鎖の妥当性を研究仮定として説明する。時点別退水を見たい場合は `USE_CUMULATIVE_CLOSURE=False` で比較する |
| 背景地図の `Access blocked` 表示 | OpenStreetMap タイルが外部サーバー側で一部ブロックされる場合がある | 研究データ自体の異常ではない。必要ならタイル変更またはローカルサーバー経由表示を検討 |

---

### 11-7. 現在のPhase 1到達点

| 項目 | 状態 |
|------|------|
| 道路ネットワーク取得 | ✅ 完了 |
| 浸水時系列データ整備 | ✅ 完了 |
| 道路閉鎖リスト生成 | ✅ 完了（浸水ナビ由来に更新） |
| 迂回ルート検索 | ✅ 完了 |
| 到達不可メッシュ・人口の出力 | ✅ 完了 |
| ルートHTML・浸水範囲重ね合わせ | ✅ 完了 |
| Phase 1成果物トップページ | ✅ 完了 |
| 出発地対象メッシュの拡張判断 | 🔄 検討中 |

**現時点では、Phase 1の最低条件である「時間ごとの道路封鎖と迂回ルート検索」は実装・可視化まで完了している。**
一方で、卒論上の説得力を高めるためには、ルート検索対象を11メッシュに限定する理由、または対象メッシュを拡張する方針を明確化する必要がある。

---

## Part 12：Phase 1 シナリオ型拡張方針の整理（2026/05/01時点）

### 12-1. 背景

実際のデータを使用している現状の Phase 1 では、浸水マップ上では広い浸水範囲を確認できる一方、ルート検索対象は浸水エリアと交差した人口メッシュ重心に限定されている。

また、浸水時系列データでは浸水箇所の変動が小さく、累積封鎖を採用すると到達不可数も時刻ごとに大きく変化しない。そのため、研究成果として「浸水の進行に応じて避難ルートが変化する」ことを示しにくい。

この課題に対して、実データを根拠にしながら、最終的な浸水範囲を指定し、t0〜t7 にかけて浸水が少しずつ広がるシナリオを作成する方針を整理した。

### 12-2. 修正前と修正後

| 項目 | 修正前 | 修正後 |
|------|--------|--------|
| Phase 1 の説明 | 実データの時刻別浸水範囲に基づく道路閉鎖・迂回ルート検索 | 実データを根拠にした段階的浸水拡大シナリオによる道路閉鎖・避難ルート検索 |
| 浸水の推移 | 取得済み時系列データをそのまま利用 | 最終浸水範囲を t0〜t7 に少しずつ拡大 |
| ルート出発地点 | 浸水エリアと交差した人口メッシュ重心 | 地図上で指定した任意地点 |
| 避難先 | 常総市の洪水対応避難所19件 | 同左。到達可能な最寄り避難所を検索 |
| 成果物 | t0〜t7 の静的 HTML | 時刻選択・地点指定・ルート確認ができるシナリオ版 HTML を追加 |
| 卒論上の位置づけ | 実浸水時系列の再現に近い確認 | 実データを根拠にした避難ルート探索機能の検証シナリオ |

### 12-3. 今後決めること

| 決定事項 | 候補 | 現時点の推奨 |
|----------|------|--------------|
| 最大浸水範囲 | A31a、既存時系列の最大範囲、手動指定範囲 | A31a または既存時系列の最大範囲 |
| 拡大基準点 | 破堤地点、浸水範囲の重心、複数開始点 | 常総市三坂町の破堤地点 |
| 拡大方法 | 距離順、面積割合、手動ステップ | 破堤地点からの距離順に t0〜t7 へ分割 |
| 道路閉鎖条件 | 既存条件維持、条件変更 | 浸水深0.5m以上で閉鎖を維持 |
| ルート到達先 | 最寄り避難所、到達可能な最寄り避難所、全避難所比較 | 到達可能な最寄り避難所 |

### 12-4. 追加作成した文書

| ファイル | 内容 |
|----------|------|
| `03_研究設計文書/Phase1_シナリオ型拡張方針.md` | シナリオ型拡張の目的、修正前後、研究上の判断、実装前の決定事項を整理 |

この方針は、2015年鬼怒川氾濫を完全に再現するものではなく、2015年常総市の実データを根拠にした避難ルート探索機能の検証シナリオとして扱う。

### 12-5. 今回の文書更新内容

Phase 1 の今後の方向性を確定させる前段階として、シナリオ型浸水拡大・任意地点ルート検索に関する修正前後の整理を複数の設計文書へ反映した。

| ファイル | 更新内容 |
|----------|----------|
| `03_研究設計文書/Phase1_シナリオ型拡張方針.md` | 新規作成。現状課題、修正前後、研究上の判断、追加想定機能、実装前に決める事項を整理 |
| `03_研究設計文書/実装仕様書.md` | Phase 1 の追加検討として、シナリオ型浸水拡大と任意地点ルート検索の仕様を追記 |
| `05_タスク管理/実装タスク一覧.md` | P1-S1〜P1-S5 として、最終浸水範囲決定、段階的浸水拡大、シナリオ閉鎖道路生成、任意地点ルート検索、シナリオ版 HTML 作成を追加 |
| `03_研究設計文書/データフロー図.md` | 最終浸水範囲・破堤地点からシナリオ浸水範囲を生成し、道路閉鎖・任意地点ルート検索へつなげるデータフローを追加 |
| `03_研究設計文書/シーケンス図.md` | 利用者が時刻を選択し、地図上の地点をクリックして避難ルートを確認する画面操作の流れを追加 |
| `AGENTS.md` | 次セッションで最初に行うことを、シナリオ型拡張の判断確定・実データ版との位置づけ整理・P1-S1〜P1-S5 の実装タスク化に更新 |

これにより、コード修正前に「実データ版 Phase 1」と「シナリオ型拡張版 Phase 1」の違いを文書上で確認できる状態になった。

### 12-6. 文書整合性確認による追加修正

コード以外の文書を確認した結果、`AGENTS.md` の冒頭説明およびエージェント分担表に古い担当表記が残っていたため、実装担当、Pythonスクリプト新規実装、文書作成・調査・タスク管理の担当を `Codex` に統一した。

| ファイル | 修正前 | 修正後 |
|----------|--------|--------|
| `AGENTS.md` | 実装担当：Claude Code / Pythonスクリプト新規実装：Claude Code / 文書作成・調査・タスク管理：Claude Code | 実装担当：Codex / Pythonスクリプト新規実装：Codex / 文書作成・調査・タスク管理：Codex |

この修正により、現在の作業体制と引き継ぎ文書の表記が一致した。
