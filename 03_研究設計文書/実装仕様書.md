# 実装仕様書

> 作成日：2026/04/28
> 対象：河川氾濫時避難シミュレーション（鬼怒川2015年）
> フェーズ：Phase 1 詳細 / Phase 2〜3 概要
> ステータス：実装前確定版

---

## 1. システム概要

### 1-1. 目的

2015年9月 鬼怒川氾濫（茨城県常総市）を事例として、以下の2シナリオを交通流シミュレーションで比較する。

| シナリオ | 内容 |
|---------|------|
| **A（ベースライン）** | 全住民が自家用車で避難。車非保有者は迎え走行依存 or 孤立。 |
| **B（提案）** | 車非保有・高齢者がデマンド交通バス（5台）で避難。迎え走行が不要になり道路上の車両が削減。 |

### 1-2. 評価指標

| 指標 | 定義 | Phase |
|------|------|-------|
| 逃げ遅れ人数 | 浸水到達時に浸水エリア内に残るエージェント数 | 1〜3 |
| 避難完了時間 | 全対象エージェントが避難所に到達するまでの時間 | 1〜3 |
| 渋滞発生規模 | 主要避難路の平均速度・渋滞継続時間 | 2〜3 |
| バス輸送人数 | バスが輸送した延べ人数（シナリオB） | 3 |

### 1-3. シミュレーション時間設定

| 項目 | 値 |
|------|-----|
| 破堤時刻 | 2015/09/10 12:50（常総市三坂町） |
| シミュレーション開始 | 12:50（t=0） |
| シミュレーション終了 | 18:50（t=21,600秒） |
| 浸水状況更新間隔 | 30分ごと（1,800秒ごと） |
| KML時点数 | 8時点（最初の3時点が発災後6時間内の主要変化） |

---

## 2. ディレクトリ・ファイル構成

```
04_プログラム/
├── data/
│   ├── flood_kml/
│   │   └── D1-No917_joso/                  # KML 8時点（展開済み）
│   ├── flood_hazard_a31/
│   │   └── A31a-24_08_10_GML/              # A31a GML（展開済み）
│   ├── population_mesh/
│   │   ├── 5歳階級別人口250メッシュ_茨城/
│   │   │   └── tblT001178Q08.txt           # 250mメッシュ人口
│   │   └── 年齢別男女別人口_茨城/
│   │       └── tblT000849C08.txt           # 市区町村別年齢別人口
│   ├── shelters/
│   │   └── 避難施設データ_茨城/
│   │       └── P20-12_08.dbf               # 避難施設（Shapefile DBF）
│   └── vehicle_stats/
│       └── 市区町村別自動車保有台数_茨城.md  # H29 車両保有台数
│
├── output/                                 # 生成ファイル出力先
│   ├── network/
│   │   ├── joso_road_network.graphml
│   │   └── joso_edges.gpkg
│   ├── flood/
│   │   ├── flood_polygons.pkl
│   │   └── flood_timeline_map.html
│   ├── closure/
│   │   ├── road_closure_timeline.json
│   │   └── road_closure_timeline.csv
│   ├── agents/
│   │   ├── origin_points.csv
│   │   └── shelters.csv
│   ├── routes/
│   │   ├── evacuation_routes_t{n}.html     # n=0〜7
│   │   └── unreachable_agents.csv
│   └── results/
│       ├── evacuation_summary.csv
│       ├── congestion_summary.csv
│       ├── ab_comparison.png
│       └── sensitivity_analysis.png
│
└── scripts/
    ├── config.py                           # 定数・設定値（全スクリプト共通）
    ├── c3_get_road_network.py              # C-3: 道路NW取得
    ├── e1_load_flood_data.py               # E-1: 浸水データ読み込み
    ├── i1_spatial_join.py                  # I-1: 空間照合
    ├── i2_generate_closure.py              # I-2: 封鎖リスト生成
    ├── i3_route_search.py                  # I-3: 迂回ルート検索
    ├── i4_convert_sumo.py                  # I-4: SUMO変換（Phase 2）
    ├── i5_scenario_a.py                    # I-5: シナリオA（Phase 2）
    ├── i6_traci_simulation.py              # I-6: TraCI動的閉鎖（Phase 2）
    ├── i7_scenario_b.py                    # I-7: シナリオB（Phase 3）
    ├── i8_sensitivity.py                   # I-8: 感度分析（Phase 3）
    └── i9_aggregate.py                     # I-9: 集計・比較（Phase 3）
```

---

## 3. 定数・設定値（config.py）

```python
# ===== 座標系 =====
CRS_WGS84   = "EPSG:4326"   # 入出力・folium可視化用
CRS_JGD2011 = "EPSG:6668"   # 空間演算の基準CRS（A31a GML準拠）
CRS_JGD2000 = "EPSG:4612"   # P20 Shapefile のCRS

# ===== 対象地域 =====
JOSO_PLACE   = "常総市, 茨城県, 日本"
JOSO_BBOX    = (139.90, 36.00, 140.10, 36.15)  # (lon_min, lat_min, lon_max, lat_max)
JOSO_CODE    = "08211"                           # 市区町村コード

# ===== 浸水閾値 =====
FLOOD_DEPTH_THRESHOLD = 2   # waterDepth コード値（A31a） >= 2 = 0.5m以上

# ===== シミュレーション時間 =====
SIM_START_EPOCH       = "2015-09-10T12:50:00"  # 破堤時刻（t=0）
SIM_DURATION_H        = 6                       # シミュレーション時間（時間）
SIM_UPDATE_INTERVAL_S = 1800                    # 浸水状況更新間隔（秒）

# ===== KML 8時点 =====
KML_TIMESTAMPS = [
    "2015-09-10T18:00:00",
    "2015-09-11T06:00:00",
    "2015-09-11T18:00:00",
    "2015-09-12T06:00:00",
    "2015-09-12T18:00:00",
    "2015-09-13T06:00:00",
    "2015-09-13T18:00:00",
    "2015-09-16T10:20:00",
]

# ===== エージェント設定 =====
TOTAL_POPULATION      = 61_483
TOTAL_HOUSEHOLDS      = 23_500
ELDERLY_RATE          = 0.27      # 65歳以上の比率
CAR_OWNERSHIP_RATE    = 0.85      # 車保有世帯の比率
FLOOD_AREA_RATIO      = 1/3       # 浸水エリアの市全体に対する割合
FLOOD_POP_ESTIMATE    = 19_800    # 浸水エリア内推定人口

# ===== バス設定（ベースケース） =====
BUS_COUNT_BASE       = 5      # バス台数
BUS_CAPACITY_STD     = 8      # 標準ミニバン定員
BUS_CAPACITY_WELFARE = 4      # 福祉車両定員
BUS_WELFARE_RATIO    = 0.20   # 福祉車両の比率
BUS_SPEED_KMH        = 20     # 平均走行速度（km/h）
BUS_ONEWAY_KM        = 5      # 片道平均距離（km）
BUS_BOARDING_MIN     = 5      # 乗降所要時間（分）
BUS_SENSITIVITY      = [3, 5, 10]  # 感度分析用台数

# ===== 道路ネットワーク =====
OSM_NETWORK_TYPE = "drive"   # osmnx の network_type

# ===== ファイルパス（scripts/ からの相対パス） =====
DATA_DIR   = "../data"
OUTPUT_DIR = "../output"

KML_DIR     = f"{DATA_DIR}/flood_kml/D1-No917_joso"
GML_DIR     = f"{DATA_DIR}/flood_hazard_a31/A31a-24_08_10_GML"
MESH_FILE   = f"{DATA_DIR}/population_mesh/5歳階級別人口250メッシュ_茨城/tblT001178Q08.txt"
SHELTER_DBF = f"{DATA_DIR}/shelters/避難施設データ_茨城/P20-12_08.dbf"

# ===== 出力パス =====
OUT_NETWORK_DIR = f"{OUTPUT_DIR}/network"
OUT_FLOOD_DIR   = f"{OUTPUT_DIR}/flood"
OUT_CLOSURE_DIR = f"{OUTPUT_DIR}/closure"
OUT_AGENTS_DIR  = f"{OUTPUT_DIR}/agents"
OUT_ROUTES_DIR  = f"{OUTPUT_DIR}/routes"
OUT_RESULTS_DIR = f"{OUTPUT_DIR}/results"

GRAPHML_PATH     = f"{OUT_NETWORK_DIR}/joso_road_network.graphml"
EDGES_GPKG_PATH  = f"{OUT_NETWORK_DIR}/joso_edges.gpkg"
NETWORK_MAP_PATH = f"{OUT_NETWORK_DIR}/joso_network_map.html"

FLOOD_PKL_PATH = f"{OUT_FLOOD_DIR}/flood_polygons.pkl"
FLOOD_MAP_PATH = f"{OUT_FLOOD_DIR}/flood_timeline_map.html"

CLOSURE_PKL_PATH  = f"{OUT_CLOSURE_DIR}/closure_dict.pkl"
CLOSURE_JSON_PATH = f"{OUT_CLOSURE_DIR}/road_closure_timeline.json"
CLOSURE_CSV_PATH  = f"{OUT_CLOSURE_DIR}/road_closure_timeline.csv"

ORIGINS_CSV_PATH  = f"{OUT_AGENTS_DIR}/origin_points.csv"
SHELTERS_CSV_PATH = f"{OUT_AGENTS_DIR}/shelters.csv"
UNREACHABLE_PATH  = f"{OUT_ROUTES_DIR}/unreachable_agents.csv"
```

---

## 4. データ入力仕様

### 4-1. 浸水KML（D1-No.917）

| 項目 | 値 |
|------|-----|
| ファイル形式 | KML（各時点1ファイル） |
| ジオメトリ型 | LineString のみ（Polygonなし） |
| 浸水深属性 | **なし**（深度情報は持たない） |
| 使用目的 | 各時点の浸水エリア境界 → 時刻別の封鎖タイミング決定 |
| 読み込み方法 | `geopandas.read_file(path, driver='KML')` |
| CRS | WGS84（EPSG:4326）→ 演算前にEPSG:6668へ変換 |

**処理方針：** KMLのLineStringはバッファ処理でPolygonに変換し、A31a GMLの waterDepth>=2 ポリゴンとの重ね合わせで封鎖エッジを確定する。

### 4-2. 浸水想定区域GML（A31a-2024）

| 項目 | 値 |
|------|-----|
| ファイル形式 | GML（国土数値情報） |
| フィーチャ型 | `ksj:PlanScale` |
| ジオメトリ型 | Polygon |
| 浸水深属性 | `ksj:waterDepth`（コード値 1〜6） |
| 対象河川 | riverNumber = `8303030018`（鬼怒川） |
| 閉鎖対象 | `ksj:waterDepth >= 2`（0.5m以上） |
| CRS | JGD2011（EPSG:6668） |
| 読み込み方法 | `geopandas.read_file(path, driver='GML')` |

**waterDepthコード値：**

| コード | 深さ |
|--------|------|
| 1 | 0〜0.5m未満 |
| **2** | **0.5〜3.0m未満 ← 閉鎖しきい値** |
| 3 | 3.0〜5.0m未満 |
| 4 | 5.0〜10.0m未満 |
| 5 | 10.0〜20.0m未満 |
| 6 | 20.0m以上 |

### 4-3. 250mメッシュ人口（T001178）

| 項目 | 値 |
|------|-----|
| ファイル | `tblT001178Q08.txt` |
| エンコーディング | Shift-JIS |
| KEY_CODE | 10桁（2次メッシュ6桁 + 250mメッシュ4桁） |
| 常総市メッシュ | KEY_CODE 先頭6桁 = `543907` または `543917` |
| 総列数 | 63列（col[0]=KEY_CODE, col[1]=名称, col[2]〜=人口データ） |
| 使用列 | col[0]（KEY_CODE）、col[2]（総人口）、高齢者人口列（実ファイルで確認） |
| 読み込み方法 | `pandas.read_csv(path, encoding='shift_jis', dtype=str)` |

**注意：** 先頭1〜2行がヘッダのため `skiprows` の行数を実ファイルで確認すること。

### 4-4. 避難施設データ（P20 Shapefile）

| 項目 | 値 |
|------|-----|
| ファイル | `P20-12_08.dbf`（SHP/SHX/PRJとセット） |
| 茨城県全レコード数 | 2,326件 |
| 常総市レコード数 | 64件（P20_001 = `08211`） |
| 洪水対応施設数 | **19件**（P20_007 = `1`） |
| CRS | JGD2000（EPSG:4612） |
| 読み込み方法 | `geopandas.read_file(path)` |

**主要フィールド：**

| フィールド | 内容 |
|-----------|------|
| P20_001 | 市区町村コード |
| P20_002 | 施設名 |
| P20_003 | 住所 |
| P20_005 | 収容人数 |
| P20_007 | 洪水対応フラグ（1=対応） |
| geometry | 座標（Point） |

**抽出条件：** `(P20_001 == "08211") & (P20_007 == "1")`

### 4-5. 道路ネットワーク（OSM via osmnx）

| 項目 | 値 |
|------|-----|
| 取得方法 | `osmnx.graph_from_place()` |
| 対象地域 | `"常総市, 茨城県, 日本"` |
| ネットワーク種別 | `network_type="drive"` |
| 保存形式 | GraphML（再取得防止） |
| CRS変換 | gdf変換後にEPSG:6668へ統一 |

---

## 5. モジュール仕様（Phase 1）

### 5-1. c3_get_road_network.py

**目的：** 常総市の道路ネットワークを取得し、GraphML・GeoPackage形式で保存する。

**入力：** なし（osmnxがOSM APIから取得）

**処理フロー：**
```
1. ox.graph_from_place(JOSO_PLACE, network_type=OSM_NETWORK_TYPE)
2. GraphML形式で保存 → joso_road_network.graphml
3. ox.graph_to_gdfs() でノード・エッジをGeoDataFrameに変換
4. エッジGDFをEPSG:6668に変換して保存 → joso_edges.gpkg
5. foliumで道路NWを可視化 → joso_network_map.html
```

**出力：**

| ファイル | 形式 | 内容 |
|---------|------|------|
| `output/network/joso_road_network.graphml` | GraphML | networkxグラフ（再利用用） |
| `output/network/joso_edges.gpkg` | GeoPackage | エッジGeoDataFrame（空間照合用） |
| `output/network/joso_network_map.html` | HTML | foliumによる可視化 |

**主要関数シグネチャ：**
```python
def get_road_network() -> nx.MultiDiGraph
def save_network(G: nx.MultiDiGraph, path: str) -> None
def network_to_gdf(G: nx.MultiDiGraph) -> tuple[gpd.GeoDataFrame, gpd.GeoDataFrame]
def visualize_network(edges_gdf: gpd.GeoDataFrame, output_path: str) -> None
```

---

### 5-2. e1_load_flood_data.py

**目的：** A31a GMLから浸水深>=0.5mのポリゴンを抽出し、KML時系列と組み合わせて時刻別浸水マップを構築する。

**統合戦略（Option B）：**
- A31a waterDepth>=2 → 「最終的に閉鎖されうる道路」の候補集合を確定
- KML時系列 → 各時点でどこまで浸水が進んでいるかの境界を取得
- 交差判定で「KML時点 t において浸水エリア内にある A31a ポリゴン」を閉鎖対象とする

**入力：**
- `GML_DIR/` 以下のGMLファイル
- `KML_DIR/` 以下の8時点KMLファイル

**処理フロー：**
```
1. A31a GML読み込み → waterDepth >= FLOOD_DEPTH_THRESHOLD のポリゴンを抽出
2. CRS変換 → EPSG:6668
3. KMLファイルをタイムスタンプ順に読み込む（8ファイル）
4. 各KML時点のLineStringを buffer(幅=10m) でPolygon化
5. A31aポリゴン × KML時点ポリゴンで intersects 判定
6. 時刻 → 浸水ポリゴンGeoDataFrameの辞書を生成
7. picklで保存 → flood_polygons.pkl
8. foliumで時刻別浸水範囲を可視化 → flood_timeline_map.html
```

**出力：**

| ファイル | 形式 | 内容 |
|---------|------|------|
| `output/flood/flood_polygons.pkl` | pickle | `dict[str, GeoDataFrame]` タイムスタンプ→浸水ポリゴン |
| `output/flood/flood_timeline_map.html` | HTML | 時刻別浸水範囲の可視化 |

**主要関数シグネチャ：**
```python
def load_a31a_gml(gml_dir: str) -> gpd.GeoDataFrame
def load_kml_timeline(kml_dir: str) -> dict[str, gpd.GeoDataFrame]
def build_flood_polygons(
    a31a: gpd.GeoDataFrame,
    kml_timeline: dict[str, gpd.GeoDataFrame]
) -> dict[str, gpd.GeoDataFrame]
def save_flood_polygons(flood_dict: dict, path: str) -> None
def visualize_flood_timeline(flood_dict: dict, output_path: str) -> None
```

---

### 5-3. i1_spatial_join.py

**目的：** 各時点の浸水ポリゴンと道路エッジを重ね合わせ、封鎖対象エッジIDを時点ごとに特定する。

**入力：**
- `output/network/joso_edges.gpkg`
- `output/flood/flood_polygons.pkl`

**処理フロー：**
```
1. 道路エッジをGeoPackageから読み込む
2. 浸水ポリゴン辞書をpickleから読み込む
3. CRS確認（両者ともEPSG:6668であることを assert で保証）
4. 各時点について：
   a. geopandas.sjoin(edges, flood_poly, how='left', predicate='intersects')
   b. 結合されたエッジのosmidをリストに追加
5. 時点別の閉鎖エッジID辞書を返す
6. 時点ごとの封鎖エッジ数を確認出力
```

**出力：** `dict[str, list[str]]`（タイムスタンプ → 閉鎖エッジIDリスト、次モジュールへ渡す）

**主要関数シグネチャ：**
```python
def load_edges(path: str) -> gpd.GeoDataFrame
def find_flooded_edges(
    edges: gpd.GeoDataFrame,
    flood_poly: gpd.GeoDataFrame
) -> list[str]
def build_closure_dict(
    edges: gpd.GeoDataFrame,
    flood_dict: dict[str, gpd.GeoDataFrame]
) -> dict[str, list[str]]
```

---

### 5-4. i2_generate_closure.py

**目的：** 空間照合結果を JSON・CSV ファイルとして出力する。

**入力：** `i1_spatial_join.py` の出力辞書

**処理フロー：**
```
1. 閉鎖辞書をJSON形式で保存 → road_closure_timeline.json
2. 同内容をCSV形式で保存 → road_closure_timeline.csv
3. 出力件数・時刻対応を標準出力で確認
```

**出力ファイル形式：**

`road_closure_timeline.json`
```json
{
  "2015-09-10T18:00:00": ["111_222_0", "333_444_0"],
  "2015-09-11T06:00:00": ["111_222_0", "555_666_0"]
}
```

`road_closure_timeline.csv`
```
timestamp,edge_id,osmid_u,osmid_v
2015-09-10T18:00:00,111_222_0,111,222
```

**主要関数シグネチャ：**
```python
def save_closure_json(closure_dict: dict, path: str) -> None
def save_closure_csv(
    closure_dict: dict,
    edges_gdf: gpd.GeoDataFrame,
    path: str
) -> None
```

---

### 5-5. i3_route_search.py

**目的：** 250mメッシュ重心（浸水エリア内）から最寄り避難所へのDijkstra最短経路を、各時点の封鎖状況を反映しながら計算する。逃げ遅れエージェント（到達不可）をカウントする。

**入力：**
- `output/network/joso_road_network.graphml`
- `output/closure/road_closure_timeline.json`
- `MESH_FILE`（T001178）
- `SHELTER_DBF`（P20）

**処理フロー：**
```
1. 出発地（origin_points）の生成：
   a. T001178 を Shift-JIS で読み込み
   b. KEY_CODE先頭6桁が 543907 または 543917 のメッシュを抽出
   c. 各メッシュのBounding Boxから重心座標を計算
   d. A31a浸水ポリゴンと重ね合わせて浸水エリア内メッシュのみ残す
   e. origin_points.csv として保存

2. 避難所（destinations）の整備：
   a. P20 DBFを geopandas で読み込む
   b. P20_001="08211" かつ P20_007="1" を抽出（19件）
   c. CRS変換 EPSG:4612 → EPSG:4326
   d. shelters.csv として保存

3. 各時点の経路計算：
   a. GraphMLからグラフGを読み込む
   b. 各出発地から最近傍ノードを取得（ox.distance.nearest_nodes）
   c. 各避難所から最近傍ノードを取得
   d. 各時点の閉鎖エッジをGから除いたサブグラフを生成
   e. networkx.shortest_path(weight='length') でDijkstra経路計算
   f. 経路なし（NetworkXNoPath）のエージェントを記録

4. 出力ファイルを保存
5. foliumで時点別ルートを可視化
```

**出力ファイル：**

| ファイル | 内容 |
|---------|------|
| `output/agents/origin_points.csv` | 出発地（KEY_CODE, lon, lat, total_pop, elderly_pop） |
| `output/agents/shelters.csv` | 避難所19件（name, capacity, lon, lat） |
| `output/routes/unreachable_agents.csv` | 逃げ遅れ（timestamp, KEY_CODE, total_pop, elderly_pop） |
| `output/routes/evacuation_routes_t{n}.html` | 時点別ルート可視化（n=0〜7） |

**主要関数シグネチャ：**
```python
def load_mesh_origins(
    mesh_file: str,
    flood_poly: gpd.GeoDataFrame
) -> gpd.GeoDataFrame

def load_shelters(dbf_path: str) -> gpd.GeoDataFrame

def make_subgraph(
    G: nx.MultiDiGraph,
    closed_edges: list[str]
) -> nx.MultiDiGraph

def find_nearest_node(G: nx.MultiDiGraph, lon: float, lat: float) -> int

def compute_route(
    G: nx.MultiDiGraph,
    origin_node: int,
    dest_nodes: list[int]
) -> list[int] | None

def run_all_timesteps(
    G: nx.MultiDiGraph,
    closure_timeline: dict,
    origins: gpd.GeoDataFrame,
    destinations: gpd.GeoDataFrame
) -> dict[str, gpd.GeoDataFrame]
```

---

## 6. モジュール仕様（Phase 2 概要）

Phase 2 は SUMO による交通流シミュレーションを実装する。Phase 1 の出力（道路NW・封鎖リスト・出発地・避難所）を入力として使用する。

| スクリプト | 主な処理 |
|-----------|---------|
| `i4_convert_sumo.py` | osmnxグラフ → OSM XML → netconvertで `.net.xml` に変換 |
| `i5_scenario_a.py` | 出発地メッシュから車両エージェントを生成し `.rou.xml` を出力 |
| `i6_traci_simulation.py` | TraCIでシミュレーション実行中に浸水時系列に応じてエッジを動的閉鎖。渋滞指標・逃げ遅れ車両を記録 |

**Phase 2 主要出力ファイル：**

| ファイル | 内容 |
|---------|------|
| `joso.osm` | OSM XML形式の道路NW |
| `joso.net.xml` | SUMO形式の道路NW |
| `scenario_a.rou.xml` | シナリオA 車両・経路定義 |
| `joso_sim.sumocfg` | SUMO設定ファイル |
| `stranded_vehicles.csv` | 逃げ遅れ車両ログ |
| `congestion_log.csv` | 時系列渋滞指標ログ |

---

## 7. モジュール仕様（Phase 3 概要）

Phase 3 は シナリオB（バス追加）・感度分析・A/B比較の可視化を実装する。

| スクリプト | 主な処理 |
|-----------|---------|
| `i7_scenario_b.py` | バス5台を SUMO PT形式で追加。Type 3・4エージェントをバス乗降設定に変更 |
| `i8_sensitivity.py` | バス台数 3/5/10 台の `.rou.xml` を自動生成し一括実行 |
| `i9_aggregate.py` | シナリオA・B の逃げ遅れ人数・渋滞指標・避難完了時間を集計し比較グラフを生成 |

---

## 8. データフロー

```
[A31a GML]          [KML 8時点]
     |                   |
     +----> e1_load_flood_data.py <----+
                  |
         [flood_polygons.pkl]

[OSM via osmnx]
     |
     v
c3_get_road_network.py
     |
[joso_road_network.graphml]
[joso_edges.gpkg]

[flood_polygons.pkl] + [joso_edges.gpkg]
     |
     v
i1_spatial_join.py
     |
[閉鎖エッジ辞書（メモリ）]
     |
     v
i2_generate_closure.py
     |
[road_closure_timeline.json / .csv]

[joso_road_network.graphml]
[road_closure_timeline.json]
[T001178 メッシュCSV]
[P20 DBF 避難施設]
     |
     v
i3_route_search.py
     |
[origin_points.csv]       → Phase 1 成果物
[shelters.csv]            → Phase 1 成果物
[unreachable_agents.csv]  → 逃げ遅れ数（Phase 1 最終評価）
[evacuation_routes_t{n}.html]
```

---

## 9. CRS管理方針

| 処理段階 | 使用CRS |
|---------|---------|
| A31a GML読み込み | EPSG:6668（ソースのまま） |
| KML読み込み | EPSG:4326 → EPSG:6668に変換 |
| P20 DBF読み込み | EPSG:4612 → EPSG:6668に変換 |
| 空間演算（sjoin, intersects） | **EPSG:6668で統一** |
| osmnx グラフ | EPSG:4326 → gdf変換後EPSG:6668 |
| folium可視化用出力 | EPSG:4326に戻す |

**原則：** 空間演算の直前に全レイヤーを `to_crs("EPSG:6668")` で統一し、`assert gdf.crs.to_epsg() == 6668` で確認する。

---

## 10. エラー処理方針

| ケース | 対応 |
|--------|------|
| KMLが読み込めない（driver未対応） | `pyogrio.supported_drivers` 確認、`LIBKML` または `KML` を明示指定 |
| GML読み込み失敗 | `pyogrio.listlayers()` でレイヤー名確認後に `layer=` 指定 |
| 経路が存在しない（孤立ノード） | `try/except nx.NetworkXNoPath` で捕捉し unreachable に記録 |
| メッシュの座標変換エラー | KEY_CODEから座標を逆算する関数で対処（JIS X 0410準拠） |
| osmnxの取得失敗（ネットワーク不通） | GraphMLキャッシュが存在する場合はキャッシュから読み込む |

---

## 11. テスト方針

Phase 1 の各スクリプトは `if __name__ == "__main__":` ブロックで個別実行できるようにし、以下を確認する。

| スクリプト | 確認事項 |
|-----------|---------|
| c3_get_road_network.py | エッジ数・ノード数が出力される。foliumマップで常総市が正しく描画される |
| e1_load_flood_data.py | 8時点分のポリゴン辞書が生成される。ポリゴンが常総市付近に位置する |
| i1_spatial_join.py | 時点が進むにつれ閉鎖エッジ数が単調増加する（累積閉鎖の確認） |
| i2_generate_closure.py | JSON/CSVが正しい形式で出力される。タイムスタンプ数=8 |
| i3_route_search.py | t=0で全メッシュ重心から少なくとも1件の到達可能ルートが存在する。t=7で到達不可エージェントが増加している |

---

## 12. 実装順序と依存関係

```
Env-1〜3（Python環境構築）
         |
    +----+----+
    |         |
    v         v
c3_get_road   e1_load_flood     ← 並列実行可
_network.py   _data.py
    |         |
    +----+----+
         |
         v
  i1_spatial_join.py
         |
         v
  i2_generate_closure.py
         |
         v
  i3_route_search.py            ← Phase 1 完了
         |
         v
  i4 → i5 → i6                 ← Phase 2（SUMO）
         |
         v
  i7 → i8 → i9                 ← Phase 3（比較・集計）
```

**並列実行可能なペア：**
`c3_get_road_network.py` と `e1_load_flood_data.py` は依存関係なし → 並列実行可。

---

## 13. 未確定事項（実装時に決定）

| 項目 | 現時点の方針 | 決定タイミング |
|------|-------------|--------------|
| KML LineStringのBuffer幅 | 10m（道路幅の概算）で試行、結果を見て調整 | e1実装時 |
| 250mメッシュの高齢者人口列インデックス | T001178の列定義を実ファイルで確認 | i3実装時 |
| osmnxのplace名 vs bbox指定 | place名優先、失敗時はBBOX指定にフォールバック | c3実装時 |
| Phase 2 SUMOバージョン | インストール時に確定 | i4実装時 |
