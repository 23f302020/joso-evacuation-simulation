# データフロー図

> 作成日：2026/04/28
> 対象：Phase 1 スクリプト群（常総市実データ版、常総市シナリオ版、茨城県内拡張版）のデータ変換パイプライン
> 表示方法：Mermaid 図は VS Code「Markdown Preview Mermaid Support」拡張、または GitHub で自動レンダリング

---

## 1. Phase 1 常総市実データ版データフロー

```mermaid
flowchart TD
    %% ===== 外部入力データ =====
    subgraph INPUT["📥 外部入力データ"]
        direction TB
        OSM_API["🌐 OpenStreetMap API\n（常総市bbox）"]
        KML_FILES["📁 flood_kml/*.kml\n8ファイル（LineString）\nEPSG:4326"]
        GML_FILES["📁 A31a-24_08_10_GML/*.gml\n84ファイル（Polygon）\nwaterDepth属性付き\nEPSG:6668"]
        MESH_CSV["📄 tblT001178Q08.txt\n250mメッシュ人口\nShift-JIS, 63列"]
        SHP_FILE["📁 P20-12_08.shp\n避難施設Shapefile\nEPSG:4612"]
    end

    %% ===== config.py =====
    CFG["⚙ config.py\nSTEP 0\n全定数・パス定義"]

    %% ===== STEP 1a =====
    subgraph S1A["STEP 1a — c3_get_road_network.py"]
        direction TB
        S1A_1["osmnx: graph_from_bbox()"]
        S1A_2["graph_to_gdfs()"]
        S1A_3["to_crs(EPSG:6668)"]
    end

    %% ===== STEP 1a 出力 =====
    subgraph OUT1A["📤 output/network/"]
        GRAPHML["joso_road_network.graphml\nMultiDiGraph\n（networkx）"]
        GPKG["joso_edges.gpkg\nGeoDataFrame\nEPSG:6668"]
        NET_HTML["joso_network_map.html\nfolium可視化"]
    end

    %% ===== STEP 1b =====
    subgraph S1B["STEP 1b — e1_load_flood_data.py"]
        direction TB
        S1B_1["KML読み込み\npyogrio driver='KML'"]
        S1B_2["LineString → buffer(50m, EPSG:6690)\n→ convex_hull エンベロープ\nEPSG:6668"]
        S1B_3["GML読み込み\nxml.etree.ElementTree\n（xlink手動解決）"]
        S1B_4["waterDepth>=2 フィルタ\n+ N03常総市境界クリップ"]
        S1B_5["KML凸包エンベロープ × A31a\nintersects → 時刻別統合\n{timestamp: GeoDataFrame}"]
    end

    %% ===== STEP 1b 出力 =====
    subgraph OUT1B["📤 output/flood/"]
        FPKL["flood_polygons.pkl\ndict[str, GeoDataFrame]\n8時点分"]
        FLOOD_HTML["flood_timeline_map.html\nfolium可視化"]
    end

    %% ===== STEP 2 =====
    subgraph S2["STEP 2 — i1_spatial_join.py"]
        direction TB
        S2_1["CRS確認\nassert == EPSG:6668"]
        S2_2["gpd.sjoin()\npredicate='intersects'\n8時点ループ"]
        S2_3["edge_id抽出\n→ closure_dict"]
    end

    %% ===== STEP 2 出力 =====
    subgraph OUT2["📤 output/closure/（中間）"]
        CPKL["closure_dict.pkl\ndict[str, list[str]]\n{timestamp: [edge_id,...]}"]
    end

    %% ===== STEP 3 =====
    subgraph S3["STEP 3 — i2_generate_closure.py"]
        direction TB
        S3_1["辞書 → JSON変換"]
        S3_2["辞書 → DataFrame → CSV変換"]
    end

    %% ===== STEP 3 出力 =====
    subgraph OUT3["📤 output/closure/"]
        CJSON["road_closure_timeline.json\n{timestamp: [edge_id,...]}"]
        CCSV["road_closure_timeline.csv\nheader: timestamp, edge_id"]
    end

    %% ===== STEP 4 =====
    subgraph S4["STEP 4 — i3_route_search.py"]
        direction TB
        S4_1["メッシュ重心計算\n→ origin_points"]
        S4_2["避難所フィルタ\n洪水対応 & 常総市 → 19件"]
        S4_3["8時点ループ\nG.remove_edges(封鎖エッジ)"]
        S4_4["nx.shortest_path()\nDijkstra（重み=length）"]
        S4_5["到達不可判定\nNetworkXNoPath"]
        S4_6["folium可視化\n時点別ルート"]
    end

    %% ===== STEP 4 出力 =====
    subgraph OUT4["📤 output/agents/ & routes/"]
        ORIG["origin_points.csv\n出発地（メッシュ重心・人口）"]
        SHELT["shelters.csv\n避難所19件（座標・収容人数）"]
        UNREACH["unreachable_agents.csv\n実データ版の到達不可集計"]
        RHTML["evacuation_routes_t0〜t7.html\nfolium可視化（8ファイル）"]
    end

    %% ===== 接続 =====
    CFG -.->|"import"| S1A
    CFG -.->|"import"| S1B
    CFG -.->|"import"| S2
    CFG -.->|"import"| S3
    CFG -.->|"import"| S4

    OSM_API -->|"osmnx API"| S1A_1
    S1A_1 --> S1A_2 --> S1A_3
    S1A --> GRAPHML
    S1A --> GPKG
    S1A --> NET_HTML

    KML_FILES -->|"pyogrio KML"| S1B_1
    S1B_1 --> S1B_2
    GML_FILES -->|"pyogrio GML"| S1B_3
    S1B_3 --> S1B_4 --> S1B_5
    S1B_2 --> S1B_5
    S1B --> FPKL
    S1B --> FLOOD_HTML

    GPKG -->|"edges_gdf"| S2_1
    FPKL -->|"flood_polygons"| S2_1
    S2_1 --> S2_2 --> S2_3
    S2 --> CPKL

    CPKL -->|"closure_dict"| S3_1
    CPKL -->|"closure_dict"| S3_2
    S3_1 --> CJSON
    S3_2 --> CCSV

    GRAPHML -->|"road graph G"| S4_3
    CJSON -->|"closure_dict"| S4_3
    MESH_CSV -->|"人口データ"| S4_1
    SHP_FILE -->|"避難施設"| S4_2
    S4_1 --> ORIG
    S4_2 --> SHELT
    ORIG -->|"出発地座標"| S4_3
    SHELT -->|"避難所座標"| S4_3
    S4_3 --> S4_4 --> S4_5
    S4_4 --> S4_6
    S4_5 --> UNREACH
    S4_6 --> RHTML
```

---

## 2. データ変換詳細（型・形式の変化）

```mermaid
flowchart LR
    subgraph C3変換["STEP 1a — 道路NW"]
        A1["osmnx MultiDiGraph\n（Python object）"]
        A2["GeoDataFrame\n（edges, EPSG:4326）"]
        A3["GeoDataFrame\n（edges, EPSG:6668）"]
        A4["GraphML file\n（XML形式）"]
        A5["GeoPackage file\n（バイナリ, EPSG:6668）"]
        A1 -->|"graph_to_gdfs()"| A2
        A2 -->|"to_crs('EPSG:6668')"| A3
        A1 -->|"save_graphml()"| A4
        A3 -->|"to_file(driver='GPKG')"| A5
    end

    subgraph E1変換["STEP 1b — 浸水データ"]
        B1["KML LineString\n（EPSG:4326）"]
        B2["GeoDataFrame\n（EPSG:6668）"]
        B3["Polygon GDF\n（buffer 50m）"]
        B4["GML Polygon\n（EPSG:6668, waterDepth属性）"]
        B5["統合Polygon GDF\n（waterDepth>=2 絞り込み）"]
        B6["dict[str, GeoDataFrame]\n（8時点分）"]
        B7["pickle バイナリ\n（.pkl）"]
        B1 -->|"to_crs('EPSG:6668')"| B2
        B2 -->|".buffer(50)"| B3
        B4 -->|"query(waterDepth>=2)"| B5
        B3 & B5 -->|"時刻別統合"| B6
        B6 -->|"pickle.dump()"| B7
    end

    subgraph I1変換["STEP 2 — 空間照合"]
        C1["edges_gdf\n（EPSG:6668）"]
        C2["flood_gdf\n（EPSG:6668）"]
        C3X["sjoin結果\n（edge_id列）"]
        C4["dict[str, list[str]]\n（封鎖edge_idリスト）"]
        C5["pickle バイナリ\n（.pkl）"]
        C1 & C2 -->|"gpd.sjoin(intersects)"| C3X
        C3X -->|"8時点集約"| C4
        C4 -->|"pickle.dump()"| C5
    end

    subgraph I2変換["STEP 3 — 封鎖リスト"]
        D1["dict[str, list[str]]"]
        D2["JSON文字列"]
        D3["DataFrame\n(timestamp, edge_id)"]
        D4[".json ファイル"]
        D5[".csv ファイル"]
        D1 -->|"json.dump()"| D2 --> D4
        D1 -->|"pd.DataFrame()"| D3
        D3 -->|"to_csv()"| D5
    end

    subgraph I3変換["STEP 4 — ルート検索"]
        E1X["MultiDiGraph G\n（networkx）"]
        E2["G_sub\n（封鎖エッジ除外）"]
        E3["最短経路\n（node list）"]
        E4["unreachable list"]
        E5[".csv ファイル"]
        E6["folium Map"]
        E7[".html ファイル"]
        E1X -->|"remove_edges_from()"| E2
        E2 -->|"nx.shortest_path()"| E3
        E2 -->|"NetworkXNoPath"| E4
        E4 -->|"to_csv()"| E5
        E3 -->|"PolyLine描画"| E6
        E6 -->|"map.save()"| E7
    end
```

---

## 3. CRS（座標参照系）変換フロー

```mermaid
flowchart LR
    WGS["EPSG:4326\nWGS84\n（KML・OSM入力）"]
    JGD2000["EPSG:4612\nJGD2000\n（P20 Shapefile入力）"]
    JGD2011["EPSG:6668\nJGD2011\n★ 空間演算の統一CRS"]
    FOLIUM["EPSG:4326\n（folium描画用）"]

    WGS -->|"to_crs('EPSG:6668')\nKML読込後・osmnx GDF変換後"| JGD2011
    JGD2000 -->|"to_crs('EPSG:6668')\nP20 Shapefile読込後"| JGD2011
    JGD2011 -->|"to_crs('EPSG:4326')\nfolium描画前"| FOLIUM
```

**原則：** すべての空間演算（`sjoin`・`intersects`・`buffer`）はEPSG:6668に統一してから実行する。
演算前に `assert gdf.crs.to_epsg() == 6668` で確認すること。

---

## 4. ファイル形式・依存関係サマリ

| ファイル | 形式 | 生成スクリプト | 消費スクリプト | 備考 |
|---------|------|--------------|--------------|------|
| `joso_road_network.graphml` | XML | c3 | i3 | networkx互換、キャッシュ再利用可 |
| `joso_edges.gpkg` | GeoPackage | c3 | i1 | EPSG:6668固定 |
| `joso_network_map.html` | HTML | c3 | — | ブラウザ確認用 |
| `flood_polygons.pkl` | pickle | e1 | i1 | `dict[str, GeoDataFrame]`、8時点 |
| `flood_timeline_map.html` | HTML | e1 | — | ブラウザ確認用 |
| `closure_dict.pkl` | pickle | i1 | i2 | `dict[str, list[str]]` 中間ファイル |
| `road_closure_timeline.json` | JSON | i2 | i3 | `{timestamp: [edge_id,...]}` |
| `road_closure_timeline.csv` | CSV | i2 | — | `timestamp, edge_id` 2列 |
| `origin_points.csv` | CSV | i3 | — | 出発地座標・人口 |
| `shelters.csv` | CSV | i3 | — | 避難所19件・収容人数 |
| `unreachable_agents.csv` | CSV | i3 | — | 実データ版の到達不可集計 |
| `evacuation_routes_t{n}.html` | HTML | i3 | — | t0〜t7の8ファイル |

---

## 5. Phase 1 シナリオ型拡張データフロー（最終版）

Phase 1 の仕上げとして、実データ版の静的ルートHTMLに加え、A31a最終浸水範囲を段階的に拡大するシナリオ版HTMLを作成した。シナリオ版は実災害の浸水推移を完全再現するものではなく、2015年鬼怒川氾濫の実データを根拠にした避難ルート探索機能の検証フローとして扱う。

| 項目 | 修正前 | 修正後 |
| --- | --- | --- |
| 浸水データ入力 | 実データ由来の時刻別浸水ポリゴン | A31a `08_10` の `waterDepth>=2` をN03常総市境界でクリップした最大浸水範囲 |
| 時系列化 | KML/浸水ナビ由来の時刻別浸水範囲 | BP030近傍破堤点からの距離分位点で t0〜t7 に累積配分 |
| 道路閉鎖 | 実データの時刻別浸水範囲と道路リンクを照合 | シナリオ浸水範囲と道路リンクを照合し、12→33本の単調増加閉鎖を生成 |
| ルート出発地 | 浸水エリアと交差した人口メッシュ重心 | 地図上でクリックした任意地点 |
| ルート出力 | 時刻ごとの静的 HTML | 時刻選択・地点指定に応じたシナリオ版 HTML |

```mermaid
flowchart TD
    subgraph INPUT_V2["入力データ"]
        A31["A31a 08_10 GML\nwaterDepth>=2"]
        N03["N03行政区域\n常総市 08211"]
        BP["BP030近傍破堤点\nlat 36.0885 / lon 139.9633"]
        ROAD["joso_road_network.graphml\njoso_edges.gpkg"]
        SHELTER["P20 洪水対応避難所\n常総市19件"]
    end

    subgraph SCRIPT_V2["v2_scenario_route_simulation.py"]
        V1["最大浸水範囲作成\nA31a × N03常総市\n290ポリゴン"]
        V2["距離計算\n破堤点→各ポリゴン重心\nEPSG:6690"]
        V3["t0〜t7へ累積配分\n進行率 3.7%→100%"]
        V4["道路閉鎖生成\nシナリオ浸水範囲 × 道路リンク"]
        V5["HTML用データ生成\n道路グラフ・浸水・閉鎖・避難所"]
    end

    subgraph OUTPUT_V2["output/scenario_v2/"]
        HTML["scenario_route_simulation.html\n時刻選択・地点指定・ルート表示"]
        CSV["scenario_v2_summary.csv\n進行率・ポリゴン数・閉鎖数"]
        DATAJS["assets/scenario_v2_data.js\n埋め込みデータ"]
        APPJS["assets/scenario_v2_app.js\nJS Dijkstra・UI制御"]
        CSS["assets/scenario_v2.css"]
    end

    A31 & N03 --> V1
    BP --> V2
    V1 --> V2 --> V3 --> V4
    ROAD --> V4
    ROAD & SHELTER & V3 & V4 --> V5
    V5 --> HTML
    V5 --> CSV
    V5 --> DATAJS
    V5 --> APPJS
    V5 --> CSS
```

### 5-1. シナリオ版の時点別出力

| 時点 | 時刻 | 進行率 | 採用ポリゴン数 | 閉鎖エッジ数 |
| --- | --- | ---: | ---: | ---: |
| t0 | 2015-09-10 18:00 | 3.7% | 11 | 12 |
| t1 | 2015-09-11 06:00 | 12.2% | 36 | 12 |
| t2 | 2015-09-11 18:00 | 20.6% | 60 | 15 |
| t3 | 2015-09-12 06:00 | 29.1% | 85 | 15 |
| t4 | 2015-09-12 18:00 | 37.6% | 109 | 18 |
| t5 | 2015-09-13 06:00 | 46.1% | 134 | 18 |
| t6 | 2015-09-13 18:00 | 54.6% | 158 | 20 |
| t7 | 2015-09-16 10:20 | 100.0% | 290 | 33 |

このフローにより、常総市では「実データ版の道路閉鎖・ルート探索」と「シナリオ版の任意地点ルート探索」の2系統で成果を説明できる状態になった。

---

## 6. Phase 1 茨城県内拡張・統合シミュレーションデータフロー（最終成果物）

茨城県内拡張版では、常総市シナリオ版の考え方を市区町村単位へ拡張し、41市区町村の個別HTMLと県全体を確認できる統合シミュレーションを作成した。卒論では、この統合シミュレーションを Phase 1 の主成果物として扱う。

```mermaid
flowchart TD
    subgraph INPUT_CITY["入力データ"]
        A31_10["A31a 08_10\n国管理河川"]
        A31_20["A31a 08_20\nその他河川"]
        N03_CITY["N03行政区域\n茨城県市区町村境界"]
        GSI_SHELTER["GSI指定緊急避難場所\n洪水対応"]
        OSM_CITY["OpenStreetMap\n市区町村別道路NW"]
    end

    subgraph CITY_SCRIPT["city_scenario.py"]
        C1["市区町村コードを指定"]
        C2["A31aを行政境界で抽出"]
        C3["waterDepth>=2を閉鎖対象に設定"]
        C4["道路リンクと浸水想定区域を照合"]
        C5["避難所・道路グラフ・閉鎖道路をHTML用データ化"]
    end

    subgraph CITY_OUTPUT["output/scenario_cities/{code}/"]
        CITY_HTML["scenario_route_simulation.html\n市区町村別ルート検索"]
        CITY_DATA["assets/data.js\n区域・浸水・閉鎖道路・避難所"]
    end

    subgraph UNIFIED_SCRIPT["統合版生成"]
        U1["prefecture_layers/t0.js〜t7.js\n県全体レイヤー"]
        U2["gen_unified.py\n統合シミュレーション生成"]
        U3["gen_index.py\n確認トップページ生成"]
    end

    subgraph FINAL_OUTPUT["Phase 1 最終成果物"]
        INDEX["output/index.html\n成果物入口"]
        UNIFIED["output/unified/scenario_route_simulation.html\n茨城県統合シミュレーション"]
        EXCLUDED["対象外3市町村\n鹿嶋市・神栖市・東海村"]
    end

    A31_10 & A31_20 & N03_CITY & GSI_SHELTER & OSM_CITY --> CITY_SCRIPT
    C1 --> C2 --> C3 --> C4 --> C5
    CITY_SCRIPT --> CITY_HTML
    CITY_SCRIPT --> CITY_DATA
    CITY_OUTPUT --> U1
    U1 --> U2 --> UNIFIED
    CITY_OUTPUT --> U3
    EXCLUDED --> U3
    U3 --> INDEX
    INDEX --> UNIFIED
    INDEX --> CITY_HTML
```

### 6-1. 茨城県内拡張版の固定値

| 項目 | 値 |
|---|---:|
| 茨城県内市区町村数 | 44 |
| シナリオ生成対象 | 41 |
| 対象外市町村 | 3 |
| 対象外市町村名 | 鹿嶋市、神栖市、東海村 |
| 市区町村別HTML | `output/scenario_cities/{code}/scenario_route_simulation.html` |
| 統合版HTML | `output/unified/scenario_route_simulation.html` |

`output/scenario_cities/` の41ディレクトリと、`output/assets/phase1-pages.js` の市区町村リンク41件・対象外3件が一致することを確認済みである。
