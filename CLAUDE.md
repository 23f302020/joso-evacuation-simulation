# 4年次本研究 — CLAUDE.md

## プロジェクト概要

**テーマ：** 河川氾濫時における自家用車避難とデマンド交通バス活用の比較シミュレーション  
**事例：** 2015年9月 鬼怒川氾濫（茨城県常総市）  
**フェーズ：** Phase 1 完了 → Phase 2（SUMO）→ Phase 3（バス比較）

---

## Fact-Forcing Gate（フック対応）

Edit/Write/Bash の直前テキストとして以下を明示すること：

```
**Fact-Forcing Gate 回答：**
1. [対象ファイルを参照/インポートしているファイル、なければ「なし」]
2. [影響する公開関数/クラス、なければ「なし」]
3. [読み書きするデータファイルの構造、なければ「なし」]
4. [ユーザー指示の引用（verbatim）]
```

---

## 現在フェーズ（最終更新：2026/07/11）

- **A'（シナリオA・teleport=-1）が完走し、床効果は不成立と確定**（arrived 9,090／not_arrived 479／long_stopped 4,026。旧R4は 9,569／0／102）。旧R4の「全到着」はSUMO既定300秒テレポートが渋滞を消していた結果。
- 滞留479台は三層分解（physical_isolation 68／intersection_blockage 24／queue_behind_blockage 387）で「**モデル規約下の滞留**」と裁定。A/B相対・Type別でのみ主張し、絶対数の外的妥当性は主張しない。
- **E3の主指標に「④Type3/4避難完了率（A/B相対）」を追加**（確定済みRQへの回帰）。完了時間は必ず完了率と対で報告する。逃げ遅れの絶対数は従指標。
- **Phase 2の「41市区町村で逃げ遅れ合計0」もteleport既定下の値**。Phase 2は再実行しないが、「渋滞由来の逃げ遅れは発生しない」という一般主張は全廃する。
- **A側E1完了**（Type3/4完了率 0.954448＝1,341/1,405）。ただし**「交通弱者ほど完了率が高い」は救出走行機構アーティファクト**（Type3/4＝救出走行の2レグ・遅発／Type1/2＝自家用車の出発集中）であり、demographic 解釈は禁止（決定46）。
- **Type3/4完了率の分母は固定定数 3,231.5人**（1,405台×2.3・A/B/S系不変）。Type正本人口3,255人は参照値として併記。分子はバス**到着**のみ（決定43・44）。
- **RQ含意を書き換え**：「バスが見捨てられた弱者を救う」ではなく「**既存の救出走行と、デマンドバス転用の、交通弱者に対する効率・公平性の比較**」（決定47）。
- **A側の未完了479台のうち84.3%（404台）は閉鎖されていないedge上＝渋滞尾部**であり、「浸水による取り残され」ではない。閉鎖起因は789秒の単一早期閉鎖ポケット（origin_0146が43台）。E3では完了時間分布で語る（決定53）。
- **type3/type4の完了率差（+0.0177）は構成効果（allocation shadow）が主因**（再加重分解で約84%）。経路長仮説は診断3で否定。出発時刻はkind間微差の候補機構に格下げ（決定51）。
- **段1.7完了**（`d11df9e`・`.gitattributes` に `*.py text eol=lf`）。`git_dirty_scripts=false` を実測し、段2のBLOCKERは解消。`scripts/cache/*.json` 2本は改行ゼロでEOL churn非適用のため無処置とし、段2'で `git rm --cached` する（決定56）。
- 次は**段2（B計測run）。コミット不要・直行可。** **正本run進行中は `scripts/` を編集しない。**
- 正本＝`開発メモ/方針判断_fable5/A_prime結果解釈_方針判断_fable5.md`（決定31〜36）・`段2計画_方針判断_fable5.md`（決定37〜42）・`E1指標定義_方針判断_fable5.md`（決定43〜49）・`churn是正と決定46改訂_方針判断_fable5.md`（決定50〜55）・`段2実行方針_方針判断_fable5.md`（決定56〜58）。
- **段2 B計測run完走**（run混合是正後の初の有効計測・run_id `measure_20260711T000129_79ef4dc8`・gates_ok=true）。ただし報告のバス到着141人は **terminated 2便（`closure_unreachable`）の車内16人を到着誤計上**しており、正味**到着125人**が正。**削減54台・B側9,515台**（決定59〜61）。16人は「乗車中孤立」＝バス方式固有リスクとして従指標・限界へ（決定60）。
- **AC6改訂**：`arrived` は避難所到着イベント由来に限る。terminated/despawn便の車内乗客は `not_arrived`。**F8新設**（車内乗客のarrived誤計上でhalt）。
- 正本に `開発メモ/方針判断_fable5/段2結果解釈_方針判断_fable5.md`（決定59〜66）を追加。
- **段2'完了**（4コミット `bb82252`/`c602dab`/`320afa7`/`23e0375`）。バス乗客集計を修正（terminated便車内をnot_arrivedへ・補正集計 arrived=125/not_arrived=16/residual=106・元summary非上書き）、**削減54台・B側9,515台**（AC3厳密一致）、pytest 64 passed。
- **段3 B確定run即時実行承認**（決定67）。改訂AC6（結合検査・番兵値検査）は実装確認済み。**判断6-4〔打切り便車内=送届完了〕は廃止**、決定60が唯一の規範。141人計上は回帰ではなく旧判断6-4の実装だった（決定68）。
- 正本に `開発メモ/方針判断_fable5/段3実行方針_方針判断_fable5.md`（決定67〜72）を追加。
- **段3 B確定run 最大2巡で停止**（`04_プログラム/テスト結果/phase3/段3_B確定run停止記録_20260711.md`）。第1巡 n'=75・AC7差22・層1=90、第2巡 n'=32削減→n'=207・AC7差58・層1=186（halt帯(34,136)超過で `halt_required=true`）。**n' は 125→75→207 と振動・削減写像 54→32→90 と発散し不動点に収束せず**。両巡とも AC2/AC5/改訂AC6 は PASS（データ異常ではない）。
- **Fable判断（決定73〜79・第3の道）**：一次確認でSUMOシード非設定（既定23423・決定論的）＋`scenario_b.rou.xml` 全車 `depart="0"`・`sigma=0.5` を確認し、振動は確率ノイズでなく入力差への決定論的バタフライ効果と裁定。**AC7・不動点closureを廃止**（決定73・第3巡/ダンピング反復/再配分変更を全面禁止・段3の2巡はrun感度データへ格下げ）／**削減54台を外生固定**（決定74・段2計測n'=125由来・循環なし・route 9,515台に固定）／**新設「段3R」でシード変動レプリケーション**（決定75・B側5run〔第1巡を#1に再利用＋4本〕・A'側3run・下限B3/A2）／**E1は帯付き報告**（決定76・n'レプリケート中央値＋min–max・二重計上上限 max(0,n'−124.2) を控除した保守下限併記）／**決定69の方向予測を撤回**（決定77・実測125→75で反証）／**S系はrun系感度取り下げ・会計算術のみ**（決定78）／**ヌル結果受容を事前コミット**（決定79・Δ符号が全レプリケート組合せで一貫→方向主張可、非一貫→「本モデルの分解能ではA/B差は検出されない」を結論として採用）。前提実装＝`p2_traci_bus.py` に `--sumo-seed` 追加（既定23423明示・単一コミット＋unitテスト）。
- 正本に `開発メモ/方針判断_fable5/段3停止後方針_方針判断_fable5.md`（決定73〜79）を追加。
- **段3R前提実装（`--sumo-seed`）を `p2_traci_bus.py` に追加**（HEAD `be1f50c`・既定23423明示・`build_sumo_command` が `--seed` を付与・manifestに `sumo_seed` 記録・66 passed）。`scenario_a.rou.xml` は sigma="0.5" ＝ **A側もシード感応**（前回留意点1解消・A側3run正当）。
- **Fable判断（決定80〜84・段3Rは「条件付きgo」）**：Opusの「前提実装完了」を2点で覆した——(1)**A側runnerは `p2_region_pipeline.py`** でシード制御なし（`--sumo-seed` はB側のみ・A側3runは現状実行不能）、(2)**確定route `scenario_b.rou.xml`（`3d6977…8297`・9,515台）がディスクから消失**、現物は破棄した第2巡route（`25420569…`・9,537台）に上書き済み。両点ともOpusが実ファイルで裏取り。**先行条件2件**＝(a)確定routeを `p3_bus_scenario.py` で再生成しSHA `3d6977…8297` 完全一致をハードゲート（第2巡routeは退避保全）／(b)`p2_region_pipeline.py` に `--sumo-seed` 追加（既定23423・manifest記録・unitテスト・アーカイブに入力route同梱）。**シード事前固定**＝B{23423,42,1,7,101}・A{23423,42,1}（決定81・事後追加/選別禁止・seedペア解釈禁止）。**#1再走**＝決定75の「第1巡再利用可」を撤回し、B/A両側とも seed 23423 を新コードで再走、旧runとの完全一致（B:9,000/515/0・99/75/24/148/247、A:9,090/479）を**再現gate**とする（決定82・下限でも#1再走は削らない）。決定76/77/78/79は不変（79にseedペア解釈禁止注記のみ追加）。総数8run・直列・予算7.5〜8h・下限B3/A2。
- 正本に `開発メモ/方針判断_fable5/段3R実行方針_方針判断_fable5.md`（決定80〜84）を追加。
- **先行条件2件完了**（コミット `678cad2`）：確定route復元（`scenario_b.rou.xml` SHA `3d6977…8297`・9,515台・第2巡routeは `archive_runs/20260711T114110_stage3_round2_input/` へ退避）／A側 `p2_region_pipeline.py` に `--sumo-seed` 追加（既定23423・manifest記録・入力routeをarchiveコピー・69 passed）。
- **Fable判断（決定85〜89・段3R本体は「go」）**：独立検証で (i) A側seed実配線は本物（前回型「宣言のみ未配線」の罠なし）、(ii) **route復元は決定論的再生成の実証**（確定routeは**CRLF**形式・gitのHEAD blobはLF＝`git restore`では生成不能・独立2回生成でバイト一致）、(iii) 差分は seed配線に限局し再現gate前提は健全、と認定。ただし**新ブロッカー＝先行条件C**を発見（Opus裏取り済み）：**B側 `archive_existing_outputs`（`p2_traci_bus.py:1008`）に入力route同梱がない**（routeを消失させた張本人のB側だけ未修正・A側:1626にはある）。**決定85** go＋先行条件C（`copy_paths={"route_file": route_file}` 1行追加・B#1前に単一コミット）／**決定86** 確定SHAはCRLFハッシュ＝**`git restore`/checkoutでの復元を禁止**（LF化でSHA不一致→全ゲート偽陽性halt・復元は `p3_bus_scenario.py` 再生成のみ）／**決定87** 決定81〜83不変／**決定88** 実行順序確定＋B#1直前に入力route SHA再計測（OneDrive/EOL事後改変検出）／**決定89** 段4予告更新。
- 正本に `開発メモ/方針判断_fable5/段3R_go判断_方針判断_fable5.md`（決定85〜89）を追加。
- **先行条件C完了**（コミット `419ceaf`・+1行）：B側 `run_bus_for_scenario` の archive呼び出し（`p2_traci_bus.py:1023`）へ `copy_paths={"route_file": route_file}` 追加（`copy2` コピー・本体ロジック無接触）。
- **Fable判断（決定90〜93・段3R本体は「execute」）**：独立検証で (i) `419ceaf` は正しい関数への+1行・simループ/会計に無接触、(ii) 即応点検すべて成立（HEAD `419ceaf`・scripts空・route SHA `3d6977…8297`・9,515台・CRLF・assignments 9,515行）、(iii) **テスト実測は78 passed**（unit 69＋regression 9・報告「69」はunitのみ）、と確認。**決定90** execute（B#1即時開始可）／**決定91** 決定85〜89不変（決定86のgit restore禁止＝最重要規範）／**決定92** B#1コマンド確定・**`--expected-vehicles 9515` 明示必須**（ゲート既定9,569はA整合値でB#1は偽陰性fail・既定は変更しない）／**決定93**（新設運用規則）B#2〜#5でhalt理由が**層1帯逸脱のみ**なら成果物保全のままFable差し戻し（決定88「層1帯は観測量」を運用で実現・ゲートコードは直さない）、層2≥層3やAC失敗を含めばハードhalt。
- 正本に `開発メモ/方針判断_fable5/段3R_execute判断_方針判断_fable5.md`（決定90〜93・B#1コマンド）を追加。
- **B#1（seed23423）再現gate完全一致**（run `final_20260711T150850_e1acce90`）：AC2 9,000/515/0・AC6 99/75/24/148/247・層分解90/26/399 が段3第1巡（旧コード）と一致。**決定89(ii)の3点（route再生成の決定論性・run再現性・SUMO既定seed=23423同値性）が仮定→事実へ昇格**。実行ログ正本＝`04_プログラム/テスト結果/phase3/段3R_レプリケーション実行ログ_20260711.md`。
- **B側5run完走（全PASS・halt無し）**：n'バンド={75,122,125,125,165}・**中央値125**（＝段2計測値・ただしroute条件差で「検証成立」とは読まない）・min–maxレンジ90（<100・留意点2非該当）。not_arrived全点∈(239.5,958)・層1全点∈(34,136)。B#4に `missing_fcd_count=2`（F級非該当・層分解引用時に注記義務）。
- **A#1（seed23423）再現gate PASS**（`full_20260711T191419_ef66e154`・arrived9,090/not_arrived479）。**A#2（seed42）がハング**（開始19:59:36→FCD凍結20:07:11・t=780）。
- **Fable判断（決定94〜98・A#2は外部要因）**：FableがWindowsイベントログで**20:06:58 Modern Standby移行**（FCD最終書込の13秒前）を特定＝外部要因ほぼ確定。Opusの「teleport=-1デッドロック」仮説は反証（B側にも救出車212警告・完走／teleport=-1グリッドロックはsim時計を止めない＝プロセス凍結≠交通デッドロック）。**決定94** 外部要因裁定・再実行を内部/外部判別試験に／**決定95** 同seed42を安全策付き再実行許可（スリープ無効化・watchdog・3h上限・選別非該当）／**決定96** 内部確定時＝(i)A側有限teleport導入は却下（A'床効果の立証基盤と両立不能）・(ii)ハングは「データ非生成」＝真因最小修正→A#1再現gateで無影響実証→続行、修正不能ならA#2をF級除外しA={23423,1}で下限A2成立（代替seed追加禁止）／**決定97** A#2再実行→A#3順序／**決定98** B側予備評価確定・実行ログ追記是正・**ヌル結論は現実的な帰結**（not_arrivedでB#1のみA#1より悪く他4点良い＝符号非一貫の前兆・A側投資はプロトコル最小限）。
- 正本に `開発メモ/方針判断_fable5/段3R_A2停止後方針_方針判断_fable5.md`（決定94〜98）を追加。
- **段3R全8run完走**（B側5run n'={75,122,125,125,165}中央値125＋A側3run not_arrived={479,1854,450}）。A#2 seed42は安全策付き再実行で完走・t=780凍結再現せず（決定94の外部要因裁定を事実へ昇格）。全run git_dirty_scripts=false・会計完全（各run arrived+not_arrived=9,569）。
- **A#2（not_arrived 1,854）は帯内保持・除外禁止**（決定81・99）。「モデルが初期条件seedに敏感である事実そのものが結果」（regime bimodality読み・段4で4点診断）。
- **従指標not_arrivedは符号非一貫（正2/負13）→ヌル確定**（決定101）。「本モデルの分解能ではA/B差は検出されない」・not_arrivedでのA/B方向主張は全面禁止。
- **主指標（Type3/4完了率）の判定は段4のE1充填後・15組合せ・raw/保守両方で全同符号のときのみ方向主張**（決定102で規則を事前固定・**符号表が出るまでE3本文を書かない**）。
- 正本に `開発メモ/方針判断_fable5/段3R_バンド組成判断_方針判断_fable5.md`（決定99〜104）を追加。
- **【研究主結論確定・2026-07-12】主指標Type3/4完了率のA/B差は「本モデルの分解能では検出されない」＝ヌル**（決定105・106）。15組合せ符号が非一貫（**符号表再計算後 `e2004d7`：raw 正10/負5・保守 正8/負7**・旧CSVの正13/負2はバグ・是正済み）。**raw点推定+0.23%pt・帯[−4.2,+22.7]%pt**（保守 +0.13%pt・[−4.2,+21.3]）がゼロをまたぐ。**方向主張は全面禁止**（多数決読み・点推定符号・B#1外れ値除外は決定79/81が禁じた事後選別）。※**決定126〜144で誤値と判明・実データ一本化で是正済み**（正=raw正13/負2・+1.23%pt・[−3.09,+22.97]／保守正13/負2・+1.21%pt・[−3.09,+22.63]〔保守max=B#4対A#2・決定139〕。旧正13/負2はバグではなく正しい値だった＝`e2004d7`の不良ハードコード回帰を撤回）。
- **regime bimodalityを第一級の発見に格上げ**（決定107）：A#2(seed42)完了率75%ロックregime（層3 86%・広域分散・バックログ0）・B#1低完了率。同一モデル・シードのみ差で大差＝単一run避難シミュの初期条件脆弱性の実証。決定36（A側外的妥当性非主張）の最強の裏付け。
- **符号表CSVバグ**（完了率実値と不整合・決定110）→E2前に完了率実値ベースで再計算（結論ヌルは不変）。定量A/B主張は④完了率ヌル一本（①完了時間=ECDF・②充足率=B絶対値・③Type別=診断のみ・決定108）。S系は縮小（バス10台×B5seed＝5run・決定109）。
- 正本に `開発メモ/方針判断_fable5/段4_主指標ヌル確定_方針判断_fable5.md`（決定105〜111）を追加。**※段4記録2件はSonnet委任がOneDrive同期で永続化失敗したためOpusが直接記録・実ファイル検証済み。**
- **S系（バス10台感度・決定109）完走**（B側5seed＝5run・確定route 9,515台・削減54固定）。バス到着n'={120,24,180,281,257}。**s10_b2（seed42）はnot_arrived 2,159で halt だが valid regimeとして帯内保持**（AC全PASS・層2<層3・決定112）。seed42はA側（A#2）とB側10台で独立にロックregime＝「seed×構成の相互作用」でregime bimodality強化（決定116）。
- **S系は「条件付き終了」**（決定113）：ヌル頑健の裁定は下すが、確定は10台E1完了率の15組符号表（raw＋保守控除・削減54固定・決定114）計算後（S-1・機械的・run無し）。判定則＝raw非一貫→即確定／全同符号→Fable差し戻し。バス3台は不要。
- **S系確定終了（`e2d6c55`・S-1完了）：** 10台E1符号表 raw 正10/負5・非一貫（`decision109_stop_s_series=True`）→ 決定113の事前登録判定則で確定・**「増車してもヌルは頑健」**・バス3台不要（決定115）。10台完了率 71.35〜102.01%（S10#4 102%は削減54固定・増車のみ設計の二重計上バイアス＝想定内・バイアス下でも非一貫）。
- **V系：** V2（`output/sumo/viz/phase3_viz.html`）・V3（`output/phase3.html`）完了。**V1 Excel未完**（Spreadsheetsローダー不応答・ツール障害）。ブラウザ視覚QA未実施（webview timeout・静的ゲートPASS）。
- 正本に `開発メモ/方針判断_fable5/S系帰結判断_方針判断_fable5.md`（決定112〜117）を追加。
- **【計画判断・2026-07-13】Fableが E1監査不整合を発見**（[[段4_計画判断_方針判断_fable5]] 決定118〜122）：`phase3r_e1_replicate_metrics.csv`（正13/負2・A#2=75.87/B#4=98.53）が band_summary/符号表/E2（正10/負5・A#2=75.02/B#4=96.45＝**引用値はこちらが正**・`e2004d7`がreplicate再生成し忘れ）と非整合（※**決定126〜130でこの帰属判断は逆と判明**：正しいのはreplicate_metrics側〔正13/負2〕・band_summary側はe2004d7が導入した不良ハードコード）。**主結論ヌルは頑健**（両カウント非一貫）だが監査に穴。**決定118＝replicate_metricsを決定43/44定義で再集計（run無し・最優先・パニック再run禁止）／決定119＝E3本文3段階・前提=118完了・X-1〜X-4事前登録／決定120＝視覚QAソフトゲート（今セッションplaywright実施）／決定121＝I-3のID訂正必須／決定122＝順序確定（118→121+120→E3→V系→P2-DOC-2即時並行可）。**

> **⚠️ 撤回済みの数値（引用禁止）：** バス乗車91・到着67・未到着24・残queue156／救出走行削減29台／シナリオB 9,540台／`selected_stop_demand_satisfaction_rate=0.271255`／`all_type34_demand_satisfaction_rate=0.020584`。`phase3_ab_comparison.csv` は破棄。**床効果もまだ確定していない。**
>
> **⚠️ 追加の撤回・格下げ（決定73・77・79）：** E1暫定 **3.87%pt・50.6%**（点値として撤回・段4で帯付き値に置換）／段3第2巡 **n'=207・層1=186**（確定値としては撤回・感度証拠としてのみ引用可）／**決定69の方向予測・確率見積り**（撤回）／**AC7・不動点closure**（廃止）。※段3第1巡 **n'=75** は撤回ではなく「段3Rのレプリケート#1（既定シード）として再利用」の格下げ扱い。段2計測 **n'=125** は「削減54の導出入力」としてのみ維持（B側E1分子には使わない）。
>
> 理由：**完走した「車＋バス」のシナリオB runは一度も存在しない。** `p2_traci_bus.py:779` が `scenario_a.rou.xml` をハードコード既定にしていたため B4 の `scenario_b.rou.xml`（9,540台）は一度もSUMOに読み込まれず、B側成果物は 14:48 の計測run（バスログ）と 15:24 の中断run（fcd/tripinfo）の混合物だった。あわせて `close_edges_with_bus()` が乗用車をリルートせず（A側は毎閉鎖でリルート＋出発前ブロック）、`p2_traci_bus.py:616` の過剰break、`time-to-teleport` のA/B不統一（A=既定300秒・B=-1）、B側 `traci_summary`／`vehicle_log` の欠如がある。シナリオAもテレポート非依存が未証明のため再実行対象。
>
> **バス状態機械は無実**（`step_bus` の `sim_time` は `traci.simulation.getTime()` 由来・独立時計なし）。バス状態機械のコードは修正しないこと。
>
> 正本＝`開発メモ/方針判断_fable5/シナリオB再実行_方針判断_fable5.md`（論点整理 opus → 判断 fable-5 → 記録 sonnet-5）。進捗の正本＝`05_タスク管理/phase3/Phase3_実装タスク詳細_常総先行.md`。

- Phase 1（自家用車避難・41市区町村）完了済み
- Phase 2（SUMO/TraCIシミュレーション）全実装完了：
  - 常総市 SUMO ネットワーク変換・TraCI 動的閉鎖・small/10pct/full 実行
  - FCD 出力・Leaflet.js 可視化（`sumo_viz.html`、シナリオ切替UI）
  - 全 41 市区町村の small/10pct 実行完了（逃げ遅れ合計 0）
  - full は代表 6 市区町村（守谷市・那珂市・行方市・大洗町・美浦村・五霞町）で実行
  - 市区町村別評価 CSV（41行）・Phase 1/2 比較 CSV（164行）・全域 HTML 生成済み
- Phase 2 比較基準固定（2026/05/19）：
  - `p2_evaluate_results.py` に避難完了時間列を追加
  - `p2_phase3_prep_agents.py` で出発地をType1〜4分類（agent_types.csv 生成）
  - `p2_build_phase2_excel.py` で Phase 2 Excelを8シート構成に生成
  - 先生コメント対応表・成果物固定リスト・試行設定比較表・図P2-5を作成
- 次フェーズ：Phase 3（バス比較・集計）

**2026-07-03：研究設計チェック（fableチェック）に基づきPhase 3方針判断3件を確定（規模乖離・車両会計・時間軸）。正本＝`05_タスク管理/phase3/Phase3実装前仕様_P3-IMPL-0.md`・`03_研究設計文書/phase3/Phase3_規模整合メモ.md`・`03_研究設計文書/共通設計/用語定義集.md`。詳細は`開発メモ/方針判断_fable5/fableチェック_修正タスク.md`。**

- **【V-4対応・2026-07-13】視覚QA V-4でS系10台の保守系列未計算が判明**（[[段4_計画判断_方針判断_fable5]] 決定123〜125）。Opus検証で保守も非一貫（正8/負7）・S10#4は保守97.15%≤100%を確認＝S系終了・主結論ヌル不変。**決定123＝S系保守を再集計しExcel/V2/V3へraw＋保守併記（run無し・rawの102%は保持し保守で置換禁止）／決定124＝決定118と単一E1整合パスに束ね1コミット（順序＝118でA正本確定→S保守→4表トリップワイヤ＋raw⇔保守パリティ検査）／決定125＝差分スコープ再QA。**
- **【決定124トリップワイヤ発火→決定110反転・2026-07-13】**（[[段4_計画判断_方針判断_fable5]] 決定126〜130）：E1整合パス実装中にband scriptの不良ハードコード `VERIFIED_COMPLETION_RATES`（決定110・`e2004d7`が"Fix"を名乗り導入した回帰・A#3=1357は実到着1349超過で物理的に不能）を検出。**正本は実データ算出（4ソース一致）＝raw 正13/負2・点推定+1.2348%pt・帯[−3.09,+22.97]／保守 正13/負2・+1.21%pt・[−3.09,+21.71]。決定110/118は誤り、決定105/106は正しい値で判断されており"復元"。主結論ヌルは不変**（負2組=B#1〔n'=75〕のみ・決定105が既に棄却）。**決定128＝実データ一本化＋全再生成＋文書差し戻し（+0.23→+1.23・正10負5→正13負2・帯[−4.23,+22.67]→[−3.09,+22.97]）＋トリップワイヤ4値更新・run無し・最上流ブロッカー／決定129＝ハードコード指標ミラー禁止（真実源=アーカイブ算出のみ）。** ※旧記録の +0.23%pt・正10/負5・帯[−4.23,+22.67] は誤値（決定128で全面是正）。
- **【着手前ゲート発火→決定131〜134・2026-07-13】**（[[段4_計画判断_方針判断_fable5]]）：決定128実装の着手前ゲートで手順書の前提SHA`419ceaf`（会話開始時HEAD）誤りが発覚→**現HEAD`e2d6c55`を正として前方是正・419ceaf巻き戻し禁止（決定131）**。Fableが**循環テスト**（ハードコードを自己照合し「64 passed」の緑が偽）を発見。**決定132＝是正範囲拡大（テスト脱循環化＝tripinfo/vehicle_log地上真実へassert・旧164行従指標表の無検証復活禁止・従指標所在をE3前に文書化）／決定133＝未追跡`p3_build_phase3_excel.mjs`を単一コミットに含める／決定134＝手順書HEAD訂正＋恒久規範拡張（陳腐化しうる値を手順・判断にハードコードせず実測・真実源はアーカイブ算出とgit実測のみ）。** 同一クラス事故3例目・根本＝ミラー信頼。主結論ヌルは不変。
- **【段C-2以降計画・2026-07-13】**（[[段4_計画判断_方針判断_fable5]] 決定135〜138）：決定135＝再生成後4値ゲートはディスク実物Readで再検証／**決定136＝コミットは明示allowlist・`git add .`禁止・`scenario_b.rou.xml`（決定86）と無関係EOL churn除外・route SHA不変を実測**／決定137＝従指標文書化はE3前ゲート／決定138＝E3前最終go/no-goゲート（ディスク4値整合・旧値残存ゼロgrep・route不変・従指標文書化・循環テスト0の全PASSでのみE3着手）。
- **【保守帯max訂正・2026-07-13】**（[[段4_計画判断_方針判断_fable5]] 決定139〜140）：段C-2再生成中にC-3ディスク実測ゲート（決定135）がFable手順書の保守帯max+21.71を取り違えと捕捉→ディスク実測**保守帯[−3.088349,+22.633452]%pt（max=B#4対A#2）**へ訂正。**raw帯[−3.088349,+22.970757]・raw点+1.234721%pt・保守点+1.2099644%pt・符号raw/保守とも正13/負2は不変・主結論ヌル不変**。決定140＝新規ガード規範（帯min/maxは15組全数から算出・raw argmaxを保守へ引き継がない・cap124.2がargmax組替え）。決定135のディスク実測ゲートが判断者自身の手計算誤りを着手前に捕捉。
- **【段C-4以降計画・2026-07-13】**（[[段4_計画判断_方針判断_fable5]] 決定141〜144）：決定141＝文書差し戻しは二層（現状文書=全訂正／判断記録=監査保持＋前方ポインタ・段4メモ⚠️注記のみ訂正）／**決定142＝コミットallowlistにgen_index.py・p3_build_visualization.py追加・gen_index再実行禁止（多ページ副作用）・output系churn分類パス明示add（実差分=phase3.html/phase3_viz.htmlのみ・Opus検証済）**／決定143＝従指標nativeソース＋補正値実測確認／決定144＝grep-zeroは現状文書限定・判断記録除外。E3前最終ゲートはraw/保守両帯照合・index/phase1/phase2誤stage無し等5条件。主結論ヌル不変。

---

## 次セッションの優先タスク

> 段3R全8run完走。方針は 決定73〜79 → 80〜84 → 85〜89 → 90〜93 → 94〜98 → [[段3R_バンド組成判断_方針判断_fable5]]（決定99〜104）。**AC7・不動点反復は廃止。第3巡・ダンピング反復・再配分変更は全面禁止。追加run・追加seedも禁止。**
>
> ⚠️ **確定route `scenario_b.rou.xml`（SHA `3d6977…8297`）はCRLF形式。`git restore`/`git checkout -- output/` で復元してはならない**（LF化しSHA不一致→全ゲート偽陽性halt）。復元は `p3_bus_scenario.py` 再生成のみ（決定86）。output系のM表示（EOL churn・内容同一を `--ignore-cr-at-eol` 差分ゼロで実証済）を「未保存変更」と誤認したrestoreが最大の事故シナリオ。

**【研究主結論＝ヌル確定（決定105〜111）】主指標Type3/4完了率のA/B差は「本モデルの分解能では検出されない」。regime bimodalityを第一級の発見に格上げ。符号表再計算・E2再生成は完了（`e2004d7`・raw 正10/負5・保守 正8/負7・点推定+0.23%pt・帯[−4.2,+22.7]）。残＝S系判定→E3本文→V系→卒論本文。正本＝[[段4_主指標ヌル確定_方針判断_fable5]]（決定105〜111）。方向主張は全面禁止。※**決定126〜144で誤値と判明・実データ一本化で是正済み**（正=+1.23%pt・正13/負2・[−3.09,+22.97]／保守正13/負2・+1.21%pt・[−3.09,+22.63]・決定139）。**E1整合是正（段C-1〜C-2）完了・残＝段C-4文書差し戻し継続＋従指標文書化＋allowlistコミット→E3本文。**

1. **決定128：実データ一本化＋訂正カスケード（最上流ブロッカー・run無し・1コミット）**——band scriptの `VERIFIED_COMPLETION_RATES` ハードコード削除→build_metrics一本化（`p3_s10_sensitivity_metrics.py` も確認）／band_summary・符号表・E2・S系符号表（raw＋保守新設）・Excel・V2/V3再生成／文書数値差し戻し（+0.23→+1.23等）／トリップワイヤ4値（主raw正13/負2・主保守正13/負2・S raw正10/負5・S保守正8/負7）再現を着手前ゲート化・不一致でhalt→Fable／自己整合assert新設（決定129）。※決定124の旧バッチ（決定118 E1整合パス）はこの決定128に吸収された。
2. **決定121：I-3のID訂正**（`段4_S系V系実装検証_20260712.md` の veh_full→`rescue_origin_0088_0004`〜`0016`・文書のみ）∥ **決定125：差分スコープ再QA**（S系保守追加箇所・S10#4のraw102%＋保守97%の方向非暗示・帯図中立性）——決定124と並行可。
3. **E3本文**（決定119・124完了後・決定106文言・regime bimodality 2事例・X-1〜X-4のFable固定往復・方向主張禁止）＝律速。
4. **V系最終**（E3数値確定後）。
5. **P2-DOC-2（卒論第3〜4章）**——run非依存・E3非依存で即時並行着手可。

> **E3の主指標**（決定32＋決定47）：① Type3/4の避難完了時間 ② 需要充足率（分母＝全Type3/4） ③ Type別公平性 ④ **Type3/4避難完了率**。**主張はA/B相対Δに限定**し、A側絶対水準の demographic 解釈は禁止。完了時間は必ず完了率と対で報告する。従指標＝逃げ遅れ数・選定停留所充足率・救出走行削減台数・despawn便数。**完了率の分母は固定定数3,231.5人**（決定43）。**B側n'＝中央値125（raw帯75–165）／保守124.2（帯75–124.2）を両併記**（決定100）。**主指標の判定基準は15組合せ・raw/保守両方で全同符号のときのみ方向主張**（非一貫なら「本モデルの分解能ではA/B差は検出されない」を結論として採用・決定79・102）。**従指標not_arrivedは符号非一貫（正2/負13）でヌル確定済み**（決定101・方向主張禁止）。

---

## データ取得状況（全取得済み）

| データ | パス（`04_プログラム/data/` 基準） |
| --- | --- |
| GSI KML 8時点 | `flood_kml/D1-No917_joso/` |
| A31a GML 国管理（鬼怒川含む） | `flood_hazard_a31/A31a-24_08_10_GML/` |
| A31a GML 都道府県管理 | `flood_hazard_a31/A31a-24_08_20_GML/` |
| A31a GML 埼玉県国管理（五霞町調査用） | `flood_hazard_a31/A31a-24_11_10_GML/` |
| A31a GML 千葉県国管理（五霞町調査用） | `flood_hazard_a31/A31a-24_12_10_GML/` |
| N03 茨城県行政区域（2015年版） | `admin_boundary/N03-150101_08_GML/` |
| 避難施設 P20（常総市洪水対応19件） | `shelters/避難施設データ_茨城/P20-12_08.dbf` |
| 人口250mメッシュ（T001178/T001208） | `population_mesh/5歳階級別人口250メッシュ_茨城/` |
| 浸水ナビ BP030（11メッシュ×8時点） | `suiboumap/hydrograph_origins_BP030.json` |

---

## タスク状態（未完了のみ）

| ID | 内容 | Phase |
|----|------|-------|
| P2-DOC-2 | Phase 2 本文ドラフト | 2 |
| P3-SEED | `--sumo-seed`：B側 `p2_traci_bus.py` ✅（`be1f50c`）／A側 `p2_region_pipeline.py` ✅（`678cad2`） | 3 |
| P3-ROUTE | 確定route `scenario_b.rou.xml`（`3d6977…8297`・9,515台）✅復元（決定論的再生成・CRLF・第2巡route退避済） | 3 |
| P3-ARCHC | 先行条件C：B側archiveへ入力route同梱 ✅（`419ceaf`・決定85） | 3 |
| P3-B5 | Phase 3 段3 B確定run → **停止（不動点非収束）**。段3Rへ移行 | 3 |
| P3-B5R | Phase 3 段3R レプリケーション（**execute可**・B5+A3run・#1再現gate・決定90〜93・コマンドは段3R_execute判断） | 3 |
| P3-E1/E2 | Phase 3 人単位・Type別評価と A/B比較CSV（✅取消・帯付きでやり直し・決定76） | 3 |
| P3-E3/S/V | Phase 3 主指標の数値確定・感度分析・成果物HTML/Excel化 | 3 |

完了済み（P2-DOC系）：比較解釈（P2-DOC-3）・先生コメント対応表（P2-DOC-5）・図P2-5・表P2-4・表P2-6・SUMO引用・バージョン記録（P2-DOC-4、`03_研究設計文書/phase2/Phase2_SUMO引用・再現性メモ.md`）・実装後検証チェックリスト（P2-DOC-6、`06_研究結果/phase2/Phase2_最終検証チェックリスト.md`）

完了済みタスク詳細 → `05_タスク管理/phase2/Phase2_実装タスク管理.md`

---

## エージェント分担

> 正本＝`05_タスク管理/モデル運用基準.md`（モデル選択と方針判断の3段階分離）

| 作業 | 担当 |
|------|------|
| Python スクリプト新規実装 | Claude Code（Opus） |
| コード修正・テスト・動作確認 | `/codex:rescue` |
| 研究方針の判断 | Fable 5（論点整理 Opus → 判断 Fable 5 → 記録 Sonnet 5） |
| 文書作成・調査・タスク管理 | Claude Code（Opus） |

---

## 重要ファイルパス

| ファイル | パス |
|---------|------|
| 共通定数 | `04_プログラム/scripts/config.py` |
| 実装手順書 | `05_タスク管理/phase1/実装手順書_Phase1.md` |
| 実装仕様書 | `03_研究設計文書/共通設計/実装仕様書.md` |
| 最新経過報告 | `01_経過報告/経過報告2026年05月/経過報告20260527.md` |
| テスト結果 Phase 1（最新） | `04_プログラム/テスト結果/phase1/テスト結果_phase1_final_20260512.md` |
| テスト結果 Phase 2 常総市 | `04_プログラム/テスト結果/phase2/テスト結果_phase2.md` |
| テスト結果 Phase 2 全域 | `04_プログラム/テスト結果/phase2/テスト結果_phase2_region.md` |
| Phase 1 本文ドラフト | `03_研究設計文書/phase1/Phase1_本文ドラフト.md` |
| Phase 1 経路検索説明 | `03_研究設計文書/phase1/論文用説明_Phase1_経路検索方法.md` |
| Phase 1 成果物固定リスト | `06_研究結果/phase1/Phase1_成果物固定リスト.md` |
| Phase 1 研究結果 | `06_研究結果/phase1/Phase1_研究結果.md` |
