# Phase 3 F系 実行系是正テスト結果（2026-07-09）

**対象：** `p2_traci_common.py`、`p2_region_pipeline.py`、`p2_traci_bus.py`、`p3_bus_scenario.py`、`p3_evaluate_equity.py`  
**目的：** シナリオB成果物のrun混合問題を受け、A/B共通の閉鎖・リルート・vehicle_log会計、route明示、teleport設定、run manifestを実装する。  
**総合判定：** コード実装と軽量検証はPASS。R4' full再実行は未完走。

---

## 実装内容

| ID | 内容 | 状態 |
|---|---|---:|
| F1 | `p2_traci_common.py` 追加。閉鎖適用、乗用車リルート、出発前ブロック、vehicle_log、traci_summaryを共通化 | ✅ |
| F2 | B側TraCIループの過剰break削除 | ✅ |
| F3 | A/B両sumocfg生成で `time-to-teleport=-1` を明示 | ✅ |
| F4 | `run-bus --route-file --phase measure|final` 必須化、route台数アサーション、run manifest追加 | ✅ |

---

## 検証

```powershell
python -m py_compile `
  04_プログラム/scripts/p2_traci_common.py `
  04_プログラム/scripts/p2_region_pipeline.py `
  04_プログラム/scripts/p2_traci_bus.py `
  04_プログラム/scripts/p3_bus_scenario.py `
  04_プログラム/scripts/p3_evaluate_equity.py
```

結果：PASS。

```powershell
& 04_プログラム/venv/Scripts/python.exe -m pytest 04_プログラム/scripts/test_p3_bus_accounting.py
```

結果：28 passed。

```powershell
& 04_プログラム/venv/Scripts/python.exe 04_プログラム/scripts/p2_traci_bus.py run-bus --help
```

結果：`--route-file` と `--phase {measure,final}` が必須引数として表示されることを確認。

route台数アサーション：

| phase | route | assignments | 結果 |
|---|---|---|---:|
| measure | `scenario_a.rou.xml` | `scenario_a_vehicle_assignments.csv` | PASS |
| final | `scenario_b.rou.xml` | `scenario_b_vehicle_assignments.csv` | PASS |

---

## R4'試行

```powershell
& 04_プログラム/venv/Scripts/python.exe 04_プログラム/scripts/p2_region_pipeline.py scenario-city --city-code 08211 --scenario full
& 04_プログラム/venv/Scripts/python.exe 04_プログラム/scripts/p2_region_pipeline.py run-city --city-code 08211 --scenario full
```

`scenario_a.sumocfg` に `time-to-teleport value="-1"` が出力されることは確認済み。  
ただしfull実行は約26分で `scenario_a_fcd.xml` の最終timestepが1260秒までしか進まず、21600秒までの完走には数時間規模を要する見込みだったため、今回開始したSUMO/Pythonプロセスを停止した。

**AC1は未達。** 次はFCD出力抑制・ログ間隔・実行方式を見直してR4'を完走させる。

停止時に途中生成された標準名の出力は誤用防止のため退避した。

| 元ファイル | 退避先 |
|---|---|
| `scenario_a_fcd.xml` | `scenario_a_fcd.partial_20260709_teleport_minus1.xml` |
| `scenario_a_tripinfo.xml` | `scenario_a_tripinfo.partial_20260709_teleport_minus1.xml` |

---

## 注意

旧B系数値（バス到着67人、救出走行削減29台、B側9,540台、充足率0.271/0.0206）は撤回済みであり、本テスト結果では再固定していない。

---

> **⚠️ 2026-07-09 方針注記（[[実行計画改訂_方針判断_fable5]] 決定9）**
> 本メモに記載された「FCD出力抑制・ログ間隔の見直しで完走させる」方針は**不採用**となった。FCD出力（60秒刻み）は律速ではなく、削ると決定5の検証(iv)（バス所要のfcd突合）・AC5（fcd閉じタグ・時刻整合）・留意点2（滞留位置の切り分け）・V2（アニメHTML）の手段を失う。
> R4'が遅い主因は **A系ループの車両単位TraCIポーリング**（`p2_traci_common.py:216,233` の `update_stop_states` が毎ステップ全走行車両へ `traci.vehicle.getSpeed()` を発行）と判定された。対処は夜間バックグラウンド実行（12時間/run予算）であり、**12時間でsim進捗50%未満の場合にのみ** TraCI subscription化／libsumo化へ転換する（10pct回帰ゲート付き）。**発動条件を満たすまで当該改修に着手してはならない。**
> `--step-length` 粗化・teleport=-1の撤回・感度runの10pct化も、いずれも不採用。
