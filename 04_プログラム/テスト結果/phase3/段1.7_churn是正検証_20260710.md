# Phase 3 段1.7：改行churn是正（.gitattributes）の実装検証（2026-07-10）

**対象**：コミット `d11df9e Normalize Python EOL handling`
**検証者**：Opus（実ファイル確認・独立実行）／記録：Sonnet 5
**正本**：`開発メモ/方針判断_fable5/03_段1_A基準・指標/churn是正と決定46改訂_方針判断_fable5.md`（決定50〜55）
**総合判定**：**PASS。BLOCKERは解消され、段2（B計測run）を開始できる状態になった。** ただし決定50の留意点1が予告した「`*.py` 限定」の穴が、**スコープ内の追跡ファイル2本として具体的に存在する**（§4）。処置は Fable 5 の判断待ち。

## 1. コミット内容

`d11df9e`：6ファイル・505挿入／504削除
- `.gitattributes`（新規1行）
- `04_プログラム/scripts/` 配下のPythonファイル5本：`c3_get_road_network.py`（136行）・`config.py`（294行）・`e1_load_flood_data.py`（492行）・`i1_spatial_join.py`（30行）・`i2_generate_closure.py`（56行）

`.gitattributes` の内容：
```
*.py text eol=lf
```

## 2. 決定50の要件充足の確認

| 要件（決定50） | 実測 | 判定 |
|---|---|---|
| `.gitattributes` に `*.py text eol=lf` | 上記のとおり1行のみ | ✅ |
| `scripts/`・`tests/` 限定の renormalize | コミット対象は `scripts/` 配下のPython 5本のみ（index上でCRLFだったファイル）。`tests/` 配下は再正規化不要だった | ✅ |
| EOL処置のみの外科的コミット（機能変更を混ぜない） | `git show --ignore-cr-at-eol --stat d11df9e -- 04_プログラム/scripts/` の出力にファイル差分が現れない＝**実変化ゼロ** | ✅ |
| コミット後 `git status --porcelain -- 04_プログラム/scripts/` が空 | 出力 `''`（0件）。`04_プログラム/tests/` も0件 | ✅ |

**`git_dirty_scripts` の実挙動**：Opusが `git status --porcelain -- 04_プログラム/scripts/` を直接実行し、出力が空文字列であること、したがって `git_dirty_scripts = False` となることを確認した。

**段1.6時点でdirtyだった3ファイル**（`p3_e1_type_metrics.py`・`p3_stagnation_decomposition.py`・`p3_validate_b_measure_gate.py`）は本コミットの対象に含まれていない。これらは index 上が既に LF であり、`eol=lf` 属性の導入によって clean filter 経由の比較が working tree の CRLF を透過するようになったため、**再正規化なしで status が clean になった**。決定50が機構的裏づけとして挙げた「`eol=lf` 属性下では working tree が CRLF に書き換えられても status は clean のまま」がそのとおり機能している。

## 3. テスト

Opus環境での実行：
```
python3 -m pytest tests/unit -q --ignore=tests/unit/test_meshcode.py --ignore=tests/unit/test_vehicle_accounting.py
→ 26 passed
```
（`test_meshcode.py`・`test_vehicle_accounting.py` は geopandas/folium 未導入のため除外。決定29の記録義務に従い実行コマンド・件数・除外理由を明記する。）
renormalize 後もテストは通り、EOL処置がコードを壊していないことを確認した。`tests/unit/test_git_state.py`（決定26で義務付けた `git_state()` の単体テスト）も存在する。

## 4. 残存リスク：`git_dirty_scripts` スコープ内の非Pythonファイル2本

決定50は「属性は `*.py` のみ、renormalize は `scripts/`・`tests/` のみ」とスコープを最小化し、留意点1で「`.gitattributes` の効果は `*.py` に限定。output/ 配下のchurnは残る」と述べていた。

Opusが `git ls-files -z 04_プログラム/scripts/` を走査した結果、**`git_dirty_scripts` の判定スコープ（`04_プログラム/scripts/`）内に、`*.py` 属性でカバーされない追跡ファイルが2本存在する**：

- `04_プログラム/scripts/cache/637d8152db8565da27fdbaaac3cbb70ae2178755.json`
- `04_プログラム/scripts/cache/97a176398c3e3d4d6b59bde11350077f55416d99.json`

現時点では両者ともクリーン（`git status --porcelain -- 04_プログラム/scripts/cache/` は0件）。しかし**これらが改行churnを起こすと `git_dirty_scripts` が `true` になり、決定50が根本対処したはずのBLOCKERが同じ形で再発する**。

なお `04_プログラム/tests/` 配下には非Pythonの追跡ファイル（`README.md`・`pytest.ini`・`テスト設計書.md`・`fixtures/README.md`）が存在するが、**決定30により `git_dirty_scripts` のスコープに `tests/` は含まれない**ため、合否には影響しない。

また `04_プログラム/scripts/bash.exe.stackdump` が working tree に存在するが、`.gitignore:57`（`**/bash.exe.stackdump`）で除外されており追跡対象外である。

**処置（`.gitattributes` に `*.json` 等を追加する／`scripts/cache/` を追跡対象から外す／現状維持で監視する 等）は Fable 5 の判断事項であり、本記録では判断しない。** 決定50は「導入後に `scripts/` でchurnが再発した場合、その場しのぎの空白無視をせず**新規BLOCKERとして停止・判断差し戻し**とする」と定めている。

## 5. 次段の状態

決定50のBLOCKERは解消。決定54の段構成に従い、**次は段2（B計測run）**。段2のゲートは AC2（車両保存9,569）・AC5（`git_dirty_scripts=false`・出所単一）・AC6（バス人数保存）＋改訂ゲート（総数の半分/2倍・層1の半分/2倍・層2≥層3）で不変。実行条件はOneDrive同期停止・直列・予算3h（1run実測47分）。
