# Phase 3 段2：B計測run（シナリオB・phase=measure）検証（2026-07-11）

**対象**：`run_id: measure_20260711T000129_79ef4dc8`・コード版 `d11df9e`・route sha256 `29464ee178a8faadc5e94d116a782598d4fa4998bce794e7cd2f0fb7c8fd6e35`（R4正本 `scenario_a.rou.xml`）
**検証者**：Opus（実ファイル確認）／記録：Sonnet 5
**正本**：`開発メモ/方針判断_fable5/04_段2_B計測/段2実行方針_方針判断_fable5.md`（決定56〜58）、`開発メモ/方針判断_fable5/03_段1_A基準・指標/A_prime結果解釈_方針判断_fable5.md`（決定31〜36）
**総合判定**：**PASS（gates_ok=true・halt不要）。決定57の失敗モードF1〜F7に該当なし。** これは run混合問題（シナリオB再実行判断）を是正して以降、**完走した「車＋バス」のシナリオBの初の有効計測**である。段2'（削減台数算出＝floor(141/2.3)=61台）へ進める。

## 1. run前チェック（決定57の4点）

実装者報告として記載。

| 項目 | 内容 |
|---|---|
| route-file sha256 | `29464ee178a8faadc5e94d116a782598d4fa4998bce794e7cd2f0fb7c8fd6e35`（R4正本と一致） |
| HEAD | `d11df9e46db93e6bae1b9713235bb56923f57ac3` |
| `git status --porcelain -- 04_プログラム/scripts/` | 空 |
| OneDrive | 停止済み、run後もプロセスなし |
| 実行形 | `p2_traci_bus.py run-bus --city-code 08211 --route-file <scenario_a.rou.xml> --phase measure` |
| 実行時間 | 2026-07-11T00:01:29 → 01:04:56（**wall-clock 約63分**） |

## 2. マニフェスト（AC5・出所単一）

Opusが `scenario_b_bus_summary.json` の `run_manifest` を確認。

- `run_id`：`measure_20260711T000129_79ef4dc8`／`phase`：`measure`
- `git_commit`：`d11df9e46db9…`（40hex・非空）／`git_dirty_scripts`：`False`／`git_scope_path` 一致

## 3. ゲート結果（`scenario_b_measure_gate.json`）

Opusが実ファイルで確認。

- `gates_ok`：true／`halt_required`：false／`halt_reasons`：空
- **AC2**：ok=true。`vehicle_log_rows` 9,569／`expected_vehicles` 9,569／`arrived` 9,085／`not_arrived_unblocked` 484／`departure_blocked` 0／`accounting_total` 9,569
- **AC5**：ok=true。`time_order_ok`・`git_commit_40hex`・`git_dirty_scripts_false`・`git_scope_path_match` すべて true。`output_checks` の passenger_log／bus_log／vehicle_log／closure_log／congestion_log すべて `missing:false`・`sha256_match:true`
- **AC6**：ok=true。`boarded` 141／`arrived` 141／`not_arrived` 0／`candidates` 247／`residual_queue` 106
- **改訂ゲート**：`a_not_arrived_baseline` 479／`b_not_arrived` 484／受容域 (239.5, 958) 内。`a_layer1_baseline` 68／B層1 71／受容域 (34, 136) 内。層2(36)<層3(377) で「層2≥層3」に該当せず（合格）

## 4. バス輸送実測（`scenario_b_bus_summary.json`）

- `bus_count` 5／`initial_bus_candidate_total` 247
- `bus_boarded_passengers` 141／`bus_arrived_passengers` 141／`bus_not_arrived_passengers` 0
- `total_trips` 26／`deadhead_trips` 0／`terminated_trips` 2／`termination_by_reason` `{"closure_unreachable": 2}`
- `two_layer_report`：`bus_transport_total` 141／`residual_queue_total` 106
- `conservation_ok`：true

**撤回済みだった前回計測（run混合）との対比（事実の記載のみ・解釈は加えない）**：前回は boarded 91／arrived 67／候補247／残queue 156／despawn 3便。今回は boarded 141／arrived 141／候補247／残queue 106／`closure_unreachable` 2便。**バス到着が 67→141 人に増えた。**

## 5. B側滞留分解（決定31・38の三層分解）

- `physical_isolation`（層1）71／`intersection_blockage`（層2）36／`queue_behind_blockage`（層3）377／合計 484
- A'（68／24／387）との対比：層1 68→71、層2 24→36、層3 387→377。**層3が支配的という構成はA'と同型**（改訂ゲートの「層2≥層3」に非該当）。

## 6. 決定57の目視確認7点の照合

| # | 項目 | 結果 |
|---|---|---|
| 1 | `type34_bus_arrived_people` 非ゼロ | **段2ではB側E1未算出**（E1本体のB側充填は段4）。バス到着141人は `bus_summary` で確認済み |
| 2 | `conservation_ok=true`・candidates/boarded/arrived/not_arrived/残queue | 247/141/141/0/106 |
| 3 | despawn（terminated）便数 | 2便・理由 `closure_unreachable`（前回3便のdespawnとは別カテゴリ。到着人数へは計上しない扱いは不変） |
| 4 | 層1/2/3 の構成比・改訂ゲート判定根拠 | 71/36/377、判定根拠は本記録 §3 のとおり |
| 5 | manifest：HEAD sha一致・sha256再計算一致・`missing:true` なし・fcd閉じタグ | HEAD sha `d11df9e`・sha256全一致・`missing:true` なし。（fcd閉じタグは段2の主成果物にfcd出力が含まれるか実装者報告で要確認だが、gate の `output_checks` は5ログすべて sha256_match） |
| 6 | run終了後 `git status --porcelain -- scripts/`（cache 2本含む） | 空（実装者報告） |
| 7 | OneDrive同期の再開 | run後もプロセスなし |

## 7. 失敗モードF1〜F7の照合（決定57）

| # | 事象 | 判定 |
|---|---|---|
| F1 | ゲート不合格 | **非該当**（gates_ok=true） |
| F2 | `bus_arrived` 0または極小 | **非該当**（141人） |
| F3 | boarded がA'参照値の半分未満 | **非該当**（AC6合格） |
| F4 | despawn便数7便以上 | **非該当**（terminated 2便・かつ理由は despawn ではなく `closure_unreachable`） |
| F5 | 層構成の大きな乖離・層2≥層3 | **非該当**（層2 36 < 層3 377） |
| F6 | run 94分超かつログ進行なし | **非該当**（63分で完走） |
| F7 | `scripts/` にdirty出現 | **非該当**（クリーン） |

## 8. 段2'への引き継ぎ

- 削減台数＝`floor(bus_arrived / k)` ＝ `floor(141 / 2.3)` ＝ **61台**（決定44の分子はバス到着人数 `bus_arrived`＝141）。
- 段2'で `scenario_b.rou.xml` を再生成（救出走行を61台削減）。同編集窓で診断4の再加重分解スクリプト化＋`scripts/cache/*.json` 2本の `git rm --cached`（決定52・56）。
- **未決の解釈事項は Fable 5 の判断へ**（本記録では判断しない）：`terminated_by_reason` が前回の `despawn` から `closure_unreachable` に変わったことの意味、バス到着が 67→141 に増えた要因、B側 not_arrived 484 と A' 479 の差の解釈。

> **⚠️ 2026-07-11 訂正（[[段2結果解釈_方針判断_fable5]] 決定59・61）**：本節の「バス到着141人・削減61台」は**採用しない**。`scenario_b_bus_log.csv` の terminated 2便（`bus_std_2` trip0・`bus_std_5` trip1・`closure_unreachable`）の車内16人が、避難所未到着（`arrive_shelter_time_s=21600`＝シミュレーション終端の番兵値）のまま `passenger_log` で `arrived=True` 計上されていた（`arrival_time_s=21600` がちょうど16行）。避難所到着イベントで確定した正味到着は **125人**（141−16）。**削減台数＝floor(125/2.3)＝54台、B側車両数＝9,515台**（決定61）。16人は「乗車中孤立」として `not_arrived` に計上する（決定60）。この計上誤りは集計層の回帰であり、段2'のコミット2（集計修正＋fixtureテスト）で是正する。**run自体は有効で再実行不要**（生ログに真値が残っている・決定59）。
