# Phase 3 I-1〜I-3（Excel・S系CSV単位系・missing_fcd分類）完了検証（2026-07-12）

- **対象**：コミット後の実装者作業（I-1 Excel／I-2 S系CSV／I-3 missing_fcd分類）
- **検証者**：Opus（実ファイル確認）／記録：Sonnet 5
- **正本**：`開発メモ/方針判断_fable5/07_段4_結論・感度/S系帰結判断_方針判断_fable5.md`（決定112〜117）

## 1. I-1 Excel（P3-IMPL-8）完了

- `outputs/p3-impl-8/phase3_results_excel.xlsx`（12.6KB・Opus実在確認）。7シート構成：8run完了率・15組符号表・raw/保守帯・S系10台を集約。全シート目視QA済み・エラーセル0件（実装者報告）。
- **留意：保存先が `4年次本研究/outputs/p3-impl-8/`（新規トップフォルダ）であり、通常の `04_プログラム/output/` と異なる。** 成果物専用ディレクトリの意図と見られるが、パスの位置づけは記録に残す。

## 2. I-2 S系CSV単位系 — 修正不要

- `phase3_s10_15_combination_signs.csv` も `delta_rate`／`delta_percentage_points` 分離済み。15行すべて `rate × 100 = %pt` で一致したため修正不要（実装者確認）。

## 3. I-3 s10_b2 missing_fcd 13の分類 — 「走行中滞留」と確定（ただしID表記に食い違い）

- 実装者の結論：13台は**未挿入（never-inserted）ではない**。全台が6,316〜6,352秒に出発しFCD各255点を持つ一方、tripinfoがなく `long_stopped=True`＝**走行中に滞留したままシミュレーション終了した車両**と確定。
- **⚠️ ID表記の食い違い（Opus検出・要注記）：** 実装者の検証記録（`段4_S系V系実装検証_20260712.md` の S-2 行）は対象を **`veh_full_origin_0088_0004`〜`0016`（自家用車＝Type1/2）** と記載しているが、**ゲートJSON `s10_b2_gate_seed42.json` の `missing_fcd_vehicle_ids`（機械生成・正）は `rescue_origin_0088_0004`〜`0016`（救出走行＝Type3/4）** である（Opus実データ確認・prefix内訳 rescue 13/13）。機械生成のゲートJSONが正本であり、実装者記録の `veh_full` は転記ミスの可能性が高い。**「走行中滞留」という結論自体は変わらない**（決定112のS-2＝非ブロッキング・ヌル結論に無影響。missing_fcdはvehicle_log由来のnot_arrived集計には影響せず、層分解の分類2,146/2,159にのみ影響）。ただし対象が救出走行（Type3/4）か自家用車（Type1/2）かは事実として異なるため、引用時はゲートJSONの `rescue_origin_0088` を正とすること。

## 4. 総括

S系・V系の実装はExcelを除き完了（V1 Excel=I-1で完了）。**研究の主結論（ヌル）に影響する事項はなし。** 残＝E3本文・ブラウザ視覚QA・P2-DOC-2。

新規SUMO run・追加seedは未実施（決定109/113の禁止どおり）。
