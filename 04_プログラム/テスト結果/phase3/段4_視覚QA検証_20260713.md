# 段4：視覚QA検証（2026-07-13）

## メタ
- 作成日：2026-07-13
- 実施：ユーザー（playwright）＋Opus独立検証
- 記録：Sonnet 5
- 根拠：決定120（視覚QAソフトゲート・今セッションでplaywright実施）承認の視覚QA

---

## V-1（描画・動作）：PASS

- 不確実性帯はraw・保守ともゼロを横断。
- 二峰性図は75.02%と95〜97%帯を正常描画。
- Canvasは初期描画後、再生操作で時刻0→660秒へ進み車両位置も更新。
- V3の全成果物リンクはHTTP 200。
- ブラウザのconsole error/warningは0件。

## V-2（方向主張の非暗示・決定120の主眼）：PASS

- 「本モデルの分解能では方向差を検出できない」と明記。
- 「バスが改善」「Bが優位」等の方向主張なし。
- 帯図は中立的な青・緑、ゼロ基準は赤破線、方向を示す矢印なし。
- Excelも方向主張禁止・符号非一貫を明記。

## V-3（撤回値の非残存）：PASS

- 成果物HTML・Excel・卒論ドラフト候補に撤回値なし。
- `06_研究結果/phase3/Phase3_研究結果.md`（line 24）には監査目的の「撤回済み・引用禁止」表として旧値が保存されているが、現行結果としての残存ではない。

## V-4（S系10台の保守系列併記）：FAIL・判断必須

- ExcelのS10#4にraw完了率102.01%が表示。二重計上バイアスの注記はあるが、**S系10台の保守系列が併記されていない**。
- 決定113/114はS系についてもraw＋保守控除表・中央値＋min–max・保守下限の併記を要求している。
- 現行HTML（V2/V3）もS系10台はraw符号のみで保守系列を表示していない。
- **Opus独立検証（実データ確認）**：S系10台の保守系列は**表示漏れではなく未計算**。`phase3_s10_replicate_metrics.csv`（`completion_rate` raw のみ・保守列なし）・`phase3_s10_band_summary.json`（`sign_counts` raw 正10/負5 のみ・`conservative_sign_counts` なし）・`phase3_s10_15_combination_signs.csv`（`delta_rate`/`sign` raw のみ・保守列なし）の3ファイルすべてがraw専用。
- **S系終了の判定（決定115）はraw非一貫で有効**（決定113の判定則は「raw非一貫→即確定」でraw基準のため不変）。ただし**報告の完全性（決定114が要求したraw＋保守控除）に欠落**。主系列（5台）はband_summaryにraw/保守両方あるが、S系10台はrawのみ。決定118（replicate_metrics残置）と同じ「監査整合性の穴・結論は不変」クラス。
- **この判断（S系10台の保守系列を再集計しExcel/V2/V3へ追加してよいか・新規SUMO run不要）はFableへ付託中**。

---

## 総括

V-1〜V-3はPASS、V-4のみ保守系列欠落でFAIL。**主結論（ヌル）・S系終了判定は不変**、報告完全性の欠落（決定114）。

---

## 関連リンク

- [[段4_計画判断_方針判断_fable5]]
- [[S系帰結判断_方針判断_fable5]]
