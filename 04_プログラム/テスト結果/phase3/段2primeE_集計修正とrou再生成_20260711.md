# Phase 3 段2'：バス乗客集計修正・削減54台でのrou再生成 検証（2026-07-11）

**対象**：4コミット `bb82252`（cache追跡解除）／`c602dab`（集計修正）／`320afa7`（診断4再加重分解）／`23e0375`（rou再生成）
**検証者**：Opus（実ファイル確認・独立集計）／記録：Sonnet 5
**正本**：`開発メモ/方針判断_fable5/04_段2_B計測/段2結果解釈_方針判断_fable5.md`（決定59〜66）
**総合判定**：**PASS。決定62の4コミット構成（cache除去→集計修正＋fixture→診断4→rou再生成〔最後〕）どおりに実装され、AC3（9,515台）・補正集計（125/16/106）・出所単一（sha256継承）を実ファイルで確認した。** 段3（B確定run）へ進める。

## 節構成

### 1. 4コミットの構成（決定62との対応）

表形式で（コミット／内容／決定62の該当項）：
- `bb82252 Stop tracking script cache JSON files`：`scripts/cache/637d8152….json`・`97a176398….json` の2本を追跡解除（2 deletions）→ **決定62コミット1（chore・決定56）**
- `c602dab Fix terminated bus passenger accounting`：`p2_traci_bus.py`（+6）・`p3_bus_accounting.py`（+8）・`p3_validate_b_measure_gate.py`（+48）・新規 `p3_correct_bus_passenger_accounting.py`（136行）・成果物 `scenario_b_bus_corrected_accounting.json` → **決定62コミット2（fix＋test）**
- `320afa7 Add allocation shadow reweighting diagnostic`：新規 `p3_allocation_shadow_reweighting.py`（197行）＋`tests/unit/test_allocation_shadow_reweighting.py`（39行）＋成果物2件 → **決定62コミット3（診断4・決定52）**
- `23e0375 Regenerate scenario B route with corrected reduction`：`p3_bus_scenario.py`（+74）・`scenario_b.rou.xml`（再生成）・`scenario_b_vehicle_assignments.csv`（9,516行）・`scenario_b_rescue_reduction.csv` → **決定62コミット4（feat/data・最後）**

→ **rou再生成が最後のコミットである**ことを確認（決定62・決定65のgit gate要件：段3実行時のHEADに修正済み集計コードが含まれる）。

### 2. 集計修正（決定59・60・63）の確認

`scenario_b_bus_corrected_accounting.json`（Opusが実ファイルで確認）：
- `source_run_id`：`measure_20260711T000129_79ef4dc8`
- `source_boarded_passengers`：141
- **`corrected_bus_arrived_passengers`：125**／**`corrected_bus_not_arrived_passengers`：16**／`residual_queue_total`：106／`initial_bus_candidate_total`：247
- `terminated_trip_keys`：`[["bus_std_2","0"], ["bus_std_5","1"]]`（決定59の2便と一致）
- `conservation_ok`：true（247 ＝ 125 ＋ 16 ＋ 106）
- **出所単一（決定62コミット2の要件）**：`passenger_log_sha256`・`bus_log_sha256`・`source_summary_sha256` を継承記録。元 `scenario_b_bus_summary.json`（141人ベース）は**上書きされていない**（別成果物として補正集計を残す方針どおり）。

### 3. 削減台数とrou再生成（AC3・決定61）の確認

Opusが実ファイルで確認：
- `scenario_b.rou.xml`：`<trip>` 要素 8,164（自家用車）＋`<vehicle>` 要素 1,351（救出走行）＝**9,515台**
- `scenario_b_vehicle_assignments.csv`：9,515行（`vehicle_kind` 内訳 private_car 8,164／rescue_car 1,351）
- **AC3期待値 8,164 ＋（1,405 − 54）＝ 9,515 と厳密一致**
- `scenario_b_rescue_reduction.csv`（4停留所）：`rescue_removed_count` 合計 **54**、`bus_arrived_passengers` 合計 **125**（正味到着と一致）。整数化は停留所別largest-remainder方式（origin_0057: arrived59・raw25.652→removed26／origin_0085: 8・3.478→3／origin_0089: 42・18.261→18／origin_0176: 16・6.957→7）。

### 4. 診断4の再加重分解（決定52）

`p3_allocation_shadow_reweighting.py`＋`test_allocation_shadow_reweighting.py`＋成果物 `scenario_a_diag_allocation_reweighting.json`・`scenario_a_diag_allocation_reweighting_by_origin.csv`（353行）を生成。決定52の「pearson 0.049 を参考降格し、再加重分解を正本統計量にする」に対応するスクリプト化。**数値の再検証は本記録では行わない**（段1.6検証記録 §3 で確認済みの 0.949807→0.964597・約84%を正とする）。

### 5. テストと衛生状態

- `pytest 04_プログラム/tests/unit`：**64 passed**（実装者報告。段1.6時点の60→段2'で新規fixture〔集計修正・再加重分解〕追加で+4）。
- `git status --porcelain -- 04_プログラム/scripts/`：空（実装者報告・段3のAC5前提）。
- 決定29の記録義務：pytest実行コマンド・件数を明記。

### 6. 決定62の完了ゲート照合

表形式で（ゲート項目／結果）：
- py_compile：PASS（実装者報告）
- pytest全通過（新fixture含む）：64 passed
- AC3カウント一致：9,515（§3）
- `git status --porcelain -- scripts/` 空：確認

### 7. 段3への引き継ぎ

- 段3（B確定run）：`scenario_b.rou.xml`（9,515台＋バス5台）・`--phase final`・HEAD `23e0375`（修正済み集計コードを含む）。
- ゲート：AC4（車両保存9,515）・AC5（出所単一）・改訂AC6（決定63：arrivedは避難所到着イベント由来／terminated便車内はnot_arrived／終端到着レコードでfail／bus_log×passenger_log結合検査）・**AC7合格帯 n'∈[120,131]**（決定65）。最大2巡・第2巡超過でhalt→Fable再判断。失敗モードF1〜F8。
- **未決の解釈事項は Fable 5 の判断へ**（本記録では判断しない）：段3のB確定runを実行してよいか、決定64の層2監視（>48）の扱い、決定65のAC7±2台超過時の運用。
