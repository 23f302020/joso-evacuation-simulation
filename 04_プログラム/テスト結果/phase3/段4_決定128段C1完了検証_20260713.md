# 段4：決定128 段C-1完了検証（2026-07-13）

## メタ

- **作成**：2026-07-13
- **実装者**：Codex
- **検証**：Opus（実行検証）
- **記録**：Sonnet 5
- **正本**：[[段4_計画判断_方針判断_fable5]] 決定126〜134

---

## 段C-1 実装内容（実装者・4ファイル限定）

- `VERIFIED_COMPLETION_RATES` を完全削除・主系列の符号/帯/summaryをbuild_metricsの実データ算出値へ一本化
- summaryとper-run行の自己整合assert追加
- S10のA集合を主系列の実算出値から取得・S10にraw/保守の完了率・15組符号・符号カウント追加・raw/保守の件数/列パリティassert追加
- 循環テストを削除し、archiveのtripinfoを独立パースする地上真実テストへ置換

**変更4ファイル：**
- `scripts/p3_phase3r_e1_bands.py`
- `scripts/p3_s10_sensitivity_metrics.py`
- `tests/unit/test_phase3r_e1_bands.py`
- `tests/unit/test_s10_sensitivity_metrics.py`

---

## Opus独立検証結果（実行確認）

- **ハードコード残存0**：`VERIFIED_COMPLETION_RATES` を scripts / tests から grep＝0件
- **循環テスト残存0**：`decision_110_sign_counts` 0件
- **新地上真実テスト確認**：
  - `test_build_metrics_matches_archive_ground_truth` が **tripinfo XMLを独立パース**（`ET.iterparse`）し到着数が `[1341,1066,1349,…]` と一致することをassert（循環でない）
  - `raw_sign_counts=={positive:13,negative:2,zero:0}`
  - `conservative_sign_counts=={positive:13,negative:2,zero:0}`
  - `test_summary_consistency_assert_halts_on_mismatch` で自己整合assertがmismatchでhaltすることを検証
- **S10テスト確認**：
  - `raw_sign_counts=={positive:10,negative:5}`
  - `conservative_sign_counts=={positive:8,negative:7}`
  - `s10_4 raw_completion_rate>1.0 かつ conservative_completion_rate<=1.0`
- **テスト実行結果**：関連2テスト **7 passed**（`test_phase3r_e1_bands.py`＋`test_s10_sensitivity_metrics.py`）。※他4テスト（`test_sumo_seed`等）はOpus環境でSUMO_HOME未設定のため収集エラー＝環境差。実装者環境では全**77 passed**
- **4値ゲート事前合格**（地上真実テストにより）：
  - 主raw **正13/負2**
  - 主保守 **正13/負2**
  - S raw **正10/負5**
  - S保守 **正8/負7**
  （すべて独立tripinfoに対し検証）
- **段C-1の実変更は4ファイル**（scripts 2＋tests 2）。`git diff --stat` の252ファイルは会話開始前からの既存EOL churn（内容同一・段C-1由来ではない）
- route SHA `3d6977…8297`・bare LF 0・CRLF維持

---

## 未実施（段C-2以降）

正本CSV/JSON（band_summary・符号表・replicate_metrics・S10系）の再生成、E2・Excel・V2/V3の訂正カスケード、単一コミット。**現時点で正本ファイルは旧不良値のまま**（修正コードは正値を産出するが、ディスク上の成果物は未再生成）。

---

## 総括

段C-1は決定131/132/134の要求（ハードコード削除・テスト脱循環化・自己整合assert・S10保守新設）を満たし、4値ゲートは地上真実テストで事前合格。主結論ヌル不変。次は段C-2（正本再生成）。
